# coding: utf-8

"""
Margin WebSocket Market Streams

Access account information, borrow and repay assets, and trade with Binance Margin.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations
import json
import pprint
from pydantic import (
    BaseModel,
    ConfigDict,
    ValidationError,
    field_validator,
)
from typing import Any, Optional
from binance_sdk_margin_trading.websocket_streams.models.margin_level_status_change import (
    MarginLevelStatusChange,
)
from binance_sdk_margin_trading.websocket_streams.models.user_liability_change import (
    UserLiabilityChange,
)
from pydantic import model_validator
from typing import Union, Set, Dict
from typing_extensions import Self

RISKDATASTREAMEVENTSRESPONSE_ONE_OF_SCHEMAS = [
    "MarginLevelStatusChange",
    "UserLiabilityChange",
]

# The `oneof_schema_N_validator` field each event schema is declared under, set
# alongside `actual_instance` so that a routed event is reachable under both names.
RISKDATASTREAMEVENTSRESPONSE_VALIDATOR_FIELD_MAP = {
    MarginLevelStatusChange: "oneof_schema_1_validator",
    UserLiabilityChange: "oneof_schema_2_validator",
}

# The event schemas, keyed by the event name the `e` field of the payload carries.
RISKDATASTREAMEVENTSRESPONSE_EVENT_SCHEMA_MAP = {
    "MARGIN_LEVEL_STATUS_CHANGE": MarginLevelStatusChange,
    "USER_LIABILITY_CHANGE": UserLiabilityChange,
}

# The same schemas, with their validator field, keyed by event name without
# separators or case, which is how an incoming event is looked up.
RISKDATASTREAMEVENTSRESPONSE_EVENT_TYPE_MAP = {
    event_name.replace("_", "").replace("-", "").lower(): (schema, validator_field)
    for event_name, schema in RISKDATASTREAMEVENTSRESPONSE_EVENT_SCHEMA_MAP.items()
    if (validator_field := RISKDATASTREAMEVENTSRESPONSE_VALIDATOR_FIELD_MAP.get(schema))
}


class RiskDataStreamEventsResponse(BaseModel):
    """
    RiskDataStreamEventsResponse
    """

    # data type: MarginLevelStatusChange
    oneof_schema_1_validator: Optional[MarginLevelStatusChange] = None
    # data type: UserLiabilityChange
    oneof_schema_2_validator: Optional[UserLiabilityChange] = None
    actual_instance: Optional[Union[MarginLevelStatusChange, UserLiabilityChange]] = (
        None
    )
    one_of_schemas: Set[str] = {"MarginLevelStatusChange", "UserLiabilityChange"}

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )

    discriminator_value_class_map: Dict[str, str] = {}

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError(
                    "If a position argument is used, only 1 is allowed to set `actual_instance`"
                )
            if kwargs:
                raise ValueError(
                    "If a position argument is used, keyword arguments cannot be used."
                )
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @classmethod
    def resolve_event_schema(cls, event_name: Any) -> Optional[Any]:
        """Returns the `(schema, validator field)` pair `event_name` belongs to.

        The event name is the only thing telling user data events apart: their
        schemas declare every field as optional, so most of them validate any
        event. Names are compared without separators or case, to accept both
        `executionReport` and `ORDER_TRADE_UPDATE`.
        """
        if not isinstance(event_name, str):
            return None

        key = event_name.replace("_", "").replace("-", "").lower()
        return RISKDATASTREAMEVENTSRESPONSE_EVENT_TYPE_MAP.get(key)

    @model_validator(mode="before")
    @classmethod
    def route_user_data_event(cls, data: Any) -> Any:
        """Deserializes a raw user data event into the schema its `e` field names."""
        if not isinstance(data, dict) or "actual_instance" in data:
            return data

        resolved = cls.resolve_event_schema(data.get("e"))
        if resolved is None:
            return data

        schema, validator_field = resolved

        instance = schema.from_dict(data)
        return {"actual_instance": instance, validator_field: instance}

    @field_validator("actual_instance")
    def actual_instance_must_validate_oneof(cls, v):

        error_messages = []
        match = 0
        # validate data type: MarginLevelStatusChange
        if not isinstance(v, MarginLevelStatusChange):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `MarginLevelStatusChange`"
            )
        else:
            match += 1
        # validate data type: UserLiabilityChange
        if not isinstance(v, UserLiabilityChange):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `UserLiabilityChange`"
            )
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when setting `actual_instance` in RiskDataStreamEventsResponse with oneOf schemas: MarginLevelStatusChange, UserLiabilityChange. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when setting `actual_instance` in RiskDataStreamEventsResponse with oneOf schemas: MarginLevelStatusChange, UserLiabilityChange. Details: "
                + ", ".join(error_messages)
            )
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        try:
            parsed = json.loads(json_str)
        except (TypeError, ValueError):
            parsed = None

        if isinstance(parsed, dict) and cls.resolve_event_schema(parsed.get("e")):
            return cls.model_validate(parsed)

        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into MarginLevelStatusChange
        try:
            instance.actual_instance = MarginLevelStatusChange.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into UserLiabilityChange
        try:
            instance.actual_instance = UserLiabilityChange.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when deserializing the JSON string into RiskDataStreamEventsResponse with oneOf schemas: MarginLevelStatusChange, UserLiabilityChange. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when deserializing the JSON string into RiskDataStreamEventsResponse with oneOf schemas: MarginLevelStatusChange, UserLiabilityChange. Details: "
                + ", ".join(error_messages)
            )
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(
            self.actual_instance.to_json
        ):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(
        self,
    ) -> Optional[Union[Dict[str, Any], MarginLevelStatusChange, UserLiabilityChange]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(
            self.actual_instance.to_dict
        ):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())
