"""
Binance Fiat REST API

OpenAPI Specification for the Binance Fiat REST API
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_fiat.rest_api.api import FiatApi
from binance_sdk_fiat.rest_api.models import GetFiatDepositWithdrawHistoryResponse
from binance_sdk_fiat.rest_api.models import GetFiatPaymentsHistoryResponse


class TestFiatApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = FiatApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    @patch("binance_common.utils.get_signature")
    def test_get_fiat_deposit_withdraw_history_success(self, mock_get_signature):
        """Test get_fiat_deposit_withdraw_history() successfully with required parameters only."""

        params = {
            "transaction_type": "transaction_type_example",
        }

        expected_response = {
            "code": "000000",
            "message": "success",
            "data": [
                {
                    "orderNo": "7d76d611-0568-4f43-afb6-24cac7767365",
                    "fiatCurrency": "BRL",
                    "indicatedAmount": "10.00",
                    "amount": "10.00",
                    "totalFee": "0.00",
                    "method": "BankAccount",
                    "status": "Expired",
                    "createTime": 1626144956000,
                    "updateTime": 1626400907000,
                }
            ],
            "total": 1,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_fiat_deposit_withdraw_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/fiat/orders" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["transactionType"] == "transaction_type_example"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetFiatDepositWithdrawHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetFiatDepositWithdrawHistoryResponse, "from_dict")
        ):
            expected = GetFiatDepositWithdrawHistoryResponse.from_dict(
                expected_response
            )
        else:
            expected = GetFiatDepositWithdrawHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_fiat_deposit_withdraw_history_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_fiat_deposit_withdraw_history() successfully with optional parameters."""

        params = {
            "transaction_type": "transaction_type_example",
            "begin_time": 56,
            "end_time": 1641782889000,
            "page": 1,
            "rows": 100,
            "recv_window": 5000,
        }

        expected_response = {
            "code": "000000",
            "message": "success",
            "data": [
                {
                    "orderNo": "7d76d611-0568-4f43-afb6-24cac7767365",
                    "fiatCurrency": "BRL",
                    "indicatedAmount": "10.00",
                    "amount": "10.00",
                    "totalFee": "0.00",
                    "method": "BankAccount",
                    "status": "Expired",
                    "createTime": 1626144956000,
                    "updateTime": 1626400907000,
                }
            ],
            "total": 1,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_fiat_deposit_withdraw_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/fiat/orders" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetFiatDepositWithdrawHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetFiatDepositWithdrawHistoryResponse, "from_dict")
        ):
            expected = GetFiatDepositWithdrawHistoryResponse.from_dict(
                expected_response
            )
        else:
            expected = GetFiatDepositWithdrawHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_fiat_deposit_withdraw_history_missing_required_param_transaction_type(
        self,
    ):
        """Test that get_fiat_deposit_withdraw_history() raises RequiredError when 'transaction_type' is missing."""
        params = {
            "transaction_type": "transaction_type_example",
        }
        params["transaction_type"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'transaction_type'"
        ):
            self.client.get_fiat_deposit_withdraw_history(**params)

    def test_get_fiat_deposit_withdraw_history_server_error(self):
        """Test that get_fiat_deposit_withdraw_history() raises an error when the server returns an error."""

        params = {
            "transaction_type": "transaction_type_example",
        }

        mock_error = Exception("ResponseError")
        self.client.get_fiat_deposit_withdraw_history = MagicMock(
            side_effect=mock_error
        )

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_fiat_deposit_withdraw_history(**params)

    @patch("binance_common.utils.get_signature")
    def test_get_fiat_payments_history_success(self, mock_get_signature):
        """Test get_fiat_payments_history() successfully with required parameters only."""

        params = {
            "transaction_type": "transaction_type_example",
        }

        expected_response = {
            "code": "000000",
            "message": "success",
            "data": [
                {
                    "orderNo": "353fca443f06466db0c4dc89f94f027a",
                    "sourceAmount": "20.0",
                    "fiatCurrency": "EUR",
                    "obtainAmount": "4.462",
                    "cryptoCurrency": "LUNA",
                    "totalFee": "0.2",
                    "price": "4.437472",
                    "status": "Failed",
                    "paymentMethod": "Credit Card",
                    "createTime": 1624529919000,
                    "updateTime": 1624529919000,
                }
            ],
            "total": 1,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_fiat_payments_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/fiat/payments" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["transactionType"] == "transaction_type_example"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetFiatPaymentsHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof or is_list or hasattr(GetFiatPaymentsHistoryResponse, "from_dict")
        ):
            expected = GetFiatPaymentsHistoryResponse.from_dict(expected_response)
        else:
            expected = GetFiatPaymentsHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_fiat_payments_history_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_fiat_payments_history() successfully with optional parameters."""

        params = {
            "transaction_type": "transaction_type_example",
            "begin_time": 56,
            "end_time": 1641782889000,
            "page": 1,
            "rows": 100,
            "recv_window": 5000,
        }

        expected_response = {
            "code": "000000",
            "message": "success",
            "data": [
                {
                    "orderNo": "353fca443f06466db0c4dc89f94f027a",
                    "sourceAmount": "20.0",
                    "fiatCurrency": "EUR",
                    "obtainAmount": "4.462",
                    "cryptoCurrency": "LUNA",
                    "totalFee": "0.2",
                    "price": "4.437472",
                    "status": "Failed",
                    "paymentMethod": "Credit Card",
                    "createTime": 1624529919000,
                    "updateTime": 1624529919000,
                }
            ],
            "total": 1,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_fiat_payments_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/fiat/payments" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetFiatPaymentsHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof or is_list or hasattr(GetFiatPaymentsHistoryResponse, "from_dict")
        ):
            expected = GetFiatPaymentsHistoryResponse.from_dict(expected_response)
        else:
            expected = GetFiatPaymentsHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_fiat_payments_history_missing_required_param_transaction_type(self):
        """Test that get_fiat_payments_history() raises RequiredError when 'transaction_type' is missing."""
        params = {
            "transaction_type": "transaction_type_example",
        }
        params["transaction_type"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'transaction_type'"
        ):
            self.client.get_fiat_payments_history(**params)

    def test_get_fiat_payments_history_server_error(self):
        """Test that get_fiat_payments_history() raises an error when the server returns an error."""

        params = {
            "transaction_type": "transaction_type_example",
        }

        mock_error = Exception("ResponseError")
        self.client.get_fiat_payments_history = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_fiat_payments_history(**params)
