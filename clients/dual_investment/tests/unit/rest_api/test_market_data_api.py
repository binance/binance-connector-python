"""
Binance Dual Investment REST API

OpenAPI Specification for the Binance Dual Investment REST API
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_dual_investment.rest_api.api import MarketDataApi
from binance_sdk_dual_investment.rest_api.models import (
    GetDualInvestmentProductListResponse,
)


class TestMarketDataApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = MarketDataApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    @patch("binance_common.utils.get_signature")
    def test_get_dual_investment_product_list_success(self, mock_get_signature):
        """Test get_dual_investment_product_list() successfully with required parameters only."""

        params = {
            "option_type": "option_type_example",
            "exercised_coin": "exercised_coin_example",
            "invest_coin": "invest_coin_example",
        }

        expected_response = {
            "total": 1,
            "list": [
                {
                    "id": "741590",
                    "investCoin": "USDT",
                    "exercisedCoin": "BNB",
                    "strikePrice": "380",
                    "duration": 4,
                    "settleDate": 1709020800000,
                    "purchaseDecimal": 8,
                    "purchaseEndTime": 1708934400000,
                    "canPurchase": True,
                    "apr": "0.6076",
                    "orderId": 8257205859,
                    "minAmount": "0.1",
                    "maxAmount": "25265.7",
                    "createTimestamp": 1708560084000,
                    "optionType": "PUT",
                    "isAutoCompoundEnable": True,
                    "autoCompoundPlanList": ["STANDARD", "ADVANCE"],
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_dual_investment_product_list(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/dci/product/list" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["optionType"] == "option_type_example"
        assert normalized["exercisedCoin"] == "exercised_coin_example"
        assert normalized["investCoin"] == "invest_coin_example"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetDualInvestmentProductListResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetDualInvestmentProductListResponse, "from_dict")
        ):
            expected = GetDualInvestmentProductListResponse.from_dict(expected_response)
        else:
            expected = GetDualInvestmentProductListResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_dual_investment_product_list_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_dual_investment_product_list() successfully with optional parameters."""

        params = {
            "option_type": "option_type_example",
            "exercised_coin": "exercised_coin_example",
            "invest_coin": "invest_coin_example",
            "page_size": 10,
            "page_index": 1,
            "recv_window": 5000,
        }

        expected_response = {
            "total": 1,
            "list": [
                {
                    "id": "741590",
                    "investCoin": "USDT",
                    "exercisedCoin": "BNB",
                    "strikePrice": "380",
                    "duration": 4,
                    "settleDate": 1709020800000,
                    "purchaseDecimal": 8,
                    "purchaseEndTime": 1708934400000,
                    "canPurchase": True,
                    "apr": "0.6076",
                    "orderId": 8257205859,
                    "minAmount": "0.1",
                    "maxAmount": "25265.7",
                    "createTimestamp": 1708560084000,
                    "optionType": "PUT",
                    "isAutoCompoundEnable": True,
                    "autoCompoundPlanList": ["STANDARD", "ADVANCE"],
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_dual_investment_product_list(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/dci/product/list" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetDualInvestmentProductListResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetDualInvestmentProductListResponse, "from_dict")
        ):
            expected = GetDualInvestmentProductListResponse.from_dict(expected_response)
        else:
            expected = GetDualInvestmentProductListResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_dual_investment_product_list_missing_required_param_option_type(self):
        """Test that get_dual_investment_product_list() raises RequiredError when 'option_type' is missing."""
        params = {
            "option_type": "option_type_example",
            "exercised_coin": "exercised_coin_example",
            "invest_coin": "invest_coin_example",
        }
        params["option_type"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'option_type'"
        ):
            self.client.get_dual_investment_product_list(**params)

    def test_get_dual_investment_product_list_missing_required_param_exercised_coin(
        self,
    ):
        """Test that get_dual_investment_product_list() raises RequiredError when 'exercised_coin' is missing."""
        params = {
            "option_type": "option_type_example",
            "exercised_coin": "exercised_coin_example",
            "invest_coin": "invest_coin_example",
        }
        params["exercised_coin"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'exercised_coin'"
        ):
            self.client.get_dual_investment_product_list(**params)

    def test_get_dual_investment_product_list_missing_required_param_invest_coin(self):
        """Test that get_dual_investment_product_list() raises RequiredError when 'invest_coin' is missing."""
        params = {
            "option_type": "option_type_example",
            "exercised_coin": "exercised_coin_example",
            "invest_coin": "invest_coin_example",
        }
        params["invest_coin"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'invest_coin'"
        ):
            self.client.get_dual_investment_product_list(**params)

    def test_get_dual_investment_product_list_server_error(self):
        """Test that get_dual_investment_product_list() raises an error when the server returns an error."""

        params = {
            "option_type": "option_type_example",
            "exercised_coin": "exercised_coin_example",
            "invest_coin": "invest_coin_example",
        }

        mock_error = Exception("ResponseError")
        self.client.get_dual_investment_product_list = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_dual_investment_product_list(**params)
