"""
Binance Convert REST API

OpenAPI Specification for the Binance Convert REST API
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.utils import is_one_of_model

from binance_convert.rest_api.api import MarketDataApi
from binance_convert.rest_api.models import ListAllConvertPairsResponse
from binance_convert.rest_api.models import QueryOrderQuantityPrecisionPerAssetResponse


class TestMarketDataApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = MarketDataApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    def test_list_all_convert_pairs_success(self):
        """Test list_all_convert_pairs() successfully with required parameters only."""

        expected_response = [
            {
                "fromAsset": "BTC",
                "toAsset": "USDT",
                "fromAssetMinAmount": "0.0004",
                "fromAssetMaxAmount": "50",
                "toAssetMinAmount": "20",
                "toAssetMaxAmount": "2500000",
            }
        ]

        self.set_mock_response(expected_response)

        response = self.client.list_all_convert_pairs()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert "/sapi/v1/convert/exchangeInfo" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(ListAllConvertPairsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list:
            expected = ListAllConvertPairsResponse.from_dict(expected_response)
        else:
            expected = ListAllConvertPairsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_list_all_convert_pairs_success_with_optional_params(self):
        """Test list_all_convert_pairs() successfully with optional parameters."""

        params = {"from_asset": "from_asset_example", "to_asset": "to_asset_example"}

        expected_response = [
            {
                "fromAsset": "BTC",
                "toAsset": "USDT",
                "fromAssetMinAmount": "0.0004",
                "fromAssetMaxAmount": "50",
                "toAssetMinAmount": "20",
                "toAssetMaxAmount": "2500000",
            }
        ]

        self.set_mock_response(expected_response)

        response = self.client.list_all_convert_pairs(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/sapi/v1/convert/exchangeInfo" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(ListAllConvertPairsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list:
            expected = ListAllConvertPairsResponse.from_dict(expected_response)
        else:
            expected = ListAllConvertPairsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_list_all_convert_pairs_server_error(self):
        """Test that list_all_convert_pairs() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.list_all_convert_pairs = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.list_all_convert_pairs()

    @patch("binance_common.utils.get_signature")
    def test_query_order_quantity_precision_per_asset_success(self, mock_get_signature):
        """Test query_order_quantity_precision_per_asset() successfully with required parameters only."""

        expected_response = [
            {"asset": "BTC", "fraction": 8},
            {"asset": "SHIB", "fraction": 2},
        ]
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.query_order_quantity_precision_per_asset()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/convert/assetInfo" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(QueryOrderQuantityPrecisionPerAssetResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list:
            expected = QueryOrderQuantityPrecisionPerAssetResponse.from_dict(
                expected_response
            )
        else:
            expected = QueryOrderQuantityPrecisionPerAssetResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_query_order_quantity_precision_per_asset_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test query_order_quantity_precision_per_asset() successfully with optional parameters."""

        params = {"recv_window": 5000}

        expected_response = [
            {"asset": "BTC", "fraction": 8},
            {"asset": "SHIB", "fraction": 2},
        ]
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.query_order_quantity_precision_per_asset(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/convert/assetInfo" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(QueryOrderQuantityPrecisionPerAssetResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list:
            expected = QueryOrderQuantityPrecisionPerAssetResponse.from_dict(
                expected_response
            )
        else:
            expected = QueryOrderQuantityPrecisionPerAssetResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_query_order_quantity_precision_per_asset_server_error(self):
        """Test that query_order_quantity_precision_per_asset() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.query_order_quantity_precision_per_asset = MagicMock(
            side_effect=mock_error
        )

        with pytest.raises(Exception, match="ResponseError"):
            self.client.query_order_quantity_precision_per_asset()
