"""
Convert REST API

Request quotes and execute cryptocurrency conversions via the Convert REST API.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from typing import Optional, Union
from requests import Session
from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.models import ApiResponse
from binance_common.signature import Signers
from binance_common.utils import send_request

from ..models import AcceptQuoteResponse
from ..models import CancelLimitOrderResponse
from ..models import GetConvertTradeHistoryResponse
from ..models import OrderStatusResponse
from ..models import PlaceLimitOrderResponse
from ..models import QueryLimitOpenOrdersResponse
from ..models import SendQuoteRequestResponse


from ..models import PlaceLimitOrderSideEnum
from ..models import PlaceLimitOrderExpiredTypeEnum
from ..models import PlaceLimitOrderWalletTypeEnum
from ..models import SendQuoteRequestWalletTypeEnum
from ..models import SendQuoteRequestValidTimeEnum


class TradeApi:
    """API Client for TradeApi endpoints."""

    def __init__(
        self,
        configuration: ConfigurationRestAPI = None,
        session: Session = None,
        signer: Signers = None,
    ) -> None:
        self._configuration = configuration
        self._session = session
        self._signer = signer

    def accept_quote(
        self,
        quote_id: Union[str, None],
        recv_window: Optional[int] = None,
    ) -> ApiResponse[AcceptQuoteResponse]:
        """
                Accept Quote (TRADE)
                POST /sapi/v1/convert/acceptQuote
                https://developers.binance.com/en/docs/catalog/core-trading-convert/api/rest-api/trade#accept-quote

                Accept the offered quote by quote ID.

        Weight(UID): 500

        Security Type: TRADE

                Args:
                    quote_id (Union[str, None]):
                    recv_window (Optional[int] = None): Request validity window in milliseconds

                Returns:
                    ApiResponse[AcceptQuoteResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        if quote_id is None:
            raise RequiredError(
                field="quote_id", error_message="Missing required parameter 'quote_id'"
            )

        body = {}
        payload = {"quote_id": quote_id, "recv_window": recv_window}

        return send_request(
            self._session,
            self._configuration,
            method="POST",
            path="/sapi/v1/convert/acceptQuote",
            payload=payload,
            body=body,
            time_unit=self._configuration.time_unit,
            response_model=AcceptQuoteResponse,
            is_signed=True,
            signer=self._signer,
        )

    def cancel_limit_order(
        self,
        order_id: Union[int, None],
        recv_window: Optional[int] = None,
    ) -> ApiResponse[CancelLimitOrderResponse]:
        """
                Cancel limit order (TRADE)
                POST /sapi/v1/convert/limit/cancelOrder
                https://developers.binance.com/en/docs/catalog/core-trading-convert/api/rest-api/trade#cancel-limit-order

                Enable users to cancel a limit order

        Weight(UID): 200

        Security Type: TRADE

                Args:
                    order_id (Union[int, None]): The orderId from `placeOrder` api
                    recv_window (Optional[int] = None): Request validity window in milliseconds

                Returns:
                    ApiResponse[CancelLimitOrderResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        if order_id is None:
            raise RequiredError(
                field="order_id", error_message="Missing required parameter 'order_id'"
            )

        body = {}
        payload = {"order_id": order_id, "recv_window": recv_window}

        return send_request(
            self._session,
            self._configuration,
            method="POST",
            path="/sapi/v1/convert/limit/cancelOrder",
            payload=payload,
            body=body,
            time_unit=self._configuration.time_unit,
            response_model=CancelLimitOrderResponse,
            is_signed=True,
            signer=self._signer,
        )

    def get_convert_trade_history(
        self,
        start_time: Union[int, None],
        end_time: Union[int, None],
        limit: Optional[int] = None,
        recv_window: Optional[int] = None,
    ) -> ApiResponse[GetConvertTradeHistoryResponse]:
        """
                Get Convert Trade History (USER_DATA)
                GET /sapi/v1/convert/tradeFlow
                https://developers.binance.com/en/docs/catalog/core-trading-convert/api/rest-api/trade#get-convert-trade-history

                Get Convert Trade History

        Weight(UID): 3000

        Security Type: USER_DATA

        Notes:
        - The max interval between `startTime` and `endTime` is 30 days.

                Args:
                    start_time (Union[int, None]):
                    end_time (Union[int, None]):
                    limit (Optional[int] = None): Number of records to return
                    recv_window (Optional[int] = None): Request validity window in milliseconds

                Returns:
                    ApiResponse[GetConvertTradeHistoryResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        if start_time is None:
            raise RequiredError(
                field="start_time",
                error_message="Missing required parameter 'start_time'",
            )
        if end_time is None:
            raise RequiredError(
                field="end_time", error_message="Missing required parameter 'end_time'"
            )

        body = {}
        payload = {
            "start_time": start_time,
            "end_time": end_time,
            "limit": limit,
            "recv_window": recv_window,
        }

        return send_request(
            self._session,
            self._configuration,
            method="GET",
            path="/sapi/v1/convert/tradeFlow",
            payload=payload,
            body=body,
            time_unit=self._configuration.time_unit,
            response_model=GetConvertTradeHistoryResponse,
            is_signed=True,
            signer=self._signer,
        )

    def order_status(
        self,
        order_id: Optional[str] = None,
        quote_id: Optional[str] = None,
    ) -> ApiResponse[OrderStatusResponse]:
        """
                Order status (USER_DATA)
                GET /sapi/v1/convert/orderStatus
                https://developers.binance.com/en/docs/catalog/core-trading-convert/api/rest-api/trade#order-status

                Query order status by order ID.

        Weight(UID): 100

        Security Type: USER_DATA

                Args:
                    order_id (Optional[str] = None): Either orderId or quoteId is required
                    quote_id (Optional[str] = None): Either orderId or quoteId is required

                Returns:
                    ApiResponse[OrderStatusResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        body = {}
        payload = {"order_id": order_id, "quote_id": quote_id}

        return send_request(
            self._session,
            self._configuration,
            method="GET",
            path="/sapi/v1/convert/orderStatus",
            payload=payload,
            body=body,
            time_unit=self._configuration.time_unit,
            response_model=OrderStatusResponse,
            is_signed=True,
            signer=self._signer,
        )

    def place_limit_order(
        self,
        base_asset: Union[str, None],
        quote_asset: Union[str, None],
        limit_price: Union[float, None],
        side: Union[PlaceLimitOrderSideEnum, None],
        expired_type: Union[PlaceLimitOrderExpiredTypeEnum, None],
        base_amount: Optional[float] = None,
        quote_amount: Optional[float] = None,
        wallet_type: Optional[PlaceLimitOrderWalletTypeEnum] = None,
        recv_window: Optional[int] = None,
    ) -> ApiResponse[PlaceLimitOrderResponse]:
        """
                Place limit order (TRADE)
                POST /sapi/v1/convert/limit/placeOrder
                https://developers.binance.com/en/docs/catalog/core-trading-convert/api/rest-api/trade#place-limit-order

                Enable users to place a limit order

        Weight(UID): 500

        Security Type: TRADE

        Notes:
        - `baseAsset` and `quoteAsset` can be determined via the `exchangeInfo` endpoint.
        - Limit price is defined from `baseAsset` to `quoteAsset`.
        - Exactly one of `baseAmount` or `quoteAmount` should be sent.

                Args:
                    base_asset (Union[str, None]): base asset (use the response `fromIsBase` from `GET /sapi/v1/convert/exchangeInfo` api to check
        which one is baseAsset )
                    quote_asset (Union[str, None]): quote asset
                    limit_price (Union[float, None]): Symbol limit price (from baseAsset to quoteAsset)
                    side (Union[PlaceLimitOrderSideEnum, None]): `BUY` or `SELL`
                    expired_type (Union[PlaceLimitOrderExpiredTypeEnum, None]): Order expiry duration. 1_D, 3_D, 7_D, 30_D (D means day)
                    base_amount (Optional[float] = None): Base asset amount. (One of `baseAmount` or `quoteAmount` is required)
                    quote_amount (Optional[float] = None): Quote asset amount. (One of `baseAmount` or `quoteAmount` is required)
                    wallet_type (Optional[PlaceLimitOrderWalletTypeEnum] = None): Wallet to use for payment. Supported values: `SPOT`, `FUNDING`, `EARN`.
        Combined wallets also supported: `SPOT_FUNDING`, `FUNDING_EARN`, `SPOT_FUNDING_EARN`, `SPOT_EARN`. Default is `SPOT`.
                    recv_window (Optional[int] = None): Request validity window in milliseconds

                Returns:
                    ApiResponse[PlaceLimitOrderResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        if base_asset is None:
            raise RequiredError(
                field="base_asset",
                error_message="Missing required parameter 'base_asset'",
            )
        if quote_asset is None:
            raise RequiredError(
                field="quote_asset",
                error_message="Missing required parameter 'quote_asset'",
            )
        if limit_price is None:
            raise RequiredError(
                field="limit_price",
                error_message="Missing required parameter 'limit_price'",
            )
        if side is None:
            raise RequiredError(
                field="side", error_message="Missing required parameter 'side'"
            )
        if expired_type is None:
            raise RequiredError(
                field="expired_type",
                error_message="Missing required parameter 'expired_type'",
            )

        body = {}
        payload = {
            "base_asset": base_asset,
            "quote_asset": quote_asset,
            "limit_price": limit_price,
            "side": side,
            "expired_type": expired_type,
            "base_amount": base_amount,
            "quote_amount": quote_amount,
            "wallet_type": wallet_type,
            "recv_window": recv_window,
        }

        return send_request(
            self._session,
            self._configuration,
            method="POST",
            path="/sapi/v1/convert/limit/placeOrder",
            payload=payload,
            body=body,
            time_unit=self._configuration.time_unit,
            response_model=PlaceLimitOrderResponse,
            is_signed=True,
            signer=self._signer,
        )

    def query_limit_open_orders(
        self,
        recv_window: Optional[int] = None,
    ) -> ApiResponse[QueryLimitOpenOrdersResponse]:
        """
                Query limit open orders (USER_DATA)
                GET /sapi/v1/convert/limit/queryOpenOrders
                https://developers.binance.com/en/docs/catalog/core-trading-convert/api/rest-api/trade#query-limit-open-orders

                Query current open limit orders

        Weight(UID): 3000

        Security Type: USER_DATA

                Args:
                    recv_window (Optional[int] = None): Request validity window in milliseconds

                Returns:
                    ApiResponse[QueryLimitOpenOrdersResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        body = {}
        payload = {"recv_window": recv_window}

        return send_request(
            self._session,
            self._configuration,
            method="GET",
            path="/sapi/v1/convert/limit/queryOpenOrders",
            payload=payload,
            body=body,
            time_unit=self._configuration.time_unit,
            response_model=QueryLimitOpenOrdersResponse,
            is_signed=True,
            signer=self._signer,
        )

    def send_quote_request(
        self,
        from_asset: Union[str, None],
        to_asset: Union[str, None],
        from_amount: Optional[float] = None,
        to_amount: Optional[float] = None,
        wallet_type: Optional[SendQuoteRequestWalletTypeEnum] = None,
        valid_time: Optional[SendQuoteRequestValidTimeEnum] = None,
        recv_window: Optional[int] = None,
    ) -> ApiResponse[SendQuoteRequestResponse]:
        """
                Send Quote Request (TRADE)
                POST /sapi/v1/convert/getQuote
                https://developers.binance.com/en/docs/catalog/core-trading-convert/api/rest-api/trade#send-quote-request

                Request a quote for the requested token pairs

        Weight(UID): 200

        Security Type: TRADE

        Notes:
        - Either `fromAmount` or `toAmount` should be sent.
        - `quoteId` is returned only if you have enough funds to convert.

                Args:
                    from_asset (Union[str, None]):
                    to_asset (Union[str, None]):
                    from_amount (Optional[float] = None): When specified, it is the amount you will be debited after the conversion
                    to_amount (Optional[float] = None): When specified, it is the amount you will be credited after the conversion
                    wallet_type (Optional[SendQuoteRequestWalletTypeEnum] = None): Wallet to use for payment. Supported values: `SPOT`, `FUNDING`, `EARN`.
        Combined wallets also supported: `SPOT_FUNDING`, `FUNDING_EARN`, `SPOT_FUNDING_EARN`, `SPOT_EARN`. Default is `SPOT`.
                    valid_time (Optional[SendQuoteRequestValidTimeEnum] = None): Quote valid duration. Supported values: 10s, 30s, 1m. Default is 10s.
                    recv_window (Optional[int] = None): Request validity window in milliseconds

                Returns:
                    ApiResponse[SendQuoteRequestResponse]

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        if from_asset is None:
            raise RequiredError(
                field="from_asset",
                error_message="Missing required parameter 'from_asset'",
            )
        if to_asset is None:
            raise RequiredError(
                field="to_asset", error_message="Missing required parameter 'to_asset'"
            )

        body = {}
        payload = {
            "from_asset": from_asset,
            "to_asset": to_asset,
            "from_amount": from_amount,
            "to_amount": to_amount,
            "wallet_type": wallet_type,
            "valid_time": valid_time,
            "recv_window": recv_window,
        }

        return send_request(
            self._session,
            self._configuration,
            method="POST",
            path="/sapi/v1/convert/getQuote",
            payload=payload,
            body=body,
            time_unit=self._configuration.time_unit,
            response_model=SendQuoteRequestResponse,
            is_signed=True,
            signer=self._signer,
        )
