"""
Stocks Trading REST API

REST APIs for Binance Stocks Trading. All endpoints under `/sapi/v1/equity/*`.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_stocks.rest_api.api import TokenizedApi
from binance_sdk_stocks.rest_api.models import TokenizedConvertHistoryResponse
from binance_sdk_stocks.rest_api.models import TokenizedConvertStatusResponse
from binance_sdk_stocks.rest_api.models import TokenizedMintResponse
from binance_sdk_stocks.rest_api.models import TokenizedRedeemResponse
from binance_sdk_stocks.rest_api.models import TokenizedConvertStatusConvertTypeEnum


class TestTokenizedApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = TokenizedApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    @patch("binance_common.utils.get_signature")
    def test_tokenized_convert_history_success(self, mock_get_signature):
        """Test tokenized_convert_history() successfully with required parameters only."""

        expected_response = {
            "rows": [
                {
                    "underlyingAsset": "AAPL",
                    "underlyingAssetAmount": "1.000000000",
                    "tokenizedAsset": "AAPLB",
                    "tokenizedAssetAmount": "1.000000000",
                    "issuerRequestId": "mint-20260505-8f3b9e1a2d3c4b5a",
                    "convertType": "MINT",
                    "status": "S",
                    "createdAt": 1735900000000,
                    "updatedAt": 1735900060000,
                }
            ],
            "hasMore": True,
            "nextLastId": 10020,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.tokenized_convert_history()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/tokenized/history" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TokenizedConvertHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof or is_list or hasattr(TokenizedConvertHistoryResponse, "from_dict")
        ):
            expected = TokenizedConvertHistoryResponse.from_dict(expected_response)
        else:
            expected = TokenizedConvertHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_tokenized_convert_history_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test tokenized_convert_history() successfully with optional parameters."""

        params = {
            "start_time": 1735800000000,
            "end_time": 1735900000000,
            "last_id": 10019,
            "size": 20,
            "recv_window": 5000,
        }

        expected_response = {
            "rows": [
                {
                    "underlyingAsset": "AAPL",
                    "underlyingAssetAmount": "1.000000000",
                    "tokenizedAsset": "AAPLB",
                    "tokenizedAssetAmount": "1.000000000",
                    "issuerRequestId": "mint-20260505-8f3b9e1a2d3c4b5a",
                    "convertType": "MINT",
                    "status": "S",
                    "createdAt": 1735900000000,
                    "updatedAt": 1735900060000,
                }
            ],
            "hasMore": True,
            "nextLastId": 10020,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.tokenized_convert_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/tokenized/history" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TokenizedConvertHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof or is_list or hasattr(TokenizedConvertHistoryResponse, "from_dict")
        ):
            expected = TokenizedConvertHistoryResponse.from_dict(expected_response)
        else:
            expected = TokenizedConvertHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_tokenized_convert_history_server_error(self):
        """Test that tokenized_convert_history() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.tokenized_convert_history = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.tokenized_convert_history()

    @patch("binance_common.utils.get_signature")
    def test_tokenized_convert_status_success(self, mock_get_signature):
        """Test tokenized_convert_status() successfully with required parameters only."""

        params = {
            "issuer_request_id": "mint-20260505-8f3b9e1a2d3c4b5a",
            "convert_type": TokenizedConvertStatusConvertTypeEnum["MINT"].value,
        }

        expected_response = {
            "underlyingAsset": "AAPL",
            "underlyingAssetAmount": "1.000000000",
            "tokenizedAsset": "AAPLB",
            "tokenizedAssetAmount": "1.000000000",
            "issuerRequestId": "mint-20260505-8f3b9e1a2d3c4b5a",
            "convertType": "MINT",
            "status": "S",
            "createdAt": 1735900000000,
            "updatedAt": 1735900060000,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.tokenized_convert_status(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/tokenized/convert-status" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["issuerRequestId"] == "mint-20260505-8f3b9e1a2d3c4b5a"
        assert (
            normalized["convertType"]
            == TokenizedConvertStatusConvertTypeEnum["MINT"].value
        )

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TokenizedConvertStatusResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof or is_list or hasattr(TokenizedConvertStatusResponse, "from_dict")
        ):
            expected = TokenizedConvertStatusResponse.from_dict(expected_response)
        else:
            expected = TokenizedConvertStatusResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_tokenized_convert_status_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test tokenized_convert_status() successfully with optional parameters."""

        params = {
            "issuer_request_id": "mint-20260505-8f3b9e1a2d3c4b5a",
            "convert_type": TokenizedConvertStatusConvertTypeEnum["MINT"].value,
            "recv_window": 5000,
        }

        expected_response = {
            "underlyingAsset": "AAPL",
            "underlyingAssetAmount": "1.000000000",
            "tokenizedAsset": "AAPLB",
            "tokenizedAssetAmount": "1.000000000",
            "issuerRequestId": "mint-20260505-8f3b9e1a2d3c4b5a",
            "convertType": "MINT",
            "status": "S",
            "createdAt": 1735900000000,
            "updatedAt": 1735900060000,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.tokenized_convert_status(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/tokenized/convert-status" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TokenizedConvertStatusResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof or is_list or hasattr(TokenizedConvertStatusResponse, "from_dict")
        ):
            expected = TokenizedConvertStatusResponse.from_dict(expected_response)
        else:
            expected = TokenizedConvertStatusResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_tokenized_convert_status_missing_required_param_issuer_request_id(self):
        """Test that tokenized_convert_status() raises RequiredError when 'issuer_request_id' is missing."""
        params = {
            "issuer_request_id": "mint-20260505-8f3b9e1a2d3c4b5a",
            "convert_type": TokenizedConvertStatusConvertTypeEnum["MINT"].value,
        }
        params["issuer_request_id"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'issuer_request_id'"
        ):
            self.client.tokenized_convert_status(**params)

    def test_tokenized_convert_status_missing_required_param_convert_type(self):
        """Test that tokenized_convert_status() raises RequiredError when 'convert_type' is missing."""
        params = {
            "issuer_request_id": "mint-20260505-8f3b9e1a2d3c4b5a",
            "convert_type": TokenizedConvertStatusConvertTypeEnum["MINT"].value,
        }
        params["convert_type"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'convert_type'"
        ):
            self.client.tokenized_convert_status(**params)

    def test_tokenized_convert_status_server_error(self):
        """Test that tokenized_convert_status() raises an error when the server returns an error."""

        params = {
            "issuer_request_id": "mint-20260505-8f3b9e1a2d3c4b5a",
            "convert_type": TokenizedConvertStatusConvertTypeEnum["MINT"].value,
        }

        mock_error = Exception("ResponseError")
        self.client.tokenized_convert_status = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.tokenized_convert_status(**params)

    @patch("binance_common.utils.get_signature")
    def test_tokenized_mint_success(self, mock_get_signature):
        """Test tokenized_mint() successfully with required parameters only."""

        params = {
            "underlying_asset": "AAPL",
            "underlying_asset_amount": "1",
        }

        expected_response = {
            "issuerRequestId": "mint-20260505-8f3b9e1a2d3c4b5a",
            "status": "P",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.tokenized_mint(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/tokenized/mint" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"
        assert normalized["underlyingAsset"] == "AAPL"
        assert normalized["underlyingAssetAmount"] == "1"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TokenizedMintResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(TokenizedMintResponse, "from_dict"):
            expected = TokenizedMintResponse.from_dict(expected_response)
        else:
            expected = TokenizedMintResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_tokenized_mint_success_with_optional_params(self, mock_get_signature):
        """Test tokenized_mint() successfully with optional parameters."""

        params = {
            "underlying_asset": "AAPL",
            "underlying_asset_amount": "1",
            "client_order_id": "mint-client-id-32chars-0000000001",
            "recv_window": 5000,
        }

        expected_response = {
            "issuerRequestId": "mint-20260505-8f3b9e1a2d3c4b5a",
            "status": "P",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.tokenized_mint(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/tokenized/mint" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TokenizedMintResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(TokenizedMintResponse, "from_dict"):
            expected = TokenizedMintResponse.from_dict(expected_response)
        else:
            expected = TokenizedMintResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_tokenized_mint_missing_required_param_underlying_asset(self):
        """Test that tokenized_mint() raises RequiredError when 'underlying_asset' is missing."""
        params = {
            "underlying_asset": "AAPL",
            "underlying_asset_amount": "1",
        }
        params["underlying_asset"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'underlying_asset'"
        ):
            self.client.tokenized_mint(**params)

    def test_tokenized_mint_missing_required_param_underlying_asset_amount(self):
        """Test that tokenized_mint() raises RequiredError when 'underlying_asset_amount' is missing."""
        params = {
            "underlying_asset": "AAPL",
            "underlying_asset_amount": "1",
        }
        params["underlying_asset_amount"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'underlying_asset_amount'"
        ):
            self.client.tokenized_mint(**params)

    def test_tokenized_mint_server_error(self):
        """Test that tokenized_mint() raises an error when the server returns an error."""

        params = {
            "underlying_asset": "AAPL",
            "underlying_asset_amount": "1",
        }

        mock_error = Exception("ResponseError")
        self.client.tokenized_mint = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.tokenized_mint(**params)

    @patch("binance_common.utils.get_signature")
    def test_tokenized_redeem_success(self, mock_get_signature):
        """Test tokenized_redeem() successfully with required parameters only."""

        params = {
            "tokenized_asset": "AAPLB",
            "tokenized_asset_amount": "1",
        }

        expected_response = {
            "issuerRequestId": "redeem-20260505-a1b2c3d4e5f6",
            "status": "P",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.tokenized_redeem(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/tokenized/redeem" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"
        assert normalized["tokenizedAsset"] == "AAPLB"
        assert normalized["tokenizedAssetAmount"] == "1"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TokenizedRedeemResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(TokenizedRedeemResponse, "from_dict"):
            expected = TokenizedRedeemResponse.from_dict(expected_response)
        else:
            expected = TokenizedRedeemResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_tokenized_redeem_success_with_optional_params(self, mock_get_signature):
        """Test tokenized_redeem() successfully with optional parameters."""

        params = {
            "tokenized_asset": "AAPLB",
            "tokenized_asset_amount": "1",
            "client_order_id": "redeem-client-id-32chars-000000001",
            "recv_window": 5000,
        }

        expected_response = {
            "issuerRequestId": "redeem-20260505-a1b2c3d4e5f6",
            "status": "P",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.tokenized_redeem(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/tokenized/redeem" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TokenizedRedeemResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(TokenizedRedeemResponse, "from_dict"):
            expected = TokenizedRedeemResponse.from_dict(expected_response)
        else:
            expected = TokenizedRedeemResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_tokenized_redeem_missing_required_param_tokenized_asset(self):
        """Test that tokenized_redeem() raises RequiredError when 'tokenized_asset' is missing."""
        params = {
            "tokenized_asset": "AAPLB",
            "tokenized_asset_amount": "1",
        }
        params["tokenized_asset"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'tokenized_asset'"
        ):
            self.client.tokenized_redeem(**params)

    def test_tokenized_redeem_missing_required_param_tokenized_asset_amount(self):
        """Test that tokenized_redeem() raises RequiredError when 'tokenized_asset_amount' is missing."""
        params = {
            "tokenized_asset": "AAPLB",
            "tokenized_asset_amount": "1",
        }
        params["tokenized_asset_amount"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'tokenized_asset_amount'"
        ):
            self.client.tokenized_redeem(**params)

    def test_tokenized_redeem_server_error(self):
        """Test that tokenized_redeem() raises an error when the server returns an error."""

        params = {
            "tokenized_asset": "AAPLB",
            "tokenized_asset_amount": "1",
        }

        mock_error = Exception("ResponseError")
        self.client.tokenized_redeem = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.tokenized_redeem(**params)
