"""
Stocks Trading REST API

REST APIs for Binance Stocks Trading. All endpoints under `/sapi/v1/equity/*`.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock

from binance_common.configuration import ConfigurationRestAPI
from binance_common.utils import is_one_of_model

from binance_sdk_stocks.rest_api.api import UserDataStreamsApi
from binance_sdk_stocks.rest_api.models import CreateRenewListenKeyResponse


class TestUserDataStreamsApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = UserDataStreamsApi(
            configuration=config, session=self.mock_session
        )

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    def test_create_renew_listen_key_success(self):
        """Test create_renew_listen_key() successfully with required parameters only."""

        expected_response = {
            "listenKey": "pqia91ma19a5s61cv6a81va65sdf19v8a65s1cv1zuz3ee1c5xz2ef6ad7"
        }

        self.set_mock_response(expected_response)

        response = self.client.create_renew_listen_key()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert "/sapi/v1/equity/listenKey" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(CreateRenewListenKeyResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(CreateRenewListenKeyResponse, "from_dict"):
            expected = CreateRenewListenKeyResponse.from_dict(expected_response)
        else:
            expected = CreateRenewListenKeyResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_create_renew_listen_key_success_with_optional_params(self):
        """Test create_renew_listen_key() successfully with optional parameters."""

        params = {"recv_window": 5000}

        expected_response = {
            "listenKey": "pqia91ma19a5s61cv6a81va65sdf19v8a65s1cv1zuz3ee1c5xz2ef6ad7"
        }

        self.set_mock_response(expected_response)

        response = self.client.create_renew_listen_key(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/sapi/v1/equity/listenKey" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(CreateRenewListenKeyResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(CreateRenewListenKeyResponse, "from_dict"):
            expected = CreateRenewListenKeyResponse.from_dict(expected_response)
        else:
            expected = CreateRenewListenKeyResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_create_renew_listen_key_server_error(self):
        """Test that create_renew_listen_key() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.create_renew_listen_key = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.create_renew_listen_key()
