"""
Stocks Trading REST API

REST APIs for Binance Stocks Trading. All endpoints under `/sapi/v1/equity/*`.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_stocks.rest_api.api import MarketDataApi
from binance_sdk_stocks.rest_api.models import ExchangeInfoResponse
from binance_sdk_stocks.rest_api.models import LatestQuoteResponse
from binance_sdk_stocks.rest_api.models import TokenizedAssetsResponse


class TestMarketDataApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = MarketDataApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    def test_exchange_info_success(self):
        """Test exchange_info() successfully with required parameters only."""

        expected_response = {
            "timezone": "UTC",
            "symbols": [
                {
                    "symbol": "AAPL",
                    "tradability": "BUY_SELL",
                    "tradabilityUpdateTime": 1735900000000,
                    "overnightSupported": True,
                    "fractionable": True,
                    "fractionableEh": False,
                    "extendedSession": True,
                    "maxNumOrders": 200,
                    "stepSize": "0.000000001",
                    "multiplierUp": "1.10",
                    "multiplierDown": "0.90",
                    "minQty": "0.000000001",
                    "maxQty": "100000.00000000",
                    "minNotional": "1.00",
                    "maxNotional": "1000000.00",
                    "listingTime": 1700000000000,
                    "delistingTime": 1,
                }
            ],
        }

        self.set_mock_response(expected_response)

        response = self.client.exchange_info()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert "/sapi/v1/equity/market/exchangeInfo" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(ExchangeInfoResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(ExchangeInfoResponse, "from_dict"):
            expected = ExchangeInfoResponse.from_dict(expected_response)
        else:
            expected = ExchangeInfoResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_exchange_info_success_with_optional_params(self):
        """Test exchange_info() successfully with optional parameters."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "timezone": "UTC",
            "symbols": [
                {
                    "symbol": "AAPL",
                    "tradability": "BUY_SELL",
                    "tradabilityUpdateTime": 1735900000000,
                    "overnightSupported": True,
                    "fractionable": True,
                    "fractionableEh": False,
                    "extendedSession": True,
                    "maxNumOrders": 200,
                    "stepSize": "0.000000001",
                    "multiplierUp": "1.10",
                    "multiplierDown": "0.90",
                    "minQty": "0.000000001",
                    "maxQty": "100000.00000000",
                    "minNotional": "1.00",
                    "maxNotional": "1000000.00",
                    "listingTime": 1700000000000,
                    "delistingTime": 1,
                }
            ],
        }

        self.set_mock_response(expected_response)

        response = self.client.exchange_info(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/sapi/v1/equity/market/exchangeInfo" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(ExchangeInfoResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(ExchangeInfoResponse, "from_dict"):
            expected = ExchangeInfoResponse.from_dict(expected_response)
        else:
            expected = ExchangeInfoResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_exchange_info_server_error(self):
        """Test that exchange_info() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.exchange_info = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.exchange_info()

    def test_latest_quote_success(self):
        """Test latest_quote() successfully with required parameters only."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "symbol": "AAPL",
            "bidPrice": "180.50",
            "askPrice": "180.52",
            "bidSize": 100,
            "askSize": 200,
        }

        self.set_mock_response(expected_response)

        response = self.client.latest_quote(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert "/sapi/v1/equity/market/quote" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["symbol"] == "AAPL"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(LatestQuoteResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(LatestQuoteResponse, "from_dict"):
            expected = LatestQuoteResponse.from_dict(expected_response)
        else:
            expected = LatestQuoteResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_latest_quote_success_with_optional_params(self):
        """Test latest_quote() successfully with optional parameters."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "symbol": "AAPL",
            "bidPrice": "180.50",
            "askPrice": "180.52",
            "bidSize": 100,
            "askSize": 200,
        }

        self.set_mock_response(expected_response)

        response = self.client.latest_quote(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/sapi/v1/equity/market/quote" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(LatestQuoteResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(LatestQuoteResponse, "from_dict"):
            expected = LatestQuoteResponse.from_dict(expected_response)
        else:
            expected = LatestQuoteResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_latest_quote_missing_required_param_symbol(self):
        """Test that latest_quote() raises RequiredError when 'symbol' is missing."""
        params = {"symbol": "AAPL"}
        params["symbol"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'symbol'"):
            self.client.latest_quote(**params)

    def test_latest_quote_server_error(self):
        """Test that latest_quote() raises an error when the server returns an error."""

        params = {"symbol": "AAPL"}

        mock_error = Exception("ResponseError")
        self.client.latest_quote = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.latest_quote(**params)

    def test_tokenized_assets_success(self):
        """Test tokenized_assets() successfully with required parameters only."""

        expected_response = [
            {
                "assetCode": "AAPLB",
                "assetName": "Apple Inc. Tokenized Stock",
                "underlyingEquitySymbol": "AAPL",
                "multiplier": "1",
                "multiplierValid": True,
            }
        ]

        self.set_mock_response(expected_response)

        response = self.client.tokenized_assets()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert "/sapi/v1/equity/market/tokenized-assets" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TokenizedAssetsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(TokenizedAssetsResponse, "from_dict"):
            expected = TokenizedAssetsResponse.from_dict(expected_response)
        else:
            expected = TokenizedAssetsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_tokenized_assets_server_error(self):
        """Test that tokenized_assets() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.tokenized_assets = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.tokenized_assets()
