"""
Stocks Trading REST API

REST APIs for Binance Stocks Trading. All endpoints under `/sapi/v1/equity/*`.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_stocks.rest_api.api import TradeApi
from binance_sdk_stocks.rest_api.models import CancelAllEquityOrdersResponse
from binance_sdk_stocks.rest_api.models import CancelEquityOrderResponse
from binance_sdk_stocks.rest_api.models import CurrentOpenOrdersResponse
from binance_sdk_stocks.rest_api.models import EquityOrderDetailResponse
from binance_sdk_stocks.rest_api.models import EquityOrderHistoryResponse
from binance_sdk_stocks.rest_api.models import EquityTradeHistoryResponse
from binance_sdk_stocks.rest_api.models import PlaceEquityOrderResponse
from binance_sdk_stocks.rest_api.models import EquityOrderHistoryOrderTypeEnum
from binance_sdk_stocks.rest_api.models import EquityOrderHistorySideEnum
from binance_sdk_stocks.rest_api.models import EquityTradeHistorySideEnum
from binance_sdk_stocks.rest_api.models import PlaceEquityOrderSideEnum
from binance_sdk_stocks.rest_api.models import PlaceEquityOrderOrderTypeEnum
from binance_sdk_stocks.rest_api.models import PlaceEquityOrderTimeInForceEnum
from binance_sdk_stocks.rest_api.models import PlaceEquityOrderTradingSessionEnum
from binance_sdk_stocks.rest_api.models import PlaceEquityOrderWalletTypeEnum


class TestTradeApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = TradeApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    @patch("binance_common.utils.get_signature")
    def test_cancel_all_equity_orders_success(self, mock_get_signature):
        """Test cancel_all_equity_orders() successfully with required parameters only."""

        expected_response = {"success": True}
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.cancel_all_equity_orders()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/cancel-all" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(CancelAllEquityOrdersResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(CancelAllEquityOrdersResponse, "from_dict"):
            expected = CancelAllEquityOrdersResponse.from_dict(expected_response)
        else:
            expected = CancelAllEquityOrdersResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_cancel_all_equity_orders_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test cancel_all_equity_orders() successfully with optional parameters."""

        params = {"recv_window": 5000}

        expected_response = {"success": True}
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.cancel_all_equity_orders(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/cancel-all" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(CancelAllEquityOrdersResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(CancelAllEquityOrdersResponse, "from_dict"):
            expected = CancelAllEquityOrdersResponse.from_dict(expected_response)
        else:
            expected = CancelAllEquityOrdersResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_cancel_all_equity_orders_server_error(self):
        """Test that cancel_all_equity_orders() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.cancel_all_equity_orders = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.cancel_all_equity_orders()

    @patch("binance_common.utils.get_signature")
    def test_cancel_equity_order_success(self, mock_get_signature):
        """Test cancel_equity_order() successfully with required parameters only."""

        params = {
            "order_id": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
        }

        expected_response = {
            "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
            "status": "S",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.cancel_equity_order(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/cancel" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"
        assert normalized["orderId"] == "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(CancelEquityOrderResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(CancelEquityOrderResponse, "from_dict"):
            expected = CancelEquityOrderResponse.from_dict(expected_response)
        else:
            expected = CancelEquityOrderResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_cancel_equity_order_success_with_optional_params(self, mock_get_signature):
        """Test cancel_equity_order() successfully with optional parameters."""

        params = {
            "order_id": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
            "recv_window": 5000,
        }

        expected_response = {
            "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
            "status": "S",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.cancel_equity_order(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/cancel" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(CancelEquityOrderResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(CancelEquityOrderResponse, "from_dict"):
            expected = CancelEquityOrderResponse.from_dict(expected_response)
        else:
            expected = CancelEquityOrderResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_cancel_equity_order_missing_required_param_order_id(self):
        """Test that cancel_equity_order() raises RequiredError when 'order_id' is missing."""
        params = {
            "order_id": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
        }
        params["order_id"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'order_id'"
        ):
            self.client.cancel_equity_order(**params)

    def test_cancel_equity_order_server_error(self):
        """Test that cancel_equity_order() raises an error when the server returns an error."""

        params = {
            "order_id": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
        }

        mock_error = Exception("ResponseError")
        self.client.cancel_equity_order = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.cancel_equity_order(**params)

    @patch("binance_common.utils.get_signature")
    def test_current_open_orders_success(self, mock_get_signature):
        """Test current_open_orders() successfully with required parameters only."""

        expected_response = [
            {
                "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
                "symbol": "AAPL",
                "quote": "USDC",
                "side": "BUY",
                "orderType": "LIMIT",
                "limitPrice": "180.50",
                "avgFilledPrice": "avgFilledPrice",
                "qty": "1",
                "notional": "notional",
                "filledQty": "0",
                "filledTotal": "filledTotal",
                "fee": "0",
                "session": "RTH",
                "status": "NEW",
                "createdAt": 1735900000000,
                "updatedAt": 1735900000000,
            }
        ]
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.current_open_orders()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/open-orders" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(CurrentOpenOrdersResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(CurrentOpenOrdersResponse, "from_dict"):
            expected = CurrentOpenOrdersResponse.from_dict(expected_response)
        else:
            expected = CurrentOpenOrdersResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_current_open_orders_success_with_optional_params(self, mock_get_signature):
        """Test current_open_orders() successfully with optional parameters."""

        params = {"recv_window": 5000}

        expected_response = [
            {
                "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
                "symbol": "AAPL",
                "quote": "USDC",
                "side": "BUY",
                "orderType": "LIMIT",
                "limitPrice": "180.50",
                "avgFilledPrice": "avgFilledPrice",
                "qty": "1",
                "notional": "notional",
                "filledQty": "0",
                "filledTotal": "filledTotal",
                "fee": "0",
                "session": "RTH",
                "status": "NEW",
                "createdAt": 1735900000000,
                "updatedAt": 1735900000000,
            }
        ]
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.current_open_orders(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/open-orders" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(CurrentOpenOrdersResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(CurrentOpenOrdersResponse, "from_dict"):
            expected = CurrentOpenOrdersResponse.from_dict(expected_response)
        else:
            expected = CurrentOpenOrdersResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_current_open_orders_server_error(self):
        """Test that current_open_orders() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.current_open_orders = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.current_open_orders()

    @patch("binance_common.utils.get_signature")
    def test_equity_order_detail_success(self, mock_get_signature):
        """Test equity_order_detail() successfully with required parameters only."""

        expected_response = {
            "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
            "clientOrderId": "web_2c9c92b74f1e4a7c8f3b9e1a2d3c4b5a",
            "symbol": "AAPL",
            "quote": "USDC",
            "side": "BUY",
            "orderType": "LIMIT",
            "limitPrice": "180.50",
            "avgFilledPrice": "180.48",
            "qty": "1",
            "notional": "notional",
            "filledQty": "1",
            "filledTotal": "filledTotal",
            "fee": "0.10",
            "session": "RTH",
            "status": "FILLED",
            "createdAt": 1735900000000,
            "updatedAt": 1735900120000,
            "trades": [
                {
                    "executionId": "exec-20260504-0001",
                    "executionAt": 1735900115000,
                    "price": "180.48",
                    "qty": "1",
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.equity_order_detail()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/detail" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(EquityOrderDetailResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(EquityOrderDetailResponse, "from_dict"):
            expected = EquityOrderDetailResponse.from_dict(expected_response)
        else:
            expected = EquityOrderDetailResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_equity_order_detail_success_with_optional_params(self, mock_get_signature):
        """Test equity_order_detail() successfully with optional parameters."""

        params = {
            "order_id": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
            "client_order_id": "web_2c9c92b74f1e4a7c8f3b9e1a2d3c4b5a",
            "recv_window": 5000,
        }

        expected_response = {
            "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
            "clientOrderId": "web_2c9c92b74f1e4a7c8f3b9e1a2d3c4b5a",
            "symbol": "AAPL",
            "quote": "USDC",
            "side": "BUY",
            "orderType": "LIMIT",
            "limitPrice": "180.50",
            "avgFilledPrice": "180.48",
            "qty": "1",
            "notional": "notional",
            "filledQty": "1",
            "filledTotal": "filledTotal",
            "fee": "0.10",
            "session": "RTH",
            "status": "FILLED",
            "createdAt": 1735900000000,
            "updatedAt": 1735900120000,
            "trades": [
                {
                    "executionId": "exec-20260504-0001",
                    "executionAt": 1735900115000,
                    "price": "180.48",
                    "qty": "1",
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.equity_order_detail(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/detail" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(EquityOrderDetailResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(EquityOrderDetailResponse, "from_dict"):
            expected = EquityOrderDetailResponse.from_dict(expected_response)
        else:
            expected = EquityOrderDetailResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_equity_order_detail_server_error(self):
        """Test that equity_order_detail() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.equity_order_detail = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.equity_order_detail()

    @patch("binance_common.utils.get_signature")
    def test_equity_order_history_success(self, mock_get_signature):
        """Test equity_order_history() successfully with required parameters only."""

        params = {
            "start_time": 1735800000000,
            "end_time": 1735900000000,
        }

        expected_response = {
            "total": 2,
            "page": 1,
            "size": 20,
            "rows": [
                {
                    "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
                    "symbol": "AAPL",
                    "quote": "USDC",
                    "side": "BUY",
                    "orderType": "LIMIT",
                    "limitPrice": "180.50",
                    "avgFilledPrice": "180.48",
                    "qty": "1",
                    "notional": "notional",
                    "filledQty": "1",
                    "filledTotal": "filledTotal",
                    "fee": "0.10",
                    "session": "RTH",
                    "status": "FILLED",
                    "createdAt": 1735900000000,
                    "updatedAt": 1735900120000,
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.equity_order_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/history" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["startTime"] == 1735800000000
        assert normalized["endTime"] == 1735900000000

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(EquityOrderHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(EquityOrderHistoryResponse, "from_dict"):
            expected = EquityOrderHistoryResponse.from_dict(expected_response)
        else:
            expected = EquityOrderHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_equity_order_history_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test equity_order_history() successfully with optional parameters."""

        params = {
            "start_time": 1735800000000,
            "end_time": 1735900000000,
            "symbol": "NVDA",
            "order_type": EquityOrderHistoryOrderTypeEnum["MARKET"].value,
            "side": EquityOrderHistorySideEnum["BUY"].value,
            "order_status": "FILLED,CANCELED",
            "current": 1,
            "size": 20,
            "recv_window": 5000,
        }

        expected_response = {
            "total": 2,
            "page": 1,
            "size": 20,
            "rows": [
                {
                    "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
                    "symbol": "AAPL",
                    "quote": "USDC",
                    "side": "BUY",
                    "orderType": "LIMIT",
                    "limitPrice": "180.50",
                    "avgFilledPrice": "180.48",
                    "qty": "1",
                    "notional": "notional",
                    "filledQty": "1",
                    "filledTotal": "filledTotal",
                    "fee": "0.10",
                    "session": "RTH",
                    "status": "FILLED",
                    "createdAt": 1735900000000,
                    "updatedAt": 1735900120000,
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.equity_order_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/history" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(EquityOrderHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(EquityOrderHistoryResponse, "from_dict"):
            expected = EquityOrderHistoryResponse.from_dict(expected_response)
        else:
            expected = EquityOrderHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_equity_order_history_missing_required_param_start_time(self):
        """Test that equity_order_history() raises RequiredError when 'start_time' is missing."""
        params = {
            "start_time": 1735800000000,
            "end_time": 1735900000000,
        }
        params["start_time"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'start_time'"
        ):
            self.client.equity_order_history(**params)

    def test_equity_order_history_missing_required_param_end_time(self):
        """Test that equity_order_history() raises RequiredError when 'end_time' is missing."""
        params = {
            "start_time": 1735800000000,
            "end_time": 1735900000000,
        }
        params["end_time"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'end_time'"
        ):
            self.client.equity_order_history(**params)

    def test_equity_order_history_server_error(self):
        """Test that equity_order_history() raises an error when the server returns an error."""

        params = {
            "start_time": 1735800000000,
            "end_time": 1735900000000,
        }

        mock_error = Exception("ResponseError")
        self.client.equity_order_history = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.equity_order_history(**params)

    @patch("binance_common.utils.get_signature")
    def test_equity_trade_history_success(self, mock_get_signature):
        """Test equity_trade_history() successfully with required parameters only."""

        params = {
            "start_time": 1735800000000,
            "end_time": 1735900000000,
        }

        expected_response = {
            "total": 1,
            "page": 1,
            "size": 20,
            "rows": [
                {
                    "executionId": "exec-20260504-0001",
                    "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
                    "symbol": "AAPL",
                    "quote": "USDC",
                    "side": "BUY",
                    "orderType": "LIMIT",
                    "price": "180.50",
                    "qty": "1",
                    "total": "180.50",
                    "executionAt": 1735900115000,
                    "updatedAt": 1735900115200,
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.equity_trade_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/trade/history" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["startTime"] == 1735800000000
        assert normalized["endTime"] == 1735900000000

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(EquityTradeHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(EquityTradeHistoryResponse, "from_dict"):
            expected = EquityTradeHistoryResponse.from_dict(expected_response)
        else:
            expected = EquityTradeHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_equity_trade_history_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test equity_trade_history() successfully with optional parameters."""

        params = {
            "start_time": 1735800000000,
            "end_time": 1735900000000,
            "symbol": "NVDA",
            "side": EquityTradeHistorySideEnum["BUY"].value,
            "order_id": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
            "current": 1,
            "size": 20,
            "recv_window": 5000,
        }

        expected_response = {
            "total": 1,
            "page": 1,
            "size": 20,
            "rows": [
                {
                    "executionId": "exec-20260504-0001",
                    "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
                    "symbol": "AAPL",
                    "quote": "USDC",
                    "side": "BUY",
                    "orderType": "LIMIT",
                    "price": "180.50",
                    "qty": "1",
                    "total": "180.50",
                    "executionAt": 1735900115000,
                    "updatedAt": 1735900115200,
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.equity_trade_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/trade/history" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(EquityTradeHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(EquityTradeHistoryResponse, "from_dict"):
            expected = EquityTradeHistoryResponse.from_dict(expected_response)
        else:
            expected = EquityTradeHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_equity_trade_history_missing_required_param_start_time(self):
        """Test that equity_trade_history() raises RequiredError when 'start_time' is missing."""
        params = {
            "start_time": 1735800000000,
            "end_time": 1735900000000,
        }
        params["start_time"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'start_time'"
        ):
            self.client.equity_trade_history(**params)

    def test_equity_trade_history_missing_required_param_end_time(self):
        """Test that equity_trade_history() raises RequiredError when 'end_time' is missing."""
        params = {
            "start_time": 1735800000000,
            "end_time": 1735900000000,
        }
        params["end_time"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'end_time'"
        ):
            self.client.equity_trade_history(**params)

    def test_equity_trade_history_server_error(self):
        """Test that equity_trade_history() raises an error when the server returns an error."""

        params = {
            "start_time": 1735800000000,
            "end_time": 1735900000000,
        }

        mock_error = Exception("ResponseError")
        self.client.equity_trade_history = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.equity_trade_history(**params)

    @patch("binance_common.utils.get_signature")
    def test_place_equity_order_success(self, mock_get_signature):
        """Test place_equity_order() successfully with required parameters only."""

        params = {
            "symbol": "AAPL",
            "side": PlaceEquityOrderSideEnum["BUY"].value,
            "order_type": PlaceEquityOrderOrderTypeEnum["MARKET"].value,
        }

        expected_response = {
            "status": "S",
            "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
            "clientOrderId": "web_2c9c92b74f1e4a7c8f3b9e1a2d3c4b5a",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.place_equity_order(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/place" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"
        assert normalized["symbol"] == "AAPL"
        assert normalized["side"] == PlaceEquityOrderSideEnum["BUY"].value
        assert normalized["orderType"] == PlaceEquityOrderOrderTypeEnum["MARKET"].value

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(PlaceEquityOrderResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(PlaceEquityOrderResponse, "from_dict"):
            expected = PlaceEquityOrderResponse.from_dict(expected_response)
        else:
            expected = PlaceEquityOrderResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_place_equity_order_success_with_optional_params(self, mock_get_signature):
        """Test place_equity_order() successfully with optional parameters."""

        params = {
            "symbol": "AAPL",
            "side": PlaceEquityOrderSideEnum["BUY"].value,
            "order_type": PlaceEquityOrderOrderTypeEnum["MARKET"].value,
            "quote_asset": "USDC",
            "price": "180.50",
            "quantity": "1",
            "notional": "1000.00",
            "time_in_force": PlaceEquityOrderTimeInForceEnum["DAY"].value,
            "trading_session": PlaceEquityOrderTradingSessionEnum["RTH"].value,
            "wallet_type": PlaceEquityOrderWalletTypeEnum["CARD"].value,
            "client_order_id": "web_2c9c92b74f1e4a7c8f3b9e1a2d3c4b5a",
            "tokenize": True,
            "recv_window": 5000,
        }

        expected_response = {
            "status": "S",
            "orderId": "c3c58f49-7b0d-4b9e-a2db-1a2f9a3b8c71",
            "clientOrderId": "web_2c9c92b74f1e4a7c8f3b9e1a2d3c4b5a",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.place_equity_order(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/equity/order/place" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(PlaceEquityOrderResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(PlaceEquityOrderResponse, "from_dict"):
            expected = PlaceEquityOrderResponse.from_dict(expected_response)
        else:
            expected = PlaceEquityOrderResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_place_equity_order_missing_required_param_symbol(self):
        """Test that place_equity_order() raises RequiredError when 'symbol' is missing."""
        params = {
            "symbol": "AAPL",
            "side": PlaceEquityOrderSideEnum["BUY"].value,
            "order_type": PlaceEquityOrderOrderTypeEnum["MARKET"].value,
        }
        params["symbol"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'symbol'"):
            self.client.place_equity_order(**params)

    def test_place_equity_order_missing_required_param_side(self):
        """Test that place_equity_order() raises RequiredError when 'side' is missing."""
        params = {
            "symbol": "AAPL",
            "side": PlaceEquityOrderSideEnum["BUY"].value,
            "order_type": PlaceEquityOrderOrderTypeEnum["MARKET"].value,
        }
        params["side"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'side'"):
            self.client.place_equity_order(**params)

    def test_place_equity_order_missing_required_param_order_type(self):
        """Test that place_equity_order() raises RequiredError when 'order_type' is missing."""
        params = {
            "symbol": "AAPL",
            "side": PlaceEquityOrderSideEnum["BUY"].value,
            "order_type": PlaceEquityOrderOrderTypeEnum["MARKET"].value,
        }
        params["order_type"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'order_type'"
        ):
            self.client.place_equity_order(**params)

    def test_place_equity_order_server_error(self):
        """Test that place_equity_order() raises an error when the server returns an error."""

        params = {
            "symbol": "AAPL",
            "side": PlaceEquityOrderSideEnum["BUY"].value,
            "order_type": PlaceEquityOrderOrderTypeEnum["MARKET"].value,
        }

        mock_error = Exception("ResponseError")
        self.client.place_equity_order = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.place_equity_order(**params)
