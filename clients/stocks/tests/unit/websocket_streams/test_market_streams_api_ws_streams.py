"""
Stocks Trading WebSocket Streams

WebSocket stream definitions for Binance Stocks Trading. Base URL: wss://nbstream.binance.com/equity
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import pytest

from unittest.mock import AsyncMock, MagicMock

from binance_common.configuration import ConfigurationWebSocketStreams
from binance_common.errors import RequiredError
from binance_common.utils import ws_streams_placeholder
from binance_common.websocket import WebSocketStreamBase, RequestStreamHandle
from binance_sdk_stocks.websocket_streams.streams import MarketStreamsApi


@pytest.fixture
def mock_websocket_client():
    """Fixture to provide a reusable mocked WebSocketStreamBase instance."""
    config = ConfigurationWebSocketStreams(stream_url="wss://mock-url")
    websocket_stream_client = WebSocketStreamBase(config)
    websocket_stream_client.subscribe = AsyncMock()
    websocket_stream_client.unsubscribe = AsyncMock()
    return websocket_stream_client


class TestWebSocketStreams:
    @pytest.fixture(autouse=True)
    def setup(self, mock_websocket_client):
        self.websocket_client = mock_websocket_client
        self.ws_streams = MarketStreamsApi(websocket_base=self.websocket_client)

    @pytest.mark.asyncio
    async def test_calendar_stream_subscription(self):
        """Test that calendar_stream() subscribes to the correct WebSocket stream."""

        expected_response = {
            "e": "calendar",
            "from": "PRE_MARKET",
            "to": "MARKET_OPEN",
            "ts": 1710334200000,
        }
        stream_endpoint = ws_streams_placeholder(
            "/calendar".replace("/", "", 1),
            {},
        )

        def mock_on(event, callback, stream):
            if event == "message" and stream == stream_endpoint:
                callback(expected_response)

        self.websocket_client.on = MagicMock(side_effect=mock_on)

        mock_callback = MagicMock()

        stream = await self.ws_streams.calendar_stream()
        assert isinstance(stream, RequestStreamHandle)
        assert callable(stream.on)
        assert callable(stream.unsubscribe)
        stream.on("message", mock_callback)
        mock_callback.assert_called_once_with(expected_response)

    @pytest.mark.asyncio
    async def test_calendar_stream_success(self):
        """Test calendar_stream() successfully with required parameters only."""

        expected_response = {
            "e": "calendar",
            "from": "PRE_MARKET",
            "to": "MARKET_OPEN",
            "ts": 1710334200000,
        }
        self.ws_streams.calendar_stream = AsyncMock(return_value=expected_response)

        response = await self.ws_streams.calendar_stream()
        assert response is not None
        assert response == expected_response

    @pytest.mark.asyncio
    async def test_calendar_stream_server_error(self):
        """Test that calendar_stream() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.ws_streams.calendar_stream = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            await self.ws_streams.calendar_stream()

    @pytest.mark.asyncio
    async def test_kline_stream_subscription(self):
        """Test that kline_stream() subscribes to the correct WebSocket stream."""

        params = {"symbol": "AAPL", "interval": "5m"}

        expected_response = {
            "e": "kline",
            "E": 1710320400000,
            "s": "AAPL",
            "k": {
                "t": 1710316800000,
                "ct": 1710317099999,
                "s": "AAPL",
                "i": "5m",
                "o": "182.50",
                "c": "183.20",
                "h": "183.50",
                "l": "182.30",
                "v": "15000",
                "n": 120,
                "x": False,
                "vw": "182.95",
            },
        }
        stream_endpoint = ws_streams_placeholder(
            "/<symbol>@kline_<interval>".replace("/", "", 1),
            params,
        )

        def mock_on(event, callback, stream):
            if event == "message" and stream == stream_endpoint:
                callback(expected_response)

        self.websocket_client.on = MagicMock(side_effect=mock_on)

        mock_callback = MagicMock()

        stream = await self.ws_streams.kline_stream(**params)
        assert isinstance(stream, RequestStreamHandle)
        assert callable(stream.on)
        assert callable(stream.unsubscribe)
        stream.on("message", mock_callback)
        mock_callback.assert_called_once_with(expected_response)

    @pytest.mark.asyncio
    async def test_kline_stream_success(self):
        """Test kline_stream() successfully with required parameters only."""

        params = {"symbol": "AAPL", "interval": "5m"}

        expected_response = {
            "e": "kline",
            "E": 1710320400000,
            "s": "AAPL",
            "k": {
                "t": 1710316800000,
                "ct": 1710317099999,
                "s": "AAPL",
                "i": "5m",
                "o": "182.50",
                "c": "183.20",
                "h": "183.50",
                "l": "182.30",
                "v": "15000",
                "n": 120,
                "x": False,
                "vw": "182.95",
            },
        }
        self.ws_streams.kline_stream = AsyncMock(return_value=expected_response)

        response = await self.ws_streams.kline_stream(**params)
        assert response is not None
        assert response == expected_response

    @pytest.mark.asyncio
    async def test_kline_stream_success_with_optional_params(self):
        """Test kline_stream() successfully with optional parameters."""

        params = {"symbol": "AAPL", "interval": "5m"}

        expected_response = {
            "e": "kline",
            "E": 1710320400000,
            "s": "AAPL",
            "k": {
                "t": 1710316800000,
                "ct": 1710317099999,
                "s": "AAPL",
                "i": "5m",
                "o": "182.50",
                "c": "183.20",
                "h": "183.50",
                "l": "182.30",
                "v": "15000",
                "n": 120,
                "x": False,
                "vw": "182.95",
            },
        }

        self.ws_streams.kline_stream = AsyncMock(return_value=expected_response)

        response = await self.ws_streams.kline_stream(**params)
        assert response is not None
        assert response == expected_response

    @pytest.mark.asyncio
    async def test_kline_stream_missing_required_param_symbol(self):
        """Test that kline_stream() raises RequiredError when 'symbol' is missing."""
        params = {"symbol": "AAPL", "interval": "5m"}
        params["symbol"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'symbol'"):
            await self.ws_streams.kline_stream(**params)

    @pytest.mark.asyncio
    async def test_kline_stream_missing_required_param_interval(self):
        """Test that kline_stream() raises RequiredError when 'interval' is missing."""
        params = {"symbol": "AAPL", "interval": "5m"}
        params["interval"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'interval'"
        ):
            await self.ws_streams.kline_stream(**params)

    @pytest.mark.asyncio
    async def test_kline_stream_server_error(self):
        """Test that kline_stream() raises an error when the server returns an error."""

        params = {"symbol": "AAPL", "interval": "5m"}

        mock_error = Exception("ResponseError")
        self.ws_streams.kline_stream = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            await self.ws_streams.kline_stream(**params)

    @pytest.mark.asyncio
    async def test_price_stream_subscription(self):
        """Test that price_stream() subscribes to the correct WebSocket stream."""

        expected_response = {
            "e": "price",
            "rates": [
                {
                    "s": "AAPL",
                    "ac": "EQ_AAPL",
                    "p": "182.5",
                    "t": 1710320400000,
                    "pc": "181.2000",
                    "tc": "182.0000",
                    "mp": "O",
                }
            ],
        }
        stream_endpoint = ws_streams_placeholder(
            "/price".replace("/", "", 1),
            {},
        )

        def mock_on(event, callback, stream):
            if event == "message" and stream == stream_endpoint:
                callback(expected_response)

        self.websocket_client.on = MagicMock(side_effect=mock_on)

        mock_callback = MagicMock()

        stream = await self.ws_streams.price_stream()
        assert isinstance(stream, RequestStreamHandle)
        assert callable(stream.on)
        assert callable(stream.unsubscribe)
        stream.on("message", mock_callback)
        mock_callback.assert_called_once_with(expected_response)

    @pytest.mark.asyncio
    async def test_price_stream_success(self):
        """Test price_stream() successfully with required parameters only."""

        expected_response = {
            "e": "price",
            "rates": [
                {
                    "s": "AAPL",
                    "ac": "EQ_AAPL",
                    "p": "182.5",
                    "t": 1710320400000,
                    "pc": "181.2000",
                    "tc": "182.0000",
                    "mp": "O",
                }
            ],
        }
        self.ws_streams.price_stream = AsyncMock(return_value=expected_response)

        response = await self.ws_streams.price_stream()
        assert response is not None
        assert response == expected_response

    @pytest.mark.asyncio
    async def test_price_stream_server_error(self):
        """Test that price_stream() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.ws_streams.price_stream = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            await self.ws_streams.price_stream()

    @pytest.mark.asyncio
    async def test_quote_stream_subscription(self):
        """Test that quote_stream() subscribes to the correct WebSocket stream."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "e": "quote",
            "E": 1710320400000,
            "s": "AAPL",
            "bp": "182.50",
            "ap": "182.55",
            "bs": 100,
            "as": 200,
            "T": 1710320399500,
        }
        stream_endpoint = ws_streams_placeholder(
            "/<symbol>@quote".replace("/", "", 1),
            params,
        )

        def mock_on(event, callback, stream):
            if event == "message" and stream == stream_endpoint:
                callback(expected_response)

        self.websocket_client.on = MagicMock(side_effect=mock_on)

        mock_callback = MagicMock()

        stream = await self.ws_streams.quote_stream(**params)
        assert isinstance(stream, RequestStreamHandle)
        assert callable(stream.on)
        assert callable(stream.unsubscribe)
        stream.on("message", mock_callback)
        mock_callback.assert_called_once_with(expected_response)

    @pytest.mark.asyncio
    async def test_quote_stream_success(self):
        """Test quote_stream() successfully with required parameters only."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "e": "quote",
            "E": 1710320400000,
            "s": "AAPL",
            "bp": "182.50",
            "ap": "182.55",
            "bs": 100,
            "as": 200,
            "T": 1710320399500,
        }
        self.ws_streams.quote_stream = AsyncMock(return_value=expected_response)

        response = await self.ws_streams.quote_stream(**params)
        assert response is not None
        assert response == expected_response

    @pytest.mark.asyncio
    async def test_quote_stream_success_with_optional_params(self):
        """Test quote_stream() successfully with optional parameters."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "e": "quote",
            "E": 1710320400000,
            "s": "AAPL",
            "bp": "182.50",
            "ap": "182.55",
            "bs": 100,
            "as": 200,
            "T": 1710320399500,
        }

        self.ws_streams.quote_stream = AsyncMock(return_value=expected_response)

        response = await self.ws_streams.quote_stream(**params)
        assert response is not None
        assert response == expected_response

    @pytest.mark.asyncio
    async def test_quote_stream_missing_required_param_symbol(self):
        """Test that quote_stream() raises RequiredError when 'symbol' is missing."""
        params = {"symbol": "AAPL"}
        params["symbol"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'symbol'"):
            await self.ws_streams.quote_stream(**params)

    @pytest.mark.asyncio
    async def test_quote_stream_server_error(self):
        """Test that quote_stream() raises an error when the server returns an error."""

        params = {"symbol": "AAPL"}

        mock_error = Exception("ResponseError")
        self.ws_streams.quote_stream = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            await self.ws_streams.quote_stream(**params)

    @pytest.mark.asyncio
    async def test_tradability_stream_subscription(self):
        """Test that tradability_stream() subscribes to the correct WebSocket stream."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "e": "tradability",
            "symbol": "AAPL",
            "assetCode": "EQ_AAPL",
            "tradability": "BUY_SELL",
            "t": 1712345678000,
        }
        stream_endpoint = ws_streams_placeholder(
            "/<symbol>@tradability".replace("/", "", 1),
            params,
        )

        def mock_on(event, callback, stream):
            if event == "message" and stream == stream_endpoint:
                callback(expected_response)

        self.websocket_client.on = MagicMock(side_effect=mock_on)

        mock_callback = MagicMock()

        stream = await self.ws_streams.tradability_stream(**params)
        assert isinstance(stream, RequestStreamHandle)
        assert callable(stream.on)
        assert callable(stream.unsubscribe)
        stream.on("message", mock_callback)
        mock_callback.assert_called_once_with(expected_response)

    @pytest.mark.asyncio
    async def test_tradability_stream_success(self):
        """Test tradability_stream() successfully with required parameters only."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "e": "tradability",
            "symbol": "AAPL",
            "assetCode": "EQ_AAPL",
            "tradability": "BUY_SELL",
            "t": 1712345678000,
        }
        self.ws_streams.tradability_stream = AsyncMock(return_value=expected_response)

        response = await self.ws_streams.tradability_stream(**params)
        assert response is not None
        assert response == expected_response

    @pytest.mark.asyncio
    async def test_tradability_stream_success_with_optional_params(self):
        """Test tradability_stream() successfully with optional parameters."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "e": "tradability",
            "symbol": "AAPL",
            "assetCode": "EQ_AAPL",
            "tradability": "BUY_SELL",
            "t": 1712345678000,
        }

        self.ws_streams.tradability_stream = AsyncMock(return_value=expected_response)

        response = await self.ws_streams.tradability_stream(**params)
        assert response is not None
        assert response == expected_response

    @pytest.mark.asyncio
    async def test_tradability_stream_missing_required_param_symbol(self):
        """Test that tradability_stream() raises RequiredError when 'symbol' is missing."""
        params = {"symbol": "AAPL"}
        params["symbol"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'symbol'"):
            await self.ws_streams.tradability_stream(**params)

    @pytest.mark.asyncio
    async def test_tradability_stream_server_error(self):
        """Test that tradability_stream() raises an error when the server returns an error."""

        params = {"symbol": "AAPL"}

        mock_error = Exception("ResponseError")
        self.ws_streams.tradability_stream = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            await self.ws_streams.tradability_stream(**params)

    @pytest.mark.asyncio
    async def test_trading_status_stream_subscription(self):
        """Test that trading_status_stream() subscribes to the correct WebSocket stream."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "e": "tradingStatus",
            "symbol": "AAPL",
            "assetCode": "EQ_AAPL",
            "status": "TRADING_HALTED",
            "msg": "MATERIAL_NEWS",
            "time": 1614021300000,
            "z": "C",
            "tradability": "NONE",
        }
        stream_endpoint = ws_streams_placeholder(
            "/<symbol>@tradingStatus".replace("/", "", 1),
            params,
        )

        def mock_on(event, callback, stream):
            if event == "message" and stream == stream_endpoint:
                callback(expected_response)

        self.websocket_client.on = MagicMock(side_effect=mock_on)

        mock_callback = MagicMock()

        stream = await self.ws_streams.trading_status_stream(**params)
        assert isinstance(stream, RequestStreamHandle)
        assert callable(stream.on)
        assert callable(stream.unsubscribe)
        stream.on("message", mock_callback)
        mock_callback.assert_called_once_with(expected_response)

    @pytest.mark.asyncio
    async def test_trading_status_stream_success(self):
        """Test trading_status_stream() successfully with required parameters only."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "e": "tradingStatus",
            "symbol": "AAPL",
            "assetCode": "EQ_AAPL",
            "status": "TRADING_HALTED",
            "msg": "MATERIAL_NEWS",
            "time": 1614021300000,
            "z": "C",
            "tradability": "NONE",
        }
        self.ws_streams.trading_status_stream = AsyncMock(
            return_value=expected_response
        )

        response = await self.ws_streams.trading_status_stream(**params)
        assert response is not None
        assert response == expected_response

    @pytest.mark.asyncio
    async def test_trading_status_stream_success_with_optional_params(self):
        """Test trading_status_stream() successfully with optional parameters."""

        params = {"symbol": "AAPL"}

        expected_response = {
            "e": "tradingStatus",
            "symbol": "AAPL",
            "assetCode": "EQ_AAPL",
            "status": "TRADING_HALTED",
            "msg": "MATERIAL_NEWS",
            "time": 1614021300000,
            "z": "C",
            "tradability": "NONE",
        }

        self.ws_streams.trading_status_stream = AsyncMock(
            return_value=expected_response
        )

        response = await self.ws_streams.trading_status_stream(**params)
        assert response is not None
        assert response == expected_response

    @pytest.mark.asyncio
    async def test_trading_status_stream_missing_required_param_symbol(self):
        """Test that trading_status_stream() raises RequiredError when 'symbol' is missing."""
        params = {"symbol": "AAPL"}
        params["symbol"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'symbol'"):
            await self.ws_streams.trading_status_stream(**params)

    @pytest.mark.asyncio
    async def test_trading_status_stream_server_error(self):
        """Test that trading_status_stream() raises an error when the server returns an error."""

        params = {"symbol": "AAPL"}

        mock_error = Exception("ResponseError")
        self.ws_streams.trading_status_stream = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            await self.ws_streams.trading_status_stream(**params)
