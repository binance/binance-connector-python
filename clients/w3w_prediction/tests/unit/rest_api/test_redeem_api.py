"""
Prediction Trading REST API

Place and manage prediction market orders, query positions, and transfer funds via the Prediction Trading REST API.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_w3w_prediction.rest_api.api import RedeemApi
from binance_sdk_w3w_prediction.rest_api.models import BatchRedeemResponse
from binance_sdk_w3w_prediction.rest_api.models import GetRedeemStatusResponse


class TestRedeemApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = RedeemApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    @patch("binance_common.utils.get_signature")
    def test_batch_redeem_success(self, mock_get_signature):
        """Test batch_redeem() successfully with required parameters only."""

        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "wallet_id": "5b5c1ec3be4e4416a5872b21c1ca5d20",
            "token_ids": ["112233"],
        }

        expected_response = {
            "batchId": "batch_20260525_redeem_001",
            "results": [
                {
                    "requestId": "req_001",
                    "txHash": "0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd",
                    "status": "PENDING",
                    "error": "error",
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.batch_redeem(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/batch-redeem" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"
        assert (
            normalized["walletAddress"] == "0x12e32db8817e292508c34111cbc4b23340df542c"
        )
        assert normalized["walletId"] == "5b5c1ec3be4e4416a5872b21c1ca5d20"
        assert "tokenIds" in normalized

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(BatchRedeemResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(BatchRedeemResponse, "from_dict"):
            expected = BatchRedeemResponse.from_dict(expected_response)
        else:
            expected = BatchRedeemResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_batch_redeem_success_with_optional_params(self, mock_get_signature):
        """Test batch_redeem() successfully with optional parameters."""

        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "wallet_id": "5b5c1ec3be4e4416a5872b21c1ca5d20",
            "token_ids": ["112233"],
            "chain_id": "56",
        }

        expected_response = {
            "batchId": "batch_20260525_redeem_001",
            "results": [
                {
                    "requestId": "req_001",
                    "txHash": "0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd",
                    "status": "PENDING",
                    "error": "error",
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.batch_redeem(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/batch-redeem" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(BatchRedeemResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(BatchRedeemResponse, "from_dict"):
            expected = BatchRedeemResponse.from_dict(expected_response)
        else:
            expected = BatchRedeemResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_batch_redeem_missing_required_param_wallet_address(self):
        """Test that batch_redeem() raises RequiredError when 'wallet_address' is missing."""
        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "wallet_id": "5b5c1ec3be4e4416a5872b21c1ca5d20",
            "token_ids": ["112233"],
        }
        params["wallet_address"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'wallet_address'"
        ):
            self.client.batch_redeem(**params)

    def test_batch_redeem_missing_required_param_wallet_id(self):
        """Test that batch_redeem() raises RequiredError when 'wallet_id' is missing."""
        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "wallet_id": "5b5c1ec3be4e4416a5872b21c1ca5d20",
            "token_ids": ["112233"],
        }
        params["wallet_id"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'wallet_id'"
        ):
            self.client.batch_redeem(**params)

    def test_batch_redeem_missing_required_param_token_ids(self):
        """Test that batch_redeem() raises RequiredError when 'token_ids' is missing."""
        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "wallet_id": "5b5c1ec3be4e4416a5872b21c1ca5d20",
            "token_ids": ["112233"],
        }
        params["token_ids"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'token_ids'"
        ):
            self.client.batch_redeem(**params)

    def test_batch_redeem_server_error(self):
        """Test that batch_redeem() raises an error when the server returns an error."""

        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "wallet_id": "5b5c1ec3be4e4416a5872b21c1ca5d20",
            "token_ids": ["112233"],
        }

        mock_error = Exception("ResponseError")
        self.client.batch_redeem = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.batch_redeem(**params)

    @patch("binance_common.utils.get_signature")
    def test_get_redeem_status_success(self, mock_get_signature):
        """Test get_redeem_status() successfully with required parameters only."""

        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "tx_hash": "0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd",
        }

        expected_response = {
            "txHash": "0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd",
            "status": "CONFIRMED",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_redeem_status(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/redeem/status" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert (
            normalized["walletAddress"] == "0x12e32db8817e292508c34111cbc4b23340df542c"
        )
        assert (
            normalized["txHash"]
            == "0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd"
        )

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRedeemStatusResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetRedeemStatusResponse, "from_dict"):
            expected = GetRedeemStatusResponse.from_dict(expected_response)
        else:
            expected = GetRedeemStatusResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_redeem_status_success_with_optional_params(self, mock_get_signature):
        """Test get_redeem_status() successfully with optional parameters."""

        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "tx_hash": "0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd",
            "recv_window": 5000,
        }

        expected_response = {
            "txHash": "0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd",
            "status": "CONFIRMED",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_redeem_status(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/redeem/status" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRedeemStatusResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetRedeemStatusResponse, "from_dict"):
            expected = GetRedeemStatusResponse.from_dict(expected_response)
        else:
            expected = GetRedeemStatusResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_redeem_status_missing_required_param_wallet_address(self):
        """Test that get_redeem_status() raises RequiredError when 'wallet_address' is missing."""
        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "tx_hash": "0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd",
        }
        params["wallet_address"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'wallet_address'"
        ):
            self.client.get_redeem_status(**params)

    def test_get_redeem_status_missing_required_param_tx_hash(self):
        """Test that get_redeem_status() raises RequiredError when 'tx_hash' is missing."""
        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "tx_hash": "0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd",
        }
        params["tx_hash"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'tx_hash'"):
            self.client.get_redeem_status(**params)

    def test_get_redeem_status_server_error(self):
        """Test that get_redeem_status() raises an error when the server returns an error."""

        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "tx_hash": "0xabc123def456789abcdef123456789abcdef123456789abcdef123456789abcd",
        }

        mock_error = Exception("ResponseError")
        self.client.get_redeem_status = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_redeem_status(**params)
