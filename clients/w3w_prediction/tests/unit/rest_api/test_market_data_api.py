"""
Prediction Trading REST API

Place and manage prediction market orders, query positions, and transfer funds via the Prediction Trading REST API.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_w3w_prediction.rest_api.api import MarketDataApi
from binance_sdk_w3w_prediction.rest_api.models import GetMarketDetailResponse
from binance_sdk_w3w_prediction.rest_api.models import ListPredictionCategoriesResponse
from binance_sdk_w3w_prediction.rest_api.models import ListPredictionMarketsResponse
from binance_sdk_w3w_prediction.rest_api.models import MarketSearchResponse
from binance_sdk_w3w_prediction.rest_api.models import QueryLastTradePriceResponse
from binance_sdk_w3w_prediction.rest_api.models import QueryOrderBookResponse


from binance_sdk_w3w_prediction.rest_api.models import ListPredictionMarketsSortByEnum
from binance_sdk_w3w_prediction.rest_api.models import ListPredictionMarketsOrderByEnum


class TestMarketDataApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = MarketDataApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    @patch("binance_common.utils.get_signature")
    def test_get_market_detail_success(self, mock_get_signature):
        """Test get_market_detail() successfully with required parameters only."""

        params = {"market_topic_id": 4229564}

        expected_response = {
            "marketTopicId": 4229564,
            "vendor": "PREDICT_FUN",
            "chainId": "56",
            "slug": "btc-price-1h-up-or-down",
            "title": "BTC Price 1h Up or Down?",
            "question": "Will BTC price go UP in the next 1 hour?",
            "description": "Resolves YES if BTC spot price is higher than the starting price at resolution time.",
            "imageUrl": "https://cdn.example.com/prediction/btc-1h.png",
            "topicType": "FLAT",
            "chartType": "CRYPTO_UP_DOWN",
            "symbol": "BTCUSDT",
            "variantData": {
                "type": "CRYPTO_UP_DOWN",
                "startPrice": "67890.12",
                "endPrice": "endPrice",
                "priceFeedId": "0xpricefeedid123",
                "priceFeedProvider": "BINANCE",
                "priceFeedSymbol": "BTCUSDT",
            },
            "participantCount": 3420,
            "collateral": "USDT",
            "feeRateBps": 200,
            "slippageBps": 1200,
            "isYieldBearing": False,
            "tradeVolume": "158234.56",
            "liquidity": "45000.00",
            "publishedAt": 1748100000000,
            "startDate": 1748131200000,
            "endDate": 1748134800000,
            "status": "REGISTERED",
            "timeline": [
                {
                    "marketTopicId": 4229560,
                    "startDate": 1748127600000,
                    "endDate": 1748131200000,
                }
            ],
            "markets": [
                {
                    "marketId": 5567895,
                    "externalId": "ext_001",
                    "title": "UP",
                    "question": "Will BTC go UP?",
                    "description": "Resolves YES if BTC price increases.",
                    "conditionId": "0xabc123",
                    "status": "REGISTERED",
                    "tradingStatus": "OPEN",
                    "tradeVolume": "90000.00",
                    "liquidity": "25000.00",
                    "decimalPrecision": 2,
                    "outcomes": [
                        {
                            "name": "YES",
                            "price": "0.52",
                            "chance": "0.52",
                            "index": 0,
                            "tokenId": "112233",
                        }
                    ],
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_market_detail(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/market/detail" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["marketTopicId"] == 4229564

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetMarketDetailResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetMarketDetailResponse, "from_dict"):
            expected = GetMarketDetailResponse.from_dict(expected_response)
        else:
            expected = GetMarketDetailResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_market_detail_success_with_optional_params(self, mock_get_signature):
        """Test get_market_detail() successfully with optional parameters."""

        params = {"market_topic_id": 4229564}

        expected_response = {
            "marketTopicId": 4229564,
            "vendor": "PREDICT_FUN",
            "chainId": "56",
            "slug": "btc-price-1h-up-or-down",
            "title": "BTC Price 1h Up or Down?",
            "question": "Will BTC price go UP in the next 1 hour?",
            "description": "Resolves YES if BTC spot price is higher than the starting price at resolution time.",
            "imageUrl": "https://cdn.example.com/prediction/btc-1h.png",
            "topicType": "FLAT",
            "chartType": "CRYPTO_UP_DOWN",
            "symbol": "BTCUSDT",
            "variantData": {
                "type": "CRYPTO_UP_DOWN",
                "startPrice": "67890.12",
                "endPrice": "endPrice",
                "priceFeedId": "0xpricefeedid123",
                "priceFeedProvider": "BINANCE",
                "priceFeedSymbol": "BTCUSDT",
            },
            "participantCount": 3420,
            "collateral": "USDT",
            "feeRateBps": 200,
            "slippageBps": 1200,
            "isYieldBearing": False,
            "tradeVolume": "158234.56",
            "liquidity": "45000.00",
            "publishedAt": 1748100000000,
            "startDate": 1748131200000,
            "endDate": 1748134800000,
            "status": "REGISTERED",
            "timeline": [
                {
                    "marketTopicId": 4229560,
                    "startDate": 1748127600000,
                    "endDate": 1748131200000,
                }
            ],
            "markets": [
                {
                    "marketId": 5567895,
                    "externalId": "ext_001",
                    "title": "UP",
                    "question": "Will BTC go UP?",
                    "description": "Resolves YES if BTC price increases.",
                    "conditionId": "0xabc123",
                    "status": "REGISTERED",
                    "tradingStatus": "OPEN",
                    "tradeVolume": "90000.00",
                    "liquidity": "25000.00",
                    "decimalPrecision": 2,
                    "outcomes": [
                        {
                            "name": "YES",
                            "price": "0.52",
                            "chance": "0.52",
                            "index": 0,
                            "tokenId": "112233",
                        }
                    ],
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_market_detail(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/market/detail" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetMarketDetailResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetMarketDetailResponse, "from_dict"):
            expected = GetMarketDetailResponse.from_dict(expected_response)
        else:
            expected = GetMarketDetailResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_market_detail_missing_required_param_market_topic_id(self):
        """Test that get_market_detail() raises RequiredError when 'market_topic_id' is missing."""
        params = {"market_topic_id": 4229564}
        params["market_topic_id"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'market_topic_id'"
        ):
            self.client.get_market_detail(**params)

    def test_get_market_detail_server_error(self):
        """Test that get_market_detail() raises an error when the server returns an error."""

        params = {"market_topic_id": 4229564}

        mock_error = Exception("ResponseError")
        self.client.get_market_detail = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_market_detail(**params)

    @patch("binance_common.utils.get_signature")
    def test_list_prediction_categories_success(self, mock_get_signature):
        """Test list_prediction_categories() successfully with required parameters only."""

        expected_response = {
            "categories": [
                {
                    "id": "crypto",
                    "name": "Crypto",
                    "icon": "crypto_icon",
                    "order": 1,
                    "subcategories": [
                        {
                            "id": "up-down",
                            "name": "Up/Down",
                            "icon": "updown_icon",
                            "order": 1,
                        }
                    ],
                }
            ]
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.list_prediction_categories()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/category/list" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(ListPredictionCategoriesResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(ListPredictionCategoriesResponse, "from_dict")
        ):
            expected = ListPredictionCategoriesResponse.from_dict(expected_response)
        else:
            expected = ListPredictionCategoriesResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_list_prediction_categories_server_error(self):
        """Test that list_prediction_categories() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.list_prediction_categories = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.list_prediction_categories()

    @patch("binance_common.utils.get_signature")
    def test_list_prediction_markets_success(self, mock_get_signature):
        """Test list_prediction_markets() successfully with required parameters only."""

        expected_response = {
            "marketTopics": [
                {
                    "marketTopicId": 4229564,
                    "vendor": "PREDICT_FUN",
                    "chainId": "56",
                    "slug": "btc-price-1h-up-or-down",
                    "title": "BTC Price 1h Up or Down?",
                    "question": "Will BTC price go UP in the next 1 hour?",
                    "description": "Resolves YES if BTC spot price is higher than the starting price at resolution time.",
                    "imageUrl": "https://cdn.example.com/prediction/btc-1h.png",
                    "topicType": "FLAT",
                    "chartType": "CRYPTO_UP_DOWN",
                    "symbol": "BTCUSDT",
                    "participantCount": 3420,
                    "collateral": "USDT",
                    "feeRateBps": 200,
                    "slippageBps": 1200,
                    "isYieldBearing": False,
                    "tradeVolume": "158234.56",
                    "liquidity": "45000.00",
                    "publishedAt": 1748100000000,
                    "startDate": 1748131200000,
                    "endDate": 1748134800000,
                    "status": "REGISTERED",
                    "markets": [{}],
                }
            ],
            "total": 128,
            "offset": 0,
            "limit": 20,
            "hasMore": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.list_prediction_markets()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/market/list" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(ListPredictionMarketsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(ListPredictionMarketsResponse, "from_dict"):
            expected = ListPredictionMarketsResponse.from_dict(expected_response)
        else:
            expected = ListPredictionMarketsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_list_prediction_markets_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test list_prediction_markets() successfully with optional parameters."""

        params = {
            "l1_category": "crypto",
            "l2_category": "up-down",
            "sort_by": ListPredictionMarketsSortByEnum["RECOMMENDED"].value,
            "order_by": ListPredictionMarketsOrderByEnum["ASC"].value,
            "offset": 0,
            "limit": 20,
        }

        expected_response = {
            "marketTopics": [
                {
                    "marketTopicId": 4229564,
                    "vendor": "PREDICT_FUN",
                    "chainId": "56",
                    "slug": "btc-price-1h-up-or-down",
                    "title": "BTC Price 1h Up or Down?",
                    "question": "Will BTC price go UP in the next 1 hour?",
                    "description": "Resolves YES if BTC spot price is higher than the starting price at resolution time.",
                    "imageUrl": "https://cdn.example.com/prediction/btc-1h.png",
                    "topicType": "FLAT",
                    "chartType": "CRYPTO_UP_DOWN",
                    "symbol": "BTCUSDT",
                    "participantCount": 3420,
                    "collateral": "USDT",
                    "feeRateBps": 200,
                    "slippageBps": 1200,
                    "isYieldBearing": False,
                    "tradeVolume": "158234.56",
                    "liquidity": "45000.00",
                    "publishedAt": 1748100000000,
                    "startDate": 1748131200000,
                    "endDate": 1748134800000,
                    "status": "REGISTERED",
                    "markets": [{}],
                }
            ],
            "total": 128,
            "offset": 0,
            "limit": 20,
            "hasMore": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.list_prediction_markets(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/market/list" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(ListPredictionMarketsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(ListPredictionMarketsResponse, "from_dict"):
            expected = ListPredictionMarketsResponse.from_dict(expected_response)
        else:
            expected = ListPredictionMarketsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_list_prediction_markets_server_error(self):
        """Test that list_prediction_markets() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.list_prediction_markets = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.list_prediction_markets()

    @patch("binance_common.utils.get_signature")
    def test_market_search_success(self, mock_get_signature):
        """Test market_search() successfully with required parameters only."""

        params = {
            "query": "BTC price",
        }

        expected_response = [
            {
                "marketTopicId": 4229564,
                "vendor": "PREDICT_FUN",
                "chainId": "56",
                "slug": "btc-price-1h-up-or-down",
                "title": "BTC Price 1h Up or Down?",
                "question": "Will BTC price go UP in the next 1 hour?",
                "description": "Resolves YES if BTC spot price is higher than the starting price.",
                "topicType": "FLAT",
                "chartType": "CRYPTO_UP_DOWN",
                "symbol": "BTCUSDT",
                "participantCount": 3420,
                "collateral": "USDT",
                "tradeVolume": "158234.56",
                "liquidity": "45000.00",
                "startDate": 1748131200000,
                "endDate": 1748134800000,
                "status": "REGISTERED",
                "markets": [{}],
            }
        ]
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.market_search(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/market/search" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["query"] == "BTC price"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(MarketSearchResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(MarketSearchResponse, "from_dict"):
            expected = MarketSearchResponse.from_dict(expected_response)
        else:
            expected = MarketSearchResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_market_search_success_with_optional_params(self, mock_get_signature):
        """Test market_search() successfully with optional parameters."""

        params = {"query": "BTC price", "top_k": 20}

        expected_response = [
            {
                "marketTopicId": 4229564,
                "vendor": "PREDICT_FUN",
                "chainId": "56",
                "slug": "btc-price-1h-up-or-down",
                "title": "BTC Price 1h Up or Down?",
                "question": "Will BTC price go UP in the next 1 hour?",
                "description": "Resolves YES if BTC spot price is higher than the starting price.",
                "topicType": "FLAT",
                "chartType": "CRYPTO_UP_DOWN",
                "symbol": "BTCUSDT",
                "participantCount": 3420,
                "collateral": "USDT",
                "tradeVolume": "158234.56",
                "liquidity": "45000.00",
                "startDate": 1748131200000,
                "endDate": 1748134800000,
                "status": "REGISTERED",
                "markets": [{}],
            }
        ]
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.market_search(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/market/search" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(MarketSearchResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(MarketSearchResponse, "from_dict"):
            expected = MarketSearchResponse.from_dict(expected_response)
        else:
            expected = MarketSearchResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_market_search_missing_required_param_query(self):
        """Test that market_search() raises RequiredError when 'query' is missing."""
        params = {
            "query": "BTC price",
        }
        params["query"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'query'"):
            self.client.market_search(**params)

    def test_market_search_server_error(self):
        """Test that market_search() raises an error when the server returns an error."""

        params = {
            "query": "BTC price",
        }

        mock_error = Exception("ResponseError")
        self.client.market_search = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.market_search(**params)

    @patch("binance_common.utils.get_signature")
    def test_query_last_trade_price_success(self, mock_get_signature):
        """Test query_last_trade_price() successfully with required parameters only."""

        params = {"market_id": 5567895}

        expected_response = {"marketId": 5567895, "lastTradePrice": "0.52"}
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.query_last_trade_price(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert (
            "/sapi/v1/w3w/wallet/prediction/order-book/last-trade-price"
            in request_kwargs["url"]
        )
        assert request_kwargs["method"] == "GET"
        assert normalized["marketId"] == 5567895

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(QueryLastTradePriceResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(QueryLastTradePriceResponse, "from_dict"):
            expected = QueryLastTradePriceResponse.from_dict(expected_response)
        else:
            expected = QueryLastTradePriceResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_query_last_trade_price_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test query_last_trade_price() successfully with optional parameters."""

        params = {"market_id": 5567895}

        expected_response = {"marketId": 5567895, "lastTradePrice": "0.52"}
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.query_last_trade_price(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert (
            "/sapi/v1/w3w/wallet/prediction/order-book/last-trade-price"
            in request_kwargs["url"]
        )
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(QueryLastTradePriceResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(QueryLastTradePriceResponse, "from_dict"):
            expected = QueryLastTradePriceResponse.from_dict(expected_response)
        else:
            expected = QueryLastTradePriceResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_query_last_trade_price_missing_required_param_market_id(self):
        """Test that query_last_trade_price() raises RequiredError when 'market_id' is missing."""
        params = {"market_id": 5567895}
        params["market_id"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'market_id'"
        ):
            self.client.query_last_trade_price(**params)

    def test_query_last_trade_price_server_error(self):
        """Test that query_last_trade_price() raises an error when the server returns an error."""

        params = {"market_id": 5567895}

        mock_error = Exception("ResponseError")
        self.client.query_last_trade_price = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.query_last_trade_price(**params)

    @patch("binance_common.utils.get_signature")
    def test_query_order_book_success(self, mock_get_signature):
        """Test query_order_book() successfully with required parameters only."""

        params = {"vendor": "predict_fun", "market_id": 5567895, "token_id": "112233"}

        expected_response = {
            "outcome": "YES",
            "tokenId": "112233",
            "timestamp": 1748131800000,
            "bids": [{"price": "0.51", "size": "5000.00"}],
            "asks": [{"price": "0.52", "size": "3000.00"}],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.query_order_book(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/order-book" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["vendor"] == "predict_fun"
        assert normalized["marketId"] == 5567895
        assert normalized["tokenId"] == "112233"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(QueryOrderBookResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(QueryOrderBookResponse, "from_dict"):
            expected = QueryOrderBookResponse.from_dict(expected_response)
        else:
            expected = QueryOrderBookResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_query_order_book_success_with_optional_params(self, mock_get_signature):
        """Test query_order_book() successfully with optional parameters."""

        params = {"vendor": "predict_fun", "market_id": 5567895, "token_id": "112233"}

        expected_response = {
            "outcome": "YES",
            "tokenId": "112233",
            "timestamp": 1748131800000,
            "bids": [{"price": "0.51", "size": "5000.00"}],
            "asks": [{"price": "0.52", "size": "3000.00"}],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.query_order_book(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/order-book" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(QueryOrderBookResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(QueryOrderBookResponse, "from_dict"):
            expected = QueryOrderBookResponse.from_dict(expected_response)
        else:
            expected = QueryOrderBookResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_query_order_book_missing_required_param_vendor(self):
        """Test that query_order_book() raises RequiredError when 'vendor' is missing."""
        params = {"vendor": "predict_fun", "market_id": 5567895, "token_id": "112233"}
        params["vendor"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'vendor'"):
            self.client.query_order_book(**params)

    def test_query_order_book_missing_required_param_market_id(self):
        """Test that query_order_book() raises RequiredError when 'market_id' is missing."""
        params = {"vendor": "predict_fun", "market_id": 5567895, "token_id": "112233"}
        params["market_id"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'market_id'"
        ):
            self.client.query_order_book(**params)

    def test_query_order_book_missing_required_param_token_id(self):
        """Test that query_order_book() raises RequiredError when 'token_id' is missing."""
        params = {"vendor": "predict_fun", "market_id": 5567895, "token_id": "112233"}
        params["token_id"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'token_id'"
        ):
            self.client.query_order_book(**params)

    def test_query_order_book_server_error(self):
        """Test that query_order_book() raises an error when the server returns an error."""

        params = {"vendor": "predict_fun", "market_id": 5567895, "token_id": "112233"}

        mock_error = Exception("ResponseError")
        self.client.query_order_book = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.query_order_book(**params)
