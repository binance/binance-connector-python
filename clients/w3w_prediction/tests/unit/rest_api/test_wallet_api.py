"""
Prediction Trading REST API

Place and manage prediction market orders, query positions, and transfer funds via the Prediction Trading REST API.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_w3w_prediction.rest_api.api import WalletApi
from binance_sdk_w3w_prediction.rest_api.models import GetPortfolioResponse
from binance_sdk_w3w_prediction.rest_api.models import GetQuotaStatusResponse
from binance_sdk_w3w_prediction.rest_api.models import ListPredictionWalletsResponse
from binance_sdk_w3w_prediction.rest_api.models import (
    QueryPaymentOptionBalancesResponse,
)


class TestWalletApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = WalletApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    @patch("binance_common.utils.get_signature")
    def test_get_portfolio_success(self, mock_get_signature):
        """Test get_portfolio() successfully with required parameters only."""

        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
        }

        expected_response = {
            "chainId": "56",
            "walletAddress": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "activePositionsCount": 3,
            "totalRealizedPnl": "120.50",
            "totalUnrealizedPnl": "15.30",
            "totalPnl": "135.80",
            "totalCostBasis": "450.00",
            "totalCurrentValue": "465.30",
            "positions": [
                {
                    "id": 10001,
                    "walletAddress": "0x12e32db8817e292508c34111cbc4b23340df542c",
                    "marketTopicId": 4229564,
                    "marketId": 5567895,
                    "tokenId": "112233",
                    "vendor": "PREDICT_FUN",
                    "currentShares": "1923.07",
                    "avgPrice": "0.52",
                    "currentPrice": "0.55",
                    "realizedPnl": "0.00",
                    "unrealizedPnl": "0.06",
                    "totalPnl": "0.06",
                    "pnlPercentage": "6.00",
                    "isResolved": False,
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_portfolio(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/pnl/portfolio" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert (
            normalized["walletAddress"] == "0x12e32db8817e292508c34111cbc4b23340df542c"
        )

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetPortfolioResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetPortfolioResponse, "from_dict"):
            expected = GetPortfolioResponse.from_dict(expected_response)
        else:
            expected = GetPortfolioResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_portfolio_success_with_optional_params(self, mock_get_signature):
        """Test get_portfolio() successfully with optional parameters."""

        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "token_id": "112233",
            "market_id": 5567895,
            "market_topic_id": 4229564,
            "active_only": False,
            "recv_window": 5000,
        }

        expected_response = {
            "chainId": "56",
            "walletAddress": "0x12e32db8817e292508c34111cbc4b23340df542c",
            "activePositionsCount": 3,
            "totalRealizedPnl": "120.50",
            "totalUnrealizedPnl": "15.30",
            "totalPnl": "135.80",
            "totalCostBasis": "450.00",
            "totalCurrentValue": "465.30",
            "positions": [
                {
                    "id": 10001,
                    "walletAddress": "0x12e32db8817e292508c34111cbc4b23340df542c",
                    "marketTopicId": 4229564,
                    "marketId": 5567895,
                    "tokenId": "112233",
                    "vendor": "PREDICT_FUN",
                    "currentShares": "1923.07",
                    "avgPrice": "0.52",
                    "currentPrice": "0.55",
                    "realizedPnl": "0.00",
                    "unrealizedPnl": "0.06",
                    "totalPnl": "0.06",
                    "pnlPercentage": "6.00",
                    "isResolved": False,
                }
            ],
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_portfolio(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/pnl/portfolio" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetPortfolioResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetPortfolioResponse, "from_dict"):
            expected = GetPortfolioResponse.from_dict(expected_response)
        else:
            expected = GetPortfolioResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_portfolio_missing_required_param_wallet_address(self):
        """Test that get_portfolio() raises RequiredError when 'wallet_address' is missing."""
        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
        }
        params["wallet_address"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'wallet_address'"
        ):
            self.client.get_portfolio(**params)

    def test_get_portfolio_server_error(self):
        """Test that get_portfolio() raises an error when the server returns an error."""

        params = {
            "wallet_address": "0x12e32db8817e292508c34111cbc4b23340df542c",
        }

        mock_error = Exception("ResponseError")
        self.client.get_portfolio = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_portfolio(**params)

    @patch("binance_common.utils.get_signature")
    def test_get_quota_status_success(self, mock_get_signature):
        """Test get_quota_status() successfully with required parameters only."""

        expected_response = {"dailyLimit": "10000.00", "remainingDailyLimit": "8500.00"}
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_quota_status()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert (
            "/sapi/v1/w3w/wallet/prediction/quota/limit/status" in request_kwargs["url"]
        )
        assert request_kwargs["method"] == "GET"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetQuotaStatusResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetQuotaStatusResponse, "from_dict"):
            expected = GetQuotaStatusResponse.from_dict(expected_response)
        else:
            expected = GetQuotaStatusResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_quota_status_success_with_optional_params(self, mock_get_signature):
        """Test get_quota_status() successfully with optional parameters."""

        params = {"recv_window": 5000}

        expected_response = {"dailyLimit": "10000.00", "remainingDailyLimit": "8500.00"}
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_quota_status(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert (
            "/sapi/v1/w3w/wallet/prediction/quota/limit/status" in request_kwargs["url"]
        )
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetQuotaStatusResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetQuotaStatusResponse, "from_dict"):
            expected = GetQuotaStatusResponse.from_dict(expected_response)
        else:
            expected = GetQuotaStatusResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_quota_status_server_error(self):
        """Test that get_quota_status() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_quota_status = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_quota_status()

    @patch("binance_common.utils.get_signature")
    def test_list_prediction_wallets_success(self, mock_get_signature):
        """Test list_prediction_wallets() successfully with required parameters only."""

        expected_response = {
            "wallets": [
                {
                    "walletAddress": "0x12e32db8817e292508c34111cbc4b23340df542c",
                    "walletId": "5b5c1ec3be4e4416a5872b21c1ca5d20",
                    "registeredTime": 1748000000000,
                }
            ]
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.list_prediction_wallets()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/wallet/list" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(ListPredictionWalletsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(ListPredictionWalletsResponse, "from_dict"):
            expected = ListPredictionWalletsResponse.from_dict(expected_response)
        else:
            expected = ListPredictionWalletsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_list_prediction_wallets_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test list_prediction_wallets() successfully with optional parameters."""

        params = {"recv_window": 5000}

        expected_response = {
            "wallets": [
                {
                    "walletAddress": "0x12e32db8817e292508c34111cbc4b23340df542c",
                    "walletId": "5b5c1ec3be4e4416a5872b21c1ca5d20",
                    "registeredTime": 1748000000000,
                }
            ]
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.list_prediction_wallets(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/w3w/wallet/prediction/wallet/list" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(ListPredictionWalletsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(ListPredictionWalletsResponse, "from_dict"):
            expected = ListPredictionWalletsResponse.from_dict(expected_response)
        else:
            expected = ListPredictionWalletsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_list_prediction_wallets_server_error(self):
        """Test that list_prediction_wallets() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.list_prediction_wallets = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.list_prediction_wallets()

    @patch("binance_common.utils.get_signature")
    def test_query_payment_option_balances_success(self, mock_get_signature):
        """Test query_payment_option_balances() successfully with required parameters only."""

        expected_response = {
            "items": [
                {
                    "accountType": "SPOT",
                    "availableBalanceDisplay": "1000.00",
                    "enabled": True,
                }
            ]
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.query_payment_option_balances()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert (
            "/sapi/v1/w3w/wallet/prediction/balance/payment-options"
            in request_kwargs["url"]
        )
        assert request_kwargs["method"] == "GET"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(QueryPaymentOptionBalancesResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(QueryPaymentOptionBalancesResponse, "from_dict")
        ):
            expected = QueryPaymentOptionBalancesResponse.from_dict(expected_response)
        else:
            expected = QueryPaymentOptionBalancesResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_query_payment_option_balances_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test query_payment_option_balances() successfully with optional parameters."""

        params = {"recv_window": 5000}

        expected_response = {
            "items": [
                {
                    "accountType": "SPOT",
                    "availableBalanceDisplay": "1000.00",
                    "enabled": True,
                }
            ]
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.query_payment_option_balances(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert (
            "/sapi/v1/w3w/wallet/prediction/balance/payment-options"
            in request_kwargs["url"]
        )
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(QueryPaymentOptionBalancesResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(QueryPaymentOptionBalancesResponse, "from_dict")
        ):
            expected = QueryPaymentOptionBalancesResponse.from_dict(expected_response)
        else:
            expected = QueryPaymentOptionBalancesResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_query_payment_option_balances_server_error(self):
        """Test that query_payment_option_balances() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.query_payment_option_balances = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.query_payment_option_balances()
