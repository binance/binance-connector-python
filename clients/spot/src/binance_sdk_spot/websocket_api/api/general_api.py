"""
Spot WebSocket API

Access market data, manage accounts, and trade on Binance Spot.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from binance_common.models import WebsocketApiResponse
from binance_common.signature import Signers
from binance_common.websocket import WebSocketAPIBase

from ..models import ExchangeInfoResponse
from ..models import ExecutionRulesResponse
from ..models import PingResponse
from ..models import TimeResponse


from ..models import ExchangeInfoSymbolStatusEnum


from ..models import ExecutionRulesSymbolStatusEnum


from typing import List, Optional


class GeneralApi:
    """API Client for GeneralApi endpoints."""

    def __init__(
        self,
        websocket_api: WebSocketAPIBase,
        signer: Signers = None,
    ) -> None:
        self.websocket_api = websocket_api
        self.signer = signer

    async def exchange_info(
        self,
        id: Optional[str] = None,
        symbol: Optional[str] = None,
        symbols: Optional[List[str]] = None,
        permissions: Optional[List[str]] = None,
        show_permission_sets: Optional[bool] = None,
        symbol_status: Optional[ExchangeInfoSymbolStatusEnum] = None,
    ) -> WebsocketApiResponse[ExchangeInfoResponse]:
        """
            Exchange information
            /exchangeInfo
            https://developers.binance.com/en/docs/catalog/core-trading-spot-trading/api/ws-api/general#exchange-info

            Query current exchange trading rules, rate limits, and symbol
        information.

        Weight(IP): 20

        Security Type: NONE

        Notes:
        **Data Source:** Memory

        **Notes:**
        * If the value provided to `symbol` or `symbols` do not exist, the endpoint will throw an error saying the symbol is invalid.
        * All parameters are optional.
        * Only one of `symbol`, `symbols`, `permissions` parameters can be specified.
        * Without parameters, `exchangeInfo` displays all symbols with `["SPOT", "MARGIN", "LEVERAGED"]` permissions.
          * In order to list *all* active symbols on the exchange, you need to explicitly request all permissions.
        * `permissions` accepts either a list of permissions, or a single permission name. E.g. `"SPOT"`.

        **Examples of Symbol Permissions Interpretation from the Response:**

        * `[["A","B"]]` means you may place an order if your account has either permission "A" **or** permission "B".
        * `[["A"],["B"]]` means you can place an order if your account has permission "A" **and** permission "B".
        * `[["A"],["B","C"]]` means you can place an order if your account has permission "A" **and** permission "B" or permission "C". (Inclusive or is applied here, not exclusive or, so your account may have both permission "B" and permission "C".)

            Args:
                    id (Optional[str] = None): Client-generated request identifier.
                    symbol (Optional[str] = None): Describe a single symbol
                    symbols (Optional[List[str]] = None): Describe multiple symbols
                    permissions (Optional[List[str]] = None): Filter symbols by permissions
                    show_permission_sets (Optional[bool] = None): Controls whether the content of the `permissionSets` field is populated or not. Defaults to `true`.
                    symbol_status (Optional[ExchangeInfoSymbolStatusEnum] = None): Filters for symbols that have this `tradingStatus`. Valid values: `TRADING`, `HALT`, `BREAK`. Cannot be used in combination with `symbol` or `symbols`.

            Returns:
                WebsocketApiResponse[ExchangeInfoResponse]

            Raises:
                RequiredError: If a required parameter is missing.

        """

        params = {
            **({"id": id} if id is not None else {}),
            **({"symbol": symbol} if symbol is not None else {}),
            **({"symbols": symbols} if symbols is not None else {}),
            **({"permissions": permissions} if permissions is not None else {}),
            **(
                {"show_permission_sets": show_permission_sets}
                if show_permission_sets is not None
                else {}
            ),
            **({"symbol_status": symbol_status} if symbol_status is not None else {}),
        }

        payload = {
            "method": "/exchangeInfo".replace("/", "", 1),
            "params": params,
        }

        return await self.websocket_api.send_message(
            payload=payload, response_model=ExchangeInfoResponse
        )

    async def execution_rules(
        self,
        id: Optional[str] = None,
        symbol: Optional[str] = None,
        symbols: Optional[List[str]] = None,
        symbol_status: Optional[ExecutionRulesSymbolStatusEnum] = None,
    ) -> WebsocketApiResponse[ExecutionRulesResponse]:
        """
            Query Execution Rules
            /executionRules
            https://developers.binance.com/en/docs/catalog/core-trading-spot-trading/api/ws-api/general#execution-rules

            Query execution rules for symbols.

        Weight: Parameter | Weight
        --- | ---
        `symbol` | 2
        `symbols` | 2 for each `symbol`, capped at a max of 40
        `symbolStatus` | 40
        None | 40

        Security Type: NONE

        Notes:
        **Data Source:** Memory

        **Note:** No combination of multiple parameters is allowed.

            Args:
                    id (Optional[str] = None): Client-generated request identifier.
                    symbol (Optional[str] = None): Query for specified symbol.
                    symbols (Optional[List[str]] = None): Query for multiple symbols.
                    symbol_status (Optional[ExecutionRulesSymbolStatusEnum] = None): Query for all symbols with the specified status. Supported values: `TRADING`, `HALT`, `BREAK`

            Returns:
                WebsocketApiResponse[ExecutionRulesResponse]

            Raises:
                RequiredError: If a required parameter is missing.

        """

        params = {
            **({"id": id} if id is not None else {}),
            **({"symbol": symbol} if symbol is not None else {}),
            **({"symbols": symbols} if symbols is not None else {}),
            **({"symbol_status": symbol_status} if symbol_status is not None else {}),
        }

        payload = {
            "method": "/executionRules".replace("/", "", 1),
            "params": params,
        }

        return await self.websocket_api.send_message(
            payload=payload, response_model=ExecutionRulesResponse
        )

    async def ping(
        self,
        id: Optional[str] = None,
    ) -> WebsocketApiResponse[PingResponse]:
        """
            Test connectivity
            /ping
            https://developers.binance.com/en/docs/catalog/core-trading-spot-trading/api/ws-api/general#ping

            Test connectivity to the WebSocket API.

        Note: You can use regular WebSocket ping frames to test connectivity as well, WebSocket API will respond with pong frames as soon as possible. ping request along with time is a safe way to test request-response handling in your application.

        Weight(IP): 1

        Security Type: NONE

        Notes:
        **Data Source:** Memory

            Args:
                    id (Optional[str] = None): Client-generated request identifier.

            Returns:
                WebsocketApiResponse[PingResponse]

            Raises:
                RequiredError: If a required parameter is missing.

        """

        params = {
            **({"id": id} if id is not None else {}),
        }

        payload = {
            "method": "/ping".replace("/", "", 1),
            "params": params,
        }

        return await self.websocket_api.send_message(
            payload=payload, response_model=PingResponse
        )

    async def time(
        self,
        id: Optional[str] = None,
    ) -> WebsocketApiResponse[TimeResponse]:
        """
            Check server time
            /time
            https://developers.binance.com/en/docs/catalog/core-trading-spot-trading/api/ws-api/general#time

            Test connectivity to the WebSocket API and get the current server time.

        Weight(IP): 1

        Security Type: NONE

        Notes:
        **Data Source:** Memory

            Args:
                    id (Optional[str] = None): Client-generated request identifier.

            Returns:
                WebsocketApiResponse[TimeResponse]

            Raises:
                RequiredError: If a required parameter is missing.

        """

        params = {
            **({"id": id} if id is not None else {}),
        }

        payload = {
            "method": "/time".replace("/", "", 1),
            "params": params,
        }

        return await self.websocket_api.send_message(
            payload=payload, response_model=TimeResponse
        )
