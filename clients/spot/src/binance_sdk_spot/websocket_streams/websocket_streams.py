"""
Spot WebSocket Market Streams

Access market data, manage accounts, and trade on Binance Spot.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from pydantic import BaseModel
from typing import Callable, Optional, TypeVar, Union

from binance_common.configuration import ConfigurationWebSocketStreams
from binance_common.websocket import (
    WebSocketStreamBase,
    WebSocketConnection,
    RequestStreamHandle,
    RequestStream,
)

from .models import UserDataStreamEventsResponse


from .streams.default_api import DefaultApi


from .models import AllMarketRollingWindowTickerWindowSizeEnum


from .models import DiffBookDepthUpdateSpeedEnum

from .models import KlineIntervalEnum


from .models import KlineOffsetIntervalEnum


from .models import PartialBookDepthLevelsEnum

from .models import PartialBookDepthUpdateSpeedEnum


from .models import RollingWindowTickerWindowSizeEnum


T = TypeVar("T", bound=BaseModel)


class SpotWebSocketStreams(WebSocketStreamBase):
    def __init__(
        self,
        configuration: ConfigurationWebSocketStreams,
    ) -> None:
        super().__init__(configuration)
        self.configuration = configuration

        self._defaultApi = DefaultApi(self)

    async def ping_server(self, connection: WebSocketConnection):
        """Sends a ping message to the WebSocket server to check the connection status.

        Args:
            connection (WebSocketConnection): The WebSocket connection to send the ping message to.
        """

        if self.session:
            await super().ping_ws_stream(connection=connection)
        else:
            raise ValueError("WebSocket session is not initialized.")

    async def subscribe(
        self,
        streams,
        response_model: Optional[T] = None,
        stream_url: Optional[str] = None,
    ):
        """Subscribes to the specified WebSocket streams.

        Args:
            streams (list): A list of WebSocket streams to subscribe to.
            response_model (Optional[T]): The Pydantic model to validate the response against.
        """

        await super().subscribe(
            streams, response_model=response_model, stream_url=stream_url
        )

    async def unsubscribe(self, streams):
        """Unsubscribes from the specified WebSocket streams.

        Args:
            streams (list): A list of WebSocket streams to unsubscribe from.
        """

        await super().unsubscribe(streams)

    def on(self, event: str, callback: Callable[[T], None], stream: str):
        """Registers a callback function for a specific WebSocket event.

        Args:
            event (str): The event type to listen for (e.g., 'message', 'error').
            callback (Callable[[T], None]): The callback function to be called when the event occurs.
            stream (str): The specific WebSocket stream to listen to.
        """

        super().on(event, callback, stream)

    async def list_subscribe(self) -> dict:
        """Lists all currently subscribed WebSocket streams.

        Returns:
            dict: A dictionary containing the current subscriptions.
        """

        return await super().list_subscribe()

    async def close_connection(
        self, connection: WebSocketConnection = None, close_session: bool = True
    ):
        """Closes the WebSocket connection and optionally the session.

        Args:
            connection (WebSocketConnection, optional): The WebSocket connection to close. If None, the current session's connection is used.
            close_session (bool, optional): Whether to close the session after closing the connection. Defaults to True.
        """

        await super().close_connection(connection, close_session)

    async def user_data(self, listenKey: str) -> RequestStreamHandle:
        """Subscribe to User Data Stream (USER_STREAM)

        Args:
            listenKey (str): The listen key for the user data stream.
        Returns:
            RequestStreamHandle: An instance of RequestStream that provides the on() and unsubscribe() methods.
        """

        return await RequestStream(
            self,
            listenKey,
            response_model=UserDataStreamEventsResponse,
        )

    async def agg_trade(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                Aggregate Trade Streams

                The Aggregate Trade Streams push trade information that is aggregated
        for a single taker order.

        Update Speed: Real-time

                Args:
                    symbol (Union[str, None]): Symbol to query
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.agg_trade(symbol, id)

    async def all_market_rolling_window_ticker(
        self,
        window_size: Union[AllMarketRollingWindowTickerWindowSizeEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                All Market Rolling Window Statistics Streams

                Rolling window ticker statistics for all market symbols, computed over
        multiple windows.

        Note that only tickers that have changed will be present in the array.

        Update Speed: 1000ms

                Args:
                    window_size (Union[AllMarketRollingWindowTickerWindowSizeEnum, None]):
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.all_market_rolling_window_ticker(window_size, id)

    async def all_mini_ticker(
        self,
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                All Market Mini Tickers Stream

                24hr rolling window mini-ticker statistics for all symbols that changed
        in an array. These are NOT the statistics of the UTC day, but a 24hr
        rolling window for the previous 24hrs. Note that only tickers that have
        changed will be present in the array.

        Update Speed: 1000ms

                Args:
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.all_mini_ticker(id)

    async def avg_price(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                Average Price

                Average price streams push changes in the average price over a fixed time interval.

        Update Speed: 1000ms

                Args:
                    symbol (Union[str, None]): Symbol to query
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.avg_price(symbol, id)

    async def block_trade(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                Block Trade Streams

                Block Trade Streams push block trade information in real-time.

        Update Speed: Real-time

                Args:
                    symbol (Union[str, None]): Symbol to query
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.block_trade(symbol, id)

    async def book_ticker(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                Individual Symbol Book Ticker Streams

                Pushes any update to the best bid or ask's price or quantity in
        real-time for a specified symbol.

        Multiple `<symbol>@bookTicker` streams can be subscribed to over one
        connection.

        Update Speed: Real-time

                Args:
                    symbol (Union[str, None]): Symbol to query
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.book_ticker(symbol, id)

    async def diff_book_depth(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
        update_speed: Optional[DiffBookDepthUpdateSpeedEnum] = None,
    ) -> RequestStreamHandle:
        r"""
                Diff. Depth Stream

                Order book price and quantity depth updates used to locally manage an order book.

        Update Speed: 1000ms or 100ms

                Args:
                    symbol (Union[str, None]): Symbol to query
                    id (Optional[str] = None): Unique WebSocket request ID.
                    update_speed (Optional[DiffBookDepthUpdateSpeedEnum] = None): Optional stream update speed suffix

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.diff_book_depth(symbol, id, update_speed)

    async def kline(
        self,
        symbol: Union[str, None],
        interval: Union[KlineIntervalEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                Kline/Candlestick Streams for UTC

                The Kline/Candlestick Stream push updates to the current
        klines/candlestick every second in `UTC+0` timezone

        Update Speed: 1000ms for `1s`, 2000ms for the other intervals

                Args:
                    symbol (Union[str, None]): Symbol to query
                    interval (Union[KlineIntervalEnum, None]):
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.kline(symbol, interval, id)

    async def kline_offset(
        self,
        symbol: Union[str, None],
        interval: Union[KlineOffsetIntervalEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                Kline/Candlestick Streams with timezone offset

                The Kline/Candlestick Stream push updates to the current
        klines/candlestick every second in `UTC+8` timezone

        **Kline/Candlestick chart intervals:**

        Supported intervals: See Kline/Candlestick chart intervals

        **UTC+8 timezone offset:**
          - Kline intervals open and close in the UTC+8 timezone. For example the 1d klines will open at the beginning of the UTC+8 day, and close at the end of the UTC+8 day.
          - Note that E (event time), t (start time) and T (close time) in the payload are Unix timestamps, which are always interpreted in UTC.

        Update Speed: 1000ms for `1s`, 2000ms for the other intervals

                Args:
                    symbol (Union[str, None]): Symbol to query
                    interval (Union[KlineOffsetIntervalEnum, None]):
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.kline_offset(symbol, interval, id)

    async def mini_ticker(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                Individual Symbol Mini Ticker Stream

                24hr rolling window mini-ticker statistics. These are NOT the statistics
        of the UTC day, but a 24hr rolling window for the previous 24hrs.

        Update Speed: 1000ms

                Args:
                    symbol (Union[str, None]): Symbol to query
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.mini_ticker(symbol, id)

    async def partial_book_depth(
        self,
        symbol: Union[str, None],
        levels: Union[PartialBookDepthLevelsEnum, None],
        id: Optional[str] = None,
        update_speed: Optional[PartialBookDepthUpdateSpeedEnum] = None,
    ) -> RequestStreamHandle:
        r"""
                WebSocket Partial Book Depth Streams

                Top **\<levels\>** bids and asks, pushed every second.

        Update Speed: 1000ms or 100ms

                Args:
                    symbol (Union[str, None]): Symbol to query
                    levels (Union[PartialBookDepthLevelsEnum, None]):
                    id (Optional[str] = None): Unique WebSocket request ID.
                    update_speed (Optional[PartialBookDepthUpdateSpeedEnum] = None): Optional stream update speed suffix

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.partial_book_depth(
            symbol, levels, id, update_speed
        )

    async def reference_price(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                Reference Price Streams

                Reference price stream for a symbol.

        Update Speed: 1000ms

                Args:
                    symbol (Union[str, None]): Symbol to query
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.reference_price(symbol, id)

    async def rolling_window_ticker(
        self,
        symbol: Union[str, None],
        window_size: Union[RollingWindowTickerWindowSizeEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                Individual Symbol Rolling Window Statistics Streams

                Rolling window ticker statistics for a single symbol, computed over
        multiple windows.

        **Note:** This stream is different from the `<symbol>@ticker` stream. The open time `"O"` always starts on a minute, while the closing time `"C"` is the current time
        of the update. As such, the effective window might be up to 59999ms wider than `<window_size>`.

        Update Speed: 1000ms

                Args:
                    symbol (Union[str, None]): Symbol to query
                    window_size (Union[RollingWindowTickerWindowSizeEnum, None]):
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.rolling_window_ticker(symbol, window_size, id)

    async def ticker(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                Individual Symbol Ticker Streams

                24hr rolling window ticker statistics for a single symbol. These are NOT
        the statistics of the UTC day, but a 24hr rolling window for the
        previous 24hrs.

        Update Speed: 1000ms

                Args:
                    symbol (Union[str, None]): Symbol to query
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.ticker(symbol, id)

    async def trade(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
                Trade Streams

                The Trade Streams push raw trade information; each trade has a unique
        buyer and seller.

        Update Speed: Real-time

                Args:
                    symbol (Union[str, None]): Symbol to query
                    id (Optional[str] = None): Unique WebSocket request ID.

                Returns:
                    RequestStreamHandle

                Raises:
                    RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.trade(symbol, id)
