"""
Binance Web3 Wallet REST API

Cross-chain wallet, market, trading, and transaction APIs for the Binance Web3 API platform.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_web3_wallet.rest_api.api import RWADataApi
from binance_sdk_web3_wallet.rest_api.models import GetRwaTokenIssuancePlatformsResponse
from binance_sdk_web3_wallet.rest_api.models import GetRwaTokenListResponse
from binance_sdk_web3_wallet.rest_api.models import GetRwaTokenPriceResponse
from binance_sdk_web3_wallet.rest_api.models import GetRwaUnderlyingInfoResponse
from binance_sdk_web3_wallet.rest_api.models import GetRwaUnderlyingMarketDataResponse
from binance_sdk_web3_wallet.rest_api.models import SearchRwaTokenResponse


from binance_sdk_web3_wallet.rest_api.models import (
    GetRwaTokenIssuancePlatformsPlatformIdEnum,
)


from binance_sdk_web3_wallet.rest_api.models import GetRwaTokenListPlatformIdEnum


from binance_sdk_web3_wallet.rest_api.models import SearchRwaTokenPlatformIdEnum


class TestRWADataApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = RWADataApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    @patch("binance_common.utils.web3_signature")
    def test_get_rwa_token_issuance_platforms_success(self, mock_get_signature):
        """Test get_rwa_token_issuance_platforms() successfully with required parameters only."""

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "platformId": "ondo",
                    "tickerCount": 264,
                    "chainDistribution": [{"binanceChainId": "56", "tokenCount": 264}],
                    "website": "https://ondo.finance",
                    "logoUrl": "https://static.onchainos.com/images/protocols/ondo.png",
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_rwa_token_issuance_platforms()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/platforms" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRwaTokenIssuancePlatformsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetRwaTokenIssuancePlatformsResponse, "from_dict")
        ):
            expected = GetRwaTokenIssuancePlatformsResponse.from_dict(expected_response)
        else:
            expected = GetRwaTokenIssuancePlatformsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.web3_signature")
    def test_get_rwa_token_issuance_platforms_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_rwa_token_issuance_platforms() successfully with optional parameters."""

        params = {
            "recv_window": 5000,
            "nonce": "unique-nonce-string",
            "platform_id": GetRwaTokenIssuancePlatformsPlatformIdEnum["ondo"].value,
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "platformId": "ondo",
                    "tickerCount": 264,
                    "chainDistribution": [{"binanceChainId": "56", "tokenCount": 264}],
                    "website": "https://ondo.finance",
                    "logoUrl": "https://static.onchainos.com/images/protocols/ondo.png",
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_rwa_token_issuance_platforms(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/platforms" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRwaTokenIssuancePlatformsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetRwaTokenIssuancePlatformsResponse, "from_dict")
        ):
            expected = GetRwaTokenIssuancePlatformsResponse.from_dict(expected_response)
        else:
            expected = GetRwaTokenIssuancePlatformsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_rwa_token_issuance_platforms_server_error(self):
        """Test that get_rwa_token_issuance_platforms() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_rwa_token_issuance_platforms = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_rwa_token_issuance_platforms()

    @patch("binance_common.utils.web3_signature")
    def test_get_rwa_token_list_success(self, mock_get_signature):
        """Test get_rwa_token_list() successfully with required parameters only."""

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "binanceChainId": "56",
                    "tokenContractAddress": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
                    "platformId": "ondo",
                    "assetType": 1,
                    "tokenName": "SolarEdge Technologies (Ondo Tokenized)",
                    "tokenSymbol": "SEDGon",
                    "tokenLogoUrl": "https://static.onchainos.com/images/web3-data/public/token/logos/7ac70d12b753405fb9ec408f1d1ab81e.png",
                    "decimals": "18",
                    "underlyingTicker": "SEDG",
                    "underlyingName": "NVIDIA Corporation",
                    "underlyingNameZh": "英伟达",
                    "tokenToShareRatio": "1",
                    "tags": ["alpha"],
                    "statusInfo": {
                        "openState": False,
                        "marketStatus": "closed",
                        "reasonCode": "MARKET_CLOSED",
                        "reasonMsg": "Weekend or Holiday",
                        "nextOpenTime": 1779111060000,
                        "nextCloseTime": 1,
                    },
                    "tokenPrice": "61.746364",
                    "referencePrice": "61.746364",
                    "volume24H": "894138582.57",
                    "marketCap": "3055506468",
                    "peRatioTTM": "peRatioTTM",
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_rwa_token_list()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/tokens" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRwaTokenListResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetRwaTokenListResponse, "from_dict"):
            expected = GetRwaTokenListResponse.from_dict(expected_response)
        else:
            expected = GetRwaTokenListResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.web3_signature")
    def test_get_rwa_token_list_success_with_optional_params(self, mock_get_signature):
        """Test get_rwa_token_list() successfully with optional parameters."""

        params = {
            "recv_window": 5000,
            "nonce": "unique-nonce-string",
            "binance_chain_id": "56",
            "platform_id": GetRwaTokenListPlatformIdEnum["ondo"].value,
            "tab_id": 9,
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "binanceChainId": "56",
                    "tokenContractAddress": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
                    "platformId": "ondo",
                    "assetType": 1,
                    "tokenName": "SolarEdge Technologies (Ondo Tokenized)",
                    "tokenSymbol": "SEDGon",
                    "tokenLogoUrl": "https://static.onchainos.com/images/web3-data/public/token/logos/7ac70d12b753405fb9ec408f1d1ab81e.png",
                    "decimals": "18",
                    "underlyingTicker": "SEDG",
                    "underlyingName": "NVIDIA Corporation",
                    "underlyingNameZh": "英伟达",
                    "tokenToShareRatio": "1",
                    "tags": ["alpha"],
                    "statusInfo": {
                        "openState": False,
                        "marketStatus": "closed",
                        "reasonCode": "MARKET_CLOSED",
                        "reasonMsg": "Weekend or Holiday",
                        "nextOpenTime": 1779111060000,
                        "nextCloseTime": 1,
                    },
                    "tokenPrice": "61.746364",
                    "referencePrice": "61.746364",
                    "volume24H": "894138582.57",
                    "marketCap": "3055506468",
                    "peRatioTTM": "peRatioTTM",
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_rwa_token_list(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/tokens" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRwaTokenListResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetRwaTokenListResponse, "from_dict"):
            expected = GetRwaTokenListResponse.from_dict(expected_response)
        else:
            expected = GetRwaTokenListResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_rwa_token_list_server_error(self):
        """Test that get_rwa_token_list() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_rwa_token_list = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_rwa_token_list()

    @patch("binance_common.utils.web3_signature")
    def test_get_rwa_token_price_success(self, mock_get_signature):
        """Test get_rwa_token_price() successfully with required parameters only."""

        params = {
            "binance_chain_id": "56",
            "token_contract_addresses": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "binanceChainId": "56",
                    "tokenContractAddress": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
                    "platformId": "ondo",
                    "tokenPrice": "61.746364",
                    "referencePrice": "61.746364",
                    "tokenPriceUpdatedAt": 1748000000000,
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_rwa_token_price(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/price" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["binanceChainId"] == "56"
        assert (
            normalized["tokenContractAddresses"]
            == "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe"
        )

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRwaTokenPriceResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetRwaTokenPriceResponse, "from_dict"):
            expected = GetRwaTokenPriceResponse.from_dict(expected_response)
        else:
            expected = GetRwaTokenPriceResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.web3_signature")
    def test_get_rwa_token_price_success_with_optional_params(self, mock_get_signature):
        """Test get_rwa_token_price() successfully with optional parameters."""

        params = {
            "binance_chain_id": "56",
            "token_contract_addresses": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
            "recv_window": 5000,
            "nonce": "unique-nonce-string",
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "binanceChainId": "56",
                    "tokenContractAddress": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
                    "platformId": "ondo",
                    "tokenPrice": "61.746364",
                    "referencePrice": "61.746364",
                    "tokenPriceUpdatedAt": 1748000000000,
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_rwa_token_price(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/price" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRwaTokenPriceResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetRwaTokenPriceResponse, "from_dict"):
            expected = GetRwaTokenPriceResponse.from_dict(expected_response)
        else:
            expected = GetRwaTokenPriceResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_rwa_token_price_missing_required_param_binance_chain_id(self):
        """Test that get_rwa_token_price() raises RequiredError when 'binance_chain_id' is missing."""
        params = {
            "binance_chain_id": "56",
            "token_contract_addresses": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }
        params["binance_chain_id"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'binance_chain_id'"
        ):
            self.client.get_rwa_token_price(**params)

    def test_get_rwa_token_price_missing_required_param_token_contract_addresses(self):
        """Test that get_rwa_token_price() raises RequiredError when 'token_contract_addresses' is missing."""
        params = {
            "binance_chain_id": "56",
            "token_contract_addresses": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }
        params["token_contract_addresses"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'token_contract_addresses'"
        ):
            self.client.get_rwa_token_price(**params)

    def test_get_rwa_token_price_server_error(self):
        """Test that get_rwa_token_price() raises an error when the server returns an error."""

        params = {
            "binance_chain_id": "56",
            "token_contract_addresses": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }

        mock_error = Exception("ResponseError")
        self.client.get_rwa_token_price = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_rwa_token_price(**params)

    @patch("binance_common.utils.web3_signature")
    def test_get_rwa_underlying_info_success(self, mock_get_signature):
        """Test get_rwa_underlying_info() successfully with required parameters only."""

        params = {
            "binance_chain_id": "56",
            "token_contract_address": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": {
                "binanceChainId": "56",
                "tokenContractAddress": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
                "platformId": "ondo",
                "underlyingTicker": "SEDG",
                "underlyingFullName": "SolarEdge Technologies, Inc.",
                "assetType": 1,
                "tokenToShareRatio": "1.003701",
                "companyInfo": {
                    "ceo": "ceo",
                    "website": "https://www.solaredge.com",
                    "industry": "Technology",
                    "conceptsEn": [],
                    "conceptsCn": [],
                    "descriptionEn": "SolarEdge Technologies, Inc. is an energy technology company...",
                    "descriptionZh": "SolarEdge是一家能源科技企业...",
                },
            },
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_rwa_underlying_info(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/underlying-profile" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["binanceChainId"] == "56"
        assert (
            normalized["tokenContractAddress"]
            == "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe"
        )

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRwaUnderlyingInfoResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetRwaUnderlyingInfoResponse, "from_dict"):
            expected = GetRwaUnderlyingInfoResponse.from_dict(expected_response)
        else:
            expected = GetRwaUnderlyingInfoResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.web3_signature")
    def test_get_rwa_underlying_info_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_rwa_underlying_info() successfully with optional parameters."""

        params = {
            "binance_chain_id": "56",
            "token_contract_address": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
            "recv_window": 5000,
            "nonce": "unique-nonce-string",
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": {
                "binanceChainId": "56",
                "tokenContractAddress": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
                "platformId": "ondo",
                "underlyingTicker": "SEDG",
                "underlyingFullName": "SolarEdge Technologies, Inc.",
                "assetType": 1,
                "tokenToShareRatio": "1.003701",
                "companyInfo": {
                    "ceo": "ceo",
                    "website": "https://www.solaredge.com",
                    "industry": "Technology",
                    "conceptsEn": [],
                    "conceptsCn": [],
                    "descriptionEn": "SolarEdge Technologies, Inc. is an energy technology company...",
                    "descriptionZh": "SolarEdge是一家能源科技企业...",
                },
            },
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_rwa_underlying_info(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/underlying-profile" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRwaUnderlyingInfoResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetRwaUnderlyingInfoResponse, "from_dict"):
            expected = GetRwaUnderlyingInfoResponse.from_dict(expected_response)
        else:
            expected = GetRwaUnderlyingInfoResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_rwa_underlying_info_missing_required_param_binance_chain_id(self):
        """Test that get_rwa_underlying_info() raises RequiredError when 'binance_chain_id' is missing."""
        params = {
            "binance_chain_id": "56",
            "token_contract_address": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }
        params["binance_chain_id"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'binance_chain_id'"
        ):
            self.client.get_rwa_underlying_info(**params)

    def test_get_rwa_underlying_info_missing_required_param_token_contract_address(
        self,
    ):
        """Test that get_rwa_underlying_info() raises RequiredError when 'token_contract_address' is missing."""
        params = {
            "binance_chain_id": "56",
            "token_contract_address": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }
        params["token_contract_address"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'token_contract_address'"
        ):
            self.client.get_rwa_underlying_info(**params)

    def test_get_rwa_underlying_info_server_error(self):
        """Test that get_rwa_underlying_info() raises an error when the server returns an error."""

        params = {
            "binance_chain_id": "56",
            "token_contract_address": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }

        mock_error = Exception("ResponseError")
        self.client.get_rwa_underlying_info = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_rwa_underlying_info(**params)

    @patch("binance_common.utils.web3_signature")
    def test_get_rwa_underlying_market_data_success(self, mock_get_signature):
        """Test get_rwa_underlying_market_data() successfully with required parameters only."""

        params = {
            "binance_chain_id": "56",
            "token_contract_address": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": {
                "binanceChainId": "56",
                "tokenContractAddress": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
                "platformId": "ondo",
                "assetType": 1,
                "statusInfo": {
                    "openState": True,
                    "marketStatus": "regular",
                    "reasonCode": "reasonCode",
                    "reasonMsg": "reasonMsg",
                    "nextOpenTime": 1,
                    "nextCloseTime": 1779134340000,
                },
                "marketData": {
                    "referencePrice": "61.746364",
                    "high52W": "63.53",
                    "low52W": "13.73",
                    "volumeShares24H": "14480829",
                    "avgDailyVolume1Y": "4243226",
                    "totalShares": "60818202",
                    "marketCap": "3055506468",
                    "turnoverRate": "23.81",
                    "amplitude": "3.05",
                    "peRatioTTM": "28.5",
                    "pbRatio": "7.15",
                    "dividendYield": "0.85",
                    "latestDividend": "0.25",
                },
            },
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_rwa_underlying_market_data(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/underlying-market" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["binanceChainId"] == "56"
        assert (
            normalized["tokenContractAddress"]
            == "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe"
        )

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRwaUnderlyingMarketDataResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetRwaUnderlyingMarketDataResponse, "from_dict")
        ):
            expected = GetRwaUnderlyingMarketDataResponse.from_dict(expected_response)
        else:
            expected = GetRwaUnderlyingMarketDataResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.web3_signature")
    def test_get_rwa_underlying_market_data_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_rwa_underlying_market_data() successfully with optional parameters."""

        params = {
            "binance_chain_id": "56",
            "token_contract_address": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
            "recv_window": 5000,
            "nonce": "unique-nonce-string",
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": {
                "binanceChainId": "56",
                "tokenContractAddress": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
                "platformId": "ondo",
                "assetType": 1,
                "statusInfo": {
                    "openState": True,
                    "marketStatus": "regular",
                    "reasonCode": "reasonCode",
                    "reasonMsg": "reasonMsg",
                    "nextOpenTime": 1,
                    "nextCloseTime": 1779134340000,
                },
                "marketData": {
                    "referencePrice": "61.746364",
                    "high52W": "63.53",
                    "low52W": "13.73",
                    "volumeShares24H": "14480829",
                    "avgDailyVolume1Y": "4243226",
                    "totalShares": "60818202",
                    "marketCap": "3055506468",
                    "turnoverRate": "23.81",
                    "amplitude": "3.05",
                    "peRatioTTM": "28.5",
                    "pbRatio": "7.15",
                    "dividendYield": "0.85",
                    "latestDividend": "0.25",
                },
            },
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_rwa_underlying_market_data(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/underlying-market" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetRwaUnderlyingMarketDataResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetRwaUnderlyingMarketDataResponse, "from_dict")
        ):
            expected = GetRwaUnderlyingMarketDataResponse.from_dict(expected_response)
        else:
            expected = GetRwaUnderlyingMarketDataResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_rwa_underlying_market_data_missing_required_param_binance_chain_id(
        self,
    ):
        """Test that get_rwa_underlying_market_data() raises RequiredError when 'binance_chain_id' is missing."""
        params = {
            "binance_chain_id": "56",
            "token_contract_address": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }
        params["binance_chain_id"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'binance_chain_id'"
        ):
            self.client.get_rwa_underlying_market_data(**params)

    def test_get_rwa_underlying_market_data_missing_required_param_token_contract_address(
        self,
    ):
        """Test that get_rwa_underlying_market_data() raises RequiredError when 'token_contract_address' is missing."""
        params = {
            "binance_chain_id": "56",
            "token_contract_address": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }
        params["token_contract_address"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'token_contract_address'"
        ):
            self.client.get_rwa_underlying_market_data(**params)

    def test_get_rwa_underlying_market_data_server_error(self):
        """Test that get_rwa_underlying_market_data() raises an error when the server returns an error."""

        params = {
            "binance_chain_id": "56",
            "token_contract_address": "0x8755c5c39b1aa9053a83ac731242a2cf4d04b0fe",
        }

        mock_error = Exception("ResponseError")
        self.client.get_rwa_underlying_market_data = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_rwa_underlying_market_data(**params)

    @patch("binance_common.utils.web3_signature")
    def test_search_rwa_token_success(self, mock_get_signature):
        """Test search_rwa_token() successfully with required parameters only."""

        params = {
            "keyword": "NVDA",
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "ticker": "NVDA",
                    "companyName": "NVIDIA Corporation",
                    "assets": [
                        {
                            "platformId": "ondo",
                            "binanceChainId": "56",
                            "tokenContractAddress": "0xa9ee28c80f960b889dfbd1902055218cba016f75",
                            "tokenSymbol": "NVDAon",
                            "assetType": 1,
                        }
                    ],
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.search_rwa_token(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/search" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["keyword"] == "NVDA"

        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(SearchRwaTokenResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(SearchRwaTokenResponse, "from_dict"):
            expected = SearchRwaTokenResponse.from_dict(expected_response)
        else:
            expected = SearchRwaTokenResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.web3_signature")
    def test_search_rwa_token_success_with_optional_params(self, mock_get_signature):
        """Test search_rwa_token() successfully with optional parameters."""

        params = {
            "keyword": "NVDA",
            "recv_window": 5000,
            "nonce": "unique-nonce-string",
            "platform_id": SearchRwaTokenPlatformIdEnum["ondo"].value,
        }

        expected_response = {
            "code": 0,
            "msg": "success",
            "data": [
                {
                    "ticker": "NVDA",
                    "companyName": "NVIDIA Corporation",
                    "assets": [
                        {
                            "platformId": "ondo",
                            "binanceChainId": "56",
                            "tokenContractAddress": "0xa9ee28c80f960b889dfbd1902055218cba016f75",
                            "tokenSymbol": "NVDAon",
                            "assetType": 1,
                        }
                    ],
                }
            ],
            "timestamp": 1748601600000,
            "success": True,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.search_rwa_token(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/api/v1/dex/market/rwa/search" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None

        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(SearchRwaTokenResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(SearchRwaTokenResponse, "from_dict"):
            expected = SearchRwaTokenResponse.from_dict(expected_response)
        else:
            expected = SearchRwaTokenResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_search_rwa_token_missing_required_param_keyword(self):
        """Test that search_rwa_token() raises RequiredError when 'keyword' is missing."""
        params = {
            "keyword": "NVDA",
        }
        params["keyword"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'keyword'"):
            self.client.search_rwa_token(**params)

    def test_search_rwa_token_server_error(self):
        """Test that search_rwa_token() raises an error when the server returns an error."""

        params = {
            "keyword": "NVDA",
        }

        mock_error = Exception("ResponseError")
        self.client.search_rwa_token = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.search_rwa_token(**params)
