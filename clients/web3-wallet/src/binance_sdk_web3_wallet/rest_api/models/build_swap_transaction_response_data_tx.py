# coding: utf-8

"""
Binance Web3 Wallet REST API

Cross-chain wallet, market, trading, and transaction APIs for the Binance Web3 API platform.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Set
from typing_extensions import Self


class BuildSwapTransactionResponseDataTx(BaseModel):
    """
    On-chain transaction payload to sign and submit.
    """  # noqa: E501

    var_from: Optional[StrictStr] = Field(
        default=None,
        description="User wallet address (transaction sender).",
        alias="from",
    )
    to: Optional[StrictStr] = Field(
        default=None,
        description="DEX router contract address (the transaction's `to` field).",
    )
    data: Optional[StrictStr] = Field(default=None, description="ABI-encoded calldata.")
    value: Optional[StrictStr] = Field(
        default=None,
        description='Native-token amount sent with the transaction (wei). "0" for ERC-20 swaps.',
    )
    gas: Optional[StrictStr] = Field(default=None, description="Gas limit estimate.")
    gas_price: Optional[StrictStr] = Field(
        default=None, description="Gas price (wei).", alias="gasPrice"
    )
    max_priority_fee_per_gas: Optional[StrictStr] = Field(
        default=None,
        description="EIP-1559 max priority fee per gas (wei).",
        alias="maxPriorityFeePerGas",
    )
    min_receive_amount: Optional[StrictStr] = Field(
        default=None,
        description="Minimum buy-token amount the user accepts at the slippage limit (smallest unit, integer string).",
        alias="minReceiveAmount",
    )
    slippage_percent: Optional[StrictStr] = Field(
        default=None,
        description="Slippage value applied to this transaction (percentage string).",
        alias="slippagePercent",
    )
    signature_data: Optional[List[StrictStr]] = Field(
        default=None,
        description="Auxiliary signature data. When `approveTransaction=true` was passed, this contains the spender address and approve calldata.",
        alias="signatureData",
    )
    compute_unit_price: Optional[StrictStr] = Field(
        default=None,
        description="Solana priority fee per compute unit (micro-lamports). Echoed when supplied; computed by the platform otherwise. Null on EVM chains.",
        alias="computeUnitPrice",
    )
    compute_unit_limit: Optional[StrictStr] = Field(
        default=None,
        description="Solana compute-unit ceiling. Echoed when supplied; not returned otherwise. Null on EVM chains.",
        alias="computeUnitLimit",
    )
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = [
        "from",
        "to",
        "data",
        "value",
        "gas",
        "gasPrice",
        "maxPriorityFeePerGas",
        "minReceiveAmount",
        "slippagePercent",
        "signatureData",
        "computeUnitPrice",
        "computeUnitLimit",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def is_array(cls) -> bool:
        return False

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BuildSwapTransactionResponseDataTx from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set(
            [
                "additional_properties",
            ]
        )

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if compute_unit_price (nullable) is None
        # and model_fields_set contains the field
        if (
            self.compute_unit_price is None
            and "compute_unit_price" in self.model_fields_set
        ):
            _dict["computeUnitPrice"] = None

        # set to None if compute_unit_limit (nullable) is None
        # and model_fields_set contains the field
        if (
            self.compute_unit_limit is None
            and "compute_unit_limit" in self.model_fields_set
        ):
            _dict["computeUnitLimit"] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BuildSwapTransactionResponseDataTx from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "from": obj.get("from"),
                "to": obj.get("to"),
                "data": obj.get("data"),
                "value": obj.get("value"),
                "gas": obj.get("gas"),
                "gasPrice": obj.get("gasPrice"),
                "maxPriorityFeePerGas": obj.get("maxPriorityFeePerGas"),
                "minReceiveAmount": obj.get("minReceiveAmount"),
                "slippagePercent": obj.get("slippagePercent"),
                "signatureData": obj.get("signatureData"),
                "computeUnitPrice": obj.get("computeUnitPrice"),
                "computeUnitLimit": obj.get("computeUnitLimit"),
            }
        )
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj
