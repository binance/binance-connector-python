"""
Binance Wallet REST API

OpenAPI Specification for the Binance Wallet REST API
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock

from binance_common.configuration import ConfigurationRestAPI
from binance_common.utils import is_one_of_model

from binance_wallet.rest_api.api import OthersApi
from binance_wallet.rest_api.models import GetSymbolsDelistScheduleForSpotResponse
from binance_wallet.rest_api.models import SystemStatusResponse


class TestOthersApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = OthersApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    def test_get_symbols_delist_schedule_for_spot_success(self):
        """Test get_symbols_delist_schedule_for_spot() successfully with required parameters only."""

        expected_response = [
            {"delistTime": 1686161202000, "symbols": ["ADAUSDT", "BNBUSDT"]},
            {"delistTime": 1686222232000, "symbols": ["ETHUSDT"]},
        ]

        self.set_mock_response(expected_response)

        response = self.client.get_symbols_delist_schedule_for_spot()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert "/sapi/v1/spot/delist-schedule" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetSymbolsDelistScheduleForSpotResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list:
            expected = GetSymbolsDelistScheduleForSpotResponse.from_dict(
                expected_response
            )
        else:
            expected = GetSymbolsDelistScheduleForSpotResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_symbols_delist_schedule_for_spot_success_with_optional_params(self):
        """Test get_symbols_delist_schedule_for_spot() successfully with optional parameters."""

        params = {"recv_window": 5000}

        expected_response = [
            {"delistTime": 1686161202000, "symbols": ["ADAUSDT", "BNBUSDT"]},
            {"delistTime": 1686222232000, "symbols": ["ETHUSDT"]},
        ]

        self.set_mock_response(expected_response)

        response = self.client.get_symbols_delist_schedule_for_spot(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/sapi/v1/spot/delist-schedule" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetSymbolsDelistScheduleForSpotResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list:
            expected = GetSymbolsDelistScheduleForSpotResponse.from_dict(
                expected_response
            )
        else:
            expected = GetSymbolsDelistScheduleForSpotResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_symbols_delist_schedule_for_spot_server_error(self):
        """Test that get_symbols_delist_schedule_for_spot() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_symbols_delist_schedule_for_spot = MagicMock(
            side_effect=mock_error
        )

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_symbols_delist_schedule_for_spot()

    def test_system_status_success(self):
        """Test system_status() successfully with required parameters only."""

        expected_response = {"status": 0, "msg": "normal"}

        self.set_mock_response(expected_response)

        response = self.client.system_status()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert "/sapi/v1/system/status" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(SystemStatusResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list:
            expected = SystemStatusResponse.from_dict(expected_response)
        else:
            expected = SystemStatusResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_system_status_server_error(self):
        """Test that system_status() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.system_status = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.system_status()
