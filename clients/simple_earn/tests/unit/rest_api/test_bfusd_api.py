"""
Binance Simple Earn REST API

OpenAPI Specification for the Binance Simple Earn REST API
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock, patch
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_simple_earn.rest_api.api import BfusdApi
from binance_sdk_simple_earn.rest_api.models import GetBfusdAccountResponse
from binance_sdk_simple_earn.rest_api.models import GetBfusdQuotaDetailsResponse
from binance_sdk_simple_earn.rest_api.models import GetBfusdRateHistoryResponse
from binance_sdk_simple_earn.rest_api.models import GetBfusdRedemptionHistoryResponse
from binance_sdk_simple_earn.rest_api.models import GetBfusdRewardsHistoryResponse
from binance_sdk_simple_earn.rest_api.models import GetBfusdSubscriptionHistoryResponse
from binance_sdk_simple_earn.rest_api.models import RedeemBfusdResponse
from binance_sdk_simple_earn.rest_api.models import SubscribeBfusdResponse


class TestBfusdApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = BfusdApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_account_success(self, mock_get_signature):
        """Test get_bfusd_account() successfully with required parameters only."""

        expected_response = {
            "bfusdAmount": "100",
            "usdtProfit": "11.00",
            "bfusdProfit": "1.81",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_account()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/account" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdAccountResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetBfusdAccountResponse, "from_dict"):
            expected = GetBfusdAccountResponse.from_dict(expected_response)
        else:
            expected = GetBfusdAccountResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_account_success_with_optional_params(self, mock_get_signature):
        """Test get_bfusd_account() successfully with optional parameters."""

        params = {"recv_window": 5000}

        expected_response = {
            "bfusdAmount": "100",
            "usdtProfit": "11.00",
            "bfusdProfit": "1.81",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_account(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/account" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdAccountResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetBfusdAccountResponse, "from_dict"):
            expected = GetBfusdAccountResponse.from_dict(expected_response)
        else:
            expected = GetBfusdAccountResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_bfusd_account_server_error(self):
        """Test that get_bfusd_account() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_bfusd_account = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_bfusd_account()

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_quota_details_success(self, mock_get_signature):
        """Test get_bfusd_quota_details() successfully with required parameters only."""

        expected_response = {
            "fastRedemptionQuota": {
                "leftQuota": "2",
                "minimum": "0.1",
                "fee": "0.001",
                "freeQuota": "100",
            },
            "standardRedemptionQuota": {
                "leftQuota": "2",
                "minimum": "0.1",
                "fee": "0.0005",
                "redeemPeriod": 3,
            },
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_quota_details()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/quota" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdQuotaDetailsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetBfusdQuotaDetailsResponse, "from_dict"):
            expected = GetBfusdQuotaDetailsResponse.from_dict(expected_response)
        else:
            expected = GetBfusdQuotaDetailsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_quota_details_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_bfusd_quota_details() successfully with optional parameters."""

        params = {"recv_window": 5000}

        expected_response = {
            "fastRedemptionQuota": {
                "leftQuota": "2",
                "minimum": "0.1",
                "fee": "0.001",
                "freeQuota": "100",
            },
            "standardRedemptionQuota": {
                "leftQuota": "2",
                "minimum": "0.1",
                "fee": "0.0005",
                "redeemPeriod": 3,
            },
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_quota_details(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/quota" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdQuotaDetailsResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetBfusdQuotaDetailsResponse, "from_dict"):
            expected = GetBfusdQuotaDetailsResponse.from_dict(expected_response)
        else:
            expected = GetBfusdQuotaDetailsResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_bfusd_quota_details_server_error(self):
        """Test that get_bfusd_quota_details() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_bfusd_quota_details = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_bfusd_quota_details()

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_rate_history_success(self, mock_get_signature):
        """Test get_bfusd_rate_history() successfully with required parameters only."""

        expected_response = {
            "rows": [{"annualPercentageRate": "0.0418", "time": 1577233578000}],
            "total": "1",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_rate_history()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/history/rateHistory" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdRateHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetBfusdRateHistoryResponse, "from_dict"):
            expected = GetBfusdRateHistoryResponse.from_dict(expected_response)
        else:
            expected = GetBfusdRateHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_rate_history_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_bfusd_rate_history() successfully with optional parameters."""

        params = {
            "start_time": 1623319461670,
            "end_time": 1641782889000,
            "current": 1,
            "size": 10,
            "recv_window": 5000,
        }

        expected_response = {
            "rows": [{"annualPercentageRate": "0.0418", "time": 1577233578000}],
            "total": "1",
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_rate_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/history/rateHistory" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdRateHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetBfusdRateHistoryResponse, "from_dict"):
            expected = GetBfusdRateHistoryResponse.from_dict(expected_response)
        else:
            expected = GetBfusdRateHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_bfusd_rate_history_server_error(self):
        """Test that get_bfusd_rate_history() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_bfusd_rate_history = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_bfusd_rate_history()

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_redemption_history_success(self, mock_get_signature):
        """Test get_bfusd_redemption_history() successfully with required parameters only."""

        expected_response = {
            "rows": [
                {
                    "time": 1575018510000,
                    "asset": "BFUSD",
                    "amount": "51",
                    "receiveAsset": "USDT",
                    "receiveAmount": "50",
                    "fee": "1",
                    "arrivalTime": 1575018510000,
                    "status": "SUCCESS",
                }
            ],
            "total": 1,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_redemption_history()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/history/redemptionHistory" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdRedemptionHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetBfusdRedemptionHistoryResponse, "from_dict")
        ):
            expected = GetBfusdRedemptionHistoryResponse.from_dict(expected_response)
        else:
            expected = GetBfusdRedemptionHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_redemption_history_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_bfusd_redemption_history() successfully with optional parameters."""

        params = {
            "start_time": 1623319461670,
            "end_time": 1641782889000,
            "current": 1,
            "size": 10,
            "recv_window": 5000,
        }

        expected_response = {
            "rows": [
                {
                    "time": 1575018510000,
                    "asset": "BFUSD",
                    "amount": "51",
                    "receiveAsset": "USDT",
                    "receiveAmount": "50",
                    "fee": "1",
                    "arrivalTime": 1575018510000,
                    "status": "SUCCESS",
                }
            ],
            "total": 1,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_redemption_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/history/redemptionHistory" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdRedemptionHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetBfusdRedemptionHistoryResponse, "from_dict")
        ):
            expected = GetBfusdRedemptionHistoryResponse.from_dict(expected_response)
        else:
            expected = GetBfusdRedemptionHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_bfusd_redemption_history_server_error(self):
        """Test that get_bfusd_redemption_history() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_bfusd_redemption_history = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_bfusd_redemption_history()

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_rewards_history_success(self, mock_get_signature):
        """Test get_bfusd_rewards_history() successfully with required parameters only."""

        expected_response = {
            "rows": [
                {
                    "time": 1575018510000,
                    "rewardAsset": "BFUSD",
                    "rewardsAmount": "1",
                    "BFUSDPosition": "100",
                    "annualPercentageRate": "0.0418",
                }
            ],
            "total": 1,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_rewards_history()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/history/rewardsHistory" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdRewardsHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof or is_list or hasattr(GetBfusdRewardsHistoryResponse, "from_dict")
        ):
            expected = GetBfusdRewardsHistoryResponse.from_dict(expected_response)
        else:
            expected = GetBfusdRewardsHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_rewards_history_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_bfusd_rewards_history() successfully with optional parameters."""

        params = {
            "start_time": 1623319461670,
            "end_time": 1641782889000,
            "current": 1,
            "size": 10,
            "recv_window": 5000,
        }

        expected_response = {
            "rows": [
                {
                    "time": 1575018510000,
                    "rewardAsset": "BFUSD",
                    "rewardsAmount": "1",
                    "BFUSDPosition": "100",
                    "annualPercentageRate": "0.0418",
                }
            ],
            "total": 1,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_rewards_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/history/rewardsHistory" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdRewardsHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof or is_list or hasattr(GetBfusdRewardsHistoryResponse, "from_dict")
        ):
            expected = GetBfusdRewardsHistoryResponse.from_dict(expected_response)
        else:
            expected = GetBfusdRewardsHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_bfusd_rewards_history_server_error(self):
        """Test that get_bfusd_rewards_history() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_bfusd_rewards_history = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_bfusd_rewards_history()

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_subscription_history_success(self, mock_get_signature):
        """Test get_bfusd_subscription_history() successfully with required parameters only."""

        expected_response = {
            "rows": [
                {
                    "time": 1575018510000,
                    "asset": "USDT",
                    "amount": "100",
                    "receiveAsset": "BFUSD",
                    "receiveAmount": "100",
                    "status": "SUCCESS",
                }
            ],
            "total": 1,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_subscription_history()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/history/subscriptionHistory" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdSubscriptionHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetBfusdSubscriptionHistoryResponse, "from_dict")
        ):
            expected = GetBfusdSubscriptionHistoryResponse.from_dict(expected_response)
        else:
            expected = GetBfusdSubscriptionHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_get_bfusd_subscription_history_success_with_optional_params(
        self, mock_get_signature
    ):
        """Test get_bfusd_subscription_history() successfully with optional parameters."""

        params = {
            "asset": "asset_example",
            "start_time": 1623319461670,
            "end_time": 1641782889000,
            "current": 1,
            "size": 10,
            "recv_window": 5000,
        }

        expected_response = {
            "rows": [
                {
                    "time": 1575018510000,
                    "asset": "USDT",
                    "amount": "100",
                    "receiveAsset": "BFUSD",
                    "receiveAmount": "100",
                    "status": "SUCCESS",
                }
            ],
            "total": 1,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.get_bfusd_subscription_history(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/history/subscriptionHistory" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetBfusdSubscriptionHistoryResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif (
            is_oneof
            or is_list
            or hasattr(GetBfusdSubscriptionHistoryResponse, "from_dict")
        ):
            expected = GetBfusdSubscriptionHistoryResponse.from_dict(expected_response)
        else:
            expected = GetBfusdSubscriptionHistoryResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_bfusd_subscription_history_server_error(self):
        """Test that get_bfusd_subscription_history() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_bfusd_subscription_history = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_bfusd_subscription_history()

    @patch("binance_common.utils.get_signature")
    def test_redeem_bfusd_success(self, mock_get_signature):
        """Test redeem_bfusd() successfully with required parameters only."""

        params = {
            "amount": 1.0,
            "type": "s",
        }

        expected_response = {
            "success": True,
            "receiveAmount": "0.23092091",
            "fee": "0.00000012",
            "arrivalTime": 1575018510000,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.redeem_bfusd(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/redeem" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"
        assert normalized["amount"] == 1.0
        assert normalized["type"] == "s"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(RedeemBfusdResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(RedeemBfusdResponse, "from_dict"):
            expected = RedeemBfusdResponse.from_dict(expected_response)
        else:
            expected = RedeemBfusdResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_redeem_bfusd_success_with_optional_params(self, mock_get_signature):
        """Test redeem_bfusd() successfully with optional parameters."""

        params = {"amount": 1.0, "type": "s", "recv_window": 5000}

        expected_response = {
            "success": True,
            "receiveAmount": "0.23092091",
            "fee": "0.00000012",
            "arrivalTime": 1575018510000,
        }
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.redeem_bfusd(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/redeem" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(RedeemBfusdResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(RedeemBfusdResponse, "from_dict"):
            expected = RedeemBfusdResponse.from_dict(expected_response)
        else:
            expected = RedeemBfusdResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_redeem_bfusd_missing_required_param_amount(self):
        """Test that redeem_bfusd() raises RequiredError when 'amount' is missing."""
        params = {
            "amount": 1.0,
            "type": "s",
        }
        params["amount"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'amount'"):
            self.client.redeem_bfusd(**params)

    def test_redeem_bfusd_missing_required_param_type(self):
        """Test that redeem_bfusd() raises RequiredError when 'type' is missing."""
        params = {
            "amount": 1.0,
            "type": "s",
        }
        params["type"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'type'"):
            self.client.redeem_bfusd(**params)

    def test_redeem_bfusd_server_error(self):
        """Test that redeem_bfusd() raises an error when the server returns an error."""

        params = {
            "amount": 1.0,
            "type": "s",
        }

        mock_error = Exception("ResponseError")
        self.client.redeem_bfusd = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.redeem_bfusd(**params)

    @patch("binance_common.utils.get_signature")
    def test_subscribe_bfusd_success(self, mock_get_signature):
        """Test subscribe_bfusd() successfully with required parameters only."""

        params = {
            "asset": "asset_example",
            "amount": 1.0,
        }

        expected_response = {"success": True, "bfusdAmount": "0.22091092"}
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.subscribe_bfusd(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()
        mock_get_signature.assert_called_once()

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/subscribe" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"
        assert normalized["asset"] == "asset_example"
        assert normalized["amount"] == 1.0

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(SubscribeBfusdResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(SubscribeBfusdResponse, "from_dict"):
            expected = SubscribeBfusdResponse.from_dict(expected_response)
        else:
            expected = SubscribeBfusdResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    @patch("binance_common.utils.get_signature")
    def test_subscribe_bfusd_success_with_optional_params(self, mock_get_signature):
        """Test subscribe_bfusd() successfully with optional parameters."""

        params = {"asset": "asset_example", "amount": 1.0, "recv_window": 5000}

        expected_response = {"success": True, "bfusdAmount": "0.22091092"}
        mock_get_signature.return_value = "mocked_signature"
        self.set_mock_response(expected_response)

        response = self.client.subscribe_bfusd(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "signature" in parse_qs(request_kwargs["params"])
        assert "/sapi/v1/bfusd/subscribe" in request_kwargs["url"]
        assert request_kwargs["method"] == "POST"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(SubscribeBfusdResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(SubscribeBfusdResponse, "from_dict"):
            expected = SubscribeBfusdResponse.from_dict(expected_response)
        else:
            expected = SubscribeBfusdResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_subscribe_bfusd_missing_required_param_asset(self):
        """Test that subscribe_bfusd() raises RequiredError when 'asset' is missing."""
        params = {
            "asset": "asset_example",
            "amount": 1.0,
        }
        params["asset"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'asset'"):
            self.client.subscribe_bfusd(**params)

    def test_subscribe_bfusd_missing_required_param_amount(self):
        """Test that subscribe_bfusd() raises RequiredError when 'amount' is missing."""
        params = {
            "asset": "asset_example",
            "amount": 1.0,
        }
        params["amount"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'amount'"):
            self.client.subscribe_bfusd(**params)

    def test_subscribe_bfusd_server_error(self):
        """Test that subscribe_bfusd() raises an error when the server returns an error."""

        params = {
            "asset": "asset_example",
            "amount": 1.0,
        }

        mock_error = Exception("ResponseError")
        self.client.subscribe_bfusd = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.subscribe_bfusd(**params)
