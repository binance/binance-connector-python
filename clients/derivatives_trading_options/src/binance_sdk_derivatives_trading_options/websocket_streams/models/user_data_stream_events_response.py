# coding: utf-8

"""
Options WebSocket Market Streams

Access market data, manage accounts, and trade Binance Options.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations
import json
import pprint
from pydantic import (
    BaseModel,
    ConfigDict,
    ValidationError,
    field_validator,
    model_validator,
)
from typing import Any, Optional
from binance_sdk_derivatives_trading_options.websocket_streams.models.account_update import (
    AccountUpdate,
)
from binance_sdk_derivatives_trading_options.websocket_streams.models.balance_position_update import (
    BalancePositionUpdate,
)
from binance_sdk_derivatives_trading_options.websocket_streams.models.greek_update import (
    GreekUpdate,
)
from binance_sdk_derivatives_trading_options.websocket_streams.models.listen_key_expired import (
    ListenKeyExpired,
)
from binance_sdk_derivatives_trading_options.websocket_streams.models.order_trade_update import (
    OrderTradeUpdate,
)
from binance_sdk_derivatives_trading_options.websocket_streams.models.risk_level_change import (
    RiskLevelChange,
)
from pydantic import model_validator
from typing import Union, Set, Dict
from typing_extensions import Self

USERDATASTREAMEVENTSRESPONSE_ONE_OF_SCHEMAS = [
    "AccountUpdate",
    "BalancePositionUpdate",
    "GreekUpdate",
    "ListenKeyExpired",
    "OrderTradeUpdate",
    "RiskLevelChange",
]

# The `oneof_schema_N_validator` field each event schema is declared under, set
# alongside `actual_instance` so that a routed event is reachable under both names.
USERDATASTREAMEVENTSRESPONSE_VALIDATOR_FIELD_MAP = {
    AccountUpdate: "oneof_schema_1_validator",
    BalancePositionUpdate: "oneof_schema_2_validator",
    GreekUpdate: "oneof_schema_3_validator",
    ListenKeyExpired: "oneof_schema_4_validator",
    OrderTradeUpdate: "oneof_schema_5_validator",
    RiskLevelChange: "oneof_schema_6_validator",
}

# The event schemas, keyed by the event name the `e` field of the payload carries.
USERDATASTREAMEVENTSRESPONSE_EVENT_SCHEMA_MAP = {
    "ACCOUNT_UPDATE": AccountUpdate,
    "BALANCE_POSITION_UPDATE": BalancePositionUpdate,
    "GREEK_UPDATE": GreekUpdate,
    "ORDER_TRADE_UPDATE": OrderTradeUpdate,
    "RISK_LEVEL_CHANGE": RiskLevelChange,
    "listenKeyExpired": ListenKeyExpired,
}

# The same schemas, with their validator field, keyed by event name without
# separators or case, which is how an incoming event is looked up.
USERDATASTREAMEVENTSRESPONSE_EVENT_TYPE_MAP = {
    event_name.replace("_", "").replace("-", "").lower(): (schema, validator_field)
    for event_name, schema in USERDATASTREAMEVENTSRESPONSE_EVENT_SCHEMA_MAP.items()
    if (validator_field := USERDATASTREAMEVENTSRESPONSE_VALIDATOR_FIELD_MAP.get(schema))
}


class UserDataStreamEventsResponse(BaseModel):
    """
    UserDataStreamEventsResponse
    """

    # data type: AccountUpdate
    oneof_schema_1_validator: Optional[AccountUpdate] = None
    # data type: BalancePositionUpdate
    oneof_schema_2_validator: Optional[BalancePositionUpdate] = None
    # data type: GreekUpdate
    oneof_schema_3_validator: Optional[GreekUpdate] = None
    # data type: ListenKeyExpired
    oneof_schema_4_validator: Optional[ListenKeyExpired] = None
    # data type: OrderTradeUpdate
    oneof_schema_5_validator: Optional[OrderTradeUpdate] = None
    # data type: RiskLevelChange
    oneof_schema_6_validator: Optional[RiskLevelChange] = None
    actual_instance: Optional[
        Union[
            AccountUpdate,
            BalancePositionUpdate,
            GreekUpdate,
            ListenKeyExpired,
            OrderTradeUpdate,
            RiskLevelChange,
        ]
    ] = None
    one_of_schemas: Set[str] = {
        "AccountUpdate",
        "BalancePositionUpdate",
        "GreekUpdate",
        "ListenKeyExpired",
        "OrderTradeUpdate",
        "RiskLevelChange",
    }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )

    discriminator_value_class_map: Dict[str, str] = {}

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError(
                    "If a position argument is used, only 1 is allowed to set `actual_instance`"
                )
            if kwargs:
                raise ValueError(
                    "If a position argument is used, keyword arguments cannot be used."
                )
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @classmethod
    def resolve_event_schema(cls, event_name: Any) -> Optional[Any]:
        """Returns the `(schema, validator field)` pair `event_name` belongs to.

        The event name is the only thing telling user data events apart: their
        schemas declare every field as optional, so most of them validate any
        event. Names are compared without separators or case, to accept both
        `executionReport` and `ORDER_TRADE_UPDATE`.
        """
        if not isinstance(event_name, str):
            return None

        key = event_name.replace("_", "").replace("-", "").lower()
        return USERDATASTREAMEVENTSRESPONSE_EVENT_TYPE_MAP.get(key)

    @model_validator(mode="before")
    @classmethod
    def route_user_data_event(cls, data: Any) -> Any:
        """Deserializes a raw user data event into the schema its `e` field names."""
        if not isinstance(data, dict) or "actual_instance" in data:
            return data

        resolved = cls.resolve_event_schema(data.get("e"))
        if resolved is None:
            return data

        schema, validator_field = resolved

        instance = schema.from_dict(data)
        return {"actual_instance": instance, validator_field: instance}

    @field_validator("actual_instance")
    def actual_instance_must_validate_oneof(cls, v):

        error_messages = []
        match = 0
        # validate data type: AccountUpdate
        if not isinstance(v, AccountUpdate):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `AccountUpdate`"
            )
        else:
            match += 1
        # validate data type: BalancePositionUpdate
        if not isinstance(v, BalancePositionUpdate):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `BalancePositionUpdate`"
            )
        else:
            match += 1
        # validate data type: GreekUpdate
        if not isinstance(v, GreekUpdate):
            error_messages.append(f"Error! Input type `{type(v)}` is not `GreekUpdate`")
        else:
            match += 1
        # validate data type: ListenKeyExpired
        if not isinstance(v, ListenKeyExpired):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `ListenKeyExpired`"
            )
        else:
            match += 1
        # validate data type: OrderTradeUpdate
        if not isinstance(v, OrderTradeUpdate):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `OrderTradeUpdate`"
            )
        else:
            match += 1
        # validate data type: RiskLevelChange
        if not isinstance(v, RiskLevelChange):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `RiskLevelChange`"
            )
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when setting `actual_instance` in UserDataStreamEventsResponse with oneOf schemas: AccountUpdate, BalancePositionUpdate, GreekUpdate, ListenKeyExpired, OrderTradeUpdate, RiskLevelChange. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when setting `actual_instance` in UserDataStreamEventsResponse with oneOf schemas: AccountUpdate, BalancePositionUpdate, GreekUpdate, ListenKeyExpired, OrderTradeUpdate, RiskLevelChange. Details: "
                + ", ".join(error_messages)
            )
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        try:
            parsed = json.loads(json_str)
        except (TypeError, ValueError):
            parsed = None

        if isinstance(parsed, dict) and cls.resolve_event_schema(parsed.get("e")):
            return cls.model_validate(parsed)

        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into AccountUpdate
        try:
            instance.actual_instance = AccountUpdate.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into BalancePositionUpdate
        try:
            instance.actual_instance = BalancePositionUpdate.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into GreekUpdate
        try:
            instance.actual_instance = GreekUpdate.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ListenKeyExpired
        try:
            instance.actual_instance = ListenKeyExpired.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into OrderTradeUpdate
        try:
            instance.actual_instance = OrderTradeUpdate.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into RiskLevelChange
        try:
            instance.actual_instance = RiskLevelChange.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when deserializing the JSON string into UserDataStreamEventsResponse with oneOf schemas: AccountUpdate, BalancePositionUpdate, GreekUpdate, ListenKeyExpired, OrderTradeUpdate, RiskLevelChange. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when deserializing the JSON string into UserDataStreamEventsResponse with oneOf schemas: AccountUpdate, BalancePositionUpdate, GreekUpdate, ListenKeyExpired, OrderTradeUpdate, RiskLevelChange. Details: "
                + ", ".join(error_messages)
            )
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(
            self.actual_instance.to_json
        ):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(
        self,
    ) -> Optional[
        Union[
            Dict[str, Any],
            AccountUpdate,
            BalancePositionUpdate,
            GreekUpdate,
            ListenKeyExpired,
            OrderTradeUpdate,
            RiskLevelChange,
        ]
    ]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(
            self.actual_instance.to_dict
        ):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())
