"""
Binance Alpha REST API

OpenAPI Specification for the Binance Alpha REST API
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

import json
import pytest
import requests

from unittest.mock import MagicMock
from urllib.parse import parse_qs

from binance_common.configuration import ConfigurationRestAPI
from binance_common.errors import RequiredError
from binance_common.utils import normalize_query_values, is_one_of_model, snake_to_camel

from binance_sdk_alpha.rest_api.api import MarketDataApi
from binance_sdk_alpha.rest_api.models import AggregatedTradesResponse
from binance_sdk_alpha.rest_api.models import GetExchangeInfoResponse
from binance_sdk_alpha.rest_api.models import KlinesResponse
from binance_sdk_alpha.rest_api.models import TickerResponse
from binance_sdk_alpha.rest_api.models import TokenListResponse


class TestMarketDataApi:
    @pytest.fixture(autouse=True)
    def setup_client(self):
        """Setup a client instance with mocked session before each test method."""
        self.mock_session = MagicMock(spec=requests.Session)
        config = ConfigurationRestAPI(
            api_key="test-api-key",
            api_secret="test-api-secret",
        )
        self.client = MarketDataApi(configuration=config, session=self.mock_session)

    def set_mock_response(self, data: dict = {}, status_code=200, headers=None):
        """Helper method to setup mock response for the client's session request."""
        if headers is None:
            headers = {}

        mock_response = MagicMock()
        mock_response.status_code = status_code
        mock_response.json.return_value = data
        mock_response.text = json.dumps(data)
        mock_response.headers = headers

        self.mock_session.request.return_value = mock_response

    def test_aggregated_trades_success(self):
        """Test aggregated_trades() successfully with required parameters only."""

        params = {
            "symbol": "symbol_example",
        }

        expected_response = {
            "code": "000000",
            "message": "",
            "messageDetail": "",
            "data": [
                {
                    "a": 58470,
                    "p": "1.00",
                    "q": "1.00",
                    "f": 58470,
                    "l": 58665,
                    "T": 1752568680000,
                    "m": True,
                }
            ],
        }

        self.set_mock_response(expected_response)

        response = self.client.aggregated_trades(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert "/bapi/defi/v1/public/alpha-trade/agg-trades" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["symbol"] == "symbol_example"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(AggregatedTradesResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(AggregatedTradesResponse, "from_dict"):
            expected = AggregatedTradesResponse.from_dict(expected_response)
        else:
            expected = AggregatedTradesResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_aggregated_trades_success_with_optional_params(self):
        """Test aggregated_trades() successfully with optional parameters."""

        params = {
            "symbol": "symbol_example",
            "from_id": 1,
            "start_time": 1623319461670,
            "end_time": 1641782889000,
            "limit": 500,
        }

        expected_response = {
            "code": "000000",
            "message": "",
            "messageDetail": "",
            "data": [
                {
                    "a": 58470,
                    "p": "1.00",
                    "q": "1.00",
                    "f": 58470,
                    "l": 58665,
                    "T": 1752568680000,
                    "m": True,
                }
            ],
        }

        self.set_mock_response(expected_response)

        response = self.client.aggregated_trades(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/bapi/defi/v1/public/alpha-trade/agg-trades" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(AggregatedTradesResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(AggregatedTradesResponse, "from_dict"):
            expected = AggregatedTradesResponse.from_dict(expected_response)
        else:
            expected = AggregatedTradesResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_aggregated_trades_missing_required_param_symbol(self):
        """Test that aggregated_trades() raises RequiredError when 'symbol' is missing."""
        params = {
            "symbol": "symbol_example",
        }
        params["symbol"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'symbol'"):
            self.client.aggregated_trades(**params)

    def test_aggregated_trades_server_error(self):
        """Test that aggregated_trades() raises an error when the server returns an error."""

        params = {
            "symbol": "symbol_example",
        }

        mock_error = Exception("ResponseError")
        self.client.aggregated_trades = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.aggregated_trades(**params)

    def test_get_exchange_info_success(self):
        """Test get_exchange_info() successfully with required parameters only."""

        expected_response = {
            "code": "000000",
            "message": "",
            "messageDetail": "",
            "success": True,
            "data": {
                "timezone": "UTC",
                "assets": [{"asset": "USDT"}],
                "symbols": [
                    {
                        "symbol": "ALPHA_105USDT",
                        "status": "TRADING",
                        "baseAsset": "ALPHA_105",
                        "quoteAsset": "USDT",
                        "pricePrecision": 8,
                        "quantityPrecision": 8,
                        "baseAssetPrecision": 8,
                        "quotePrecision": 8,
                        "filters": [
                            {
                                "filterType": "PRICE_FILTER",
                                "minPrice": "0.00000001",
                                "maxPrice": "1000",
                                "tickSize": "0.00000001",
                            },
                            {
                                "filterType": "LOT_SIZE",
                                "stepSize": "0.01000000",
                                "maxQty": "277778",
                                "minQty": "0.01000000",
                            },
                            {"filterType": "MAX_NUM_ORDERS", "limit": 200},
                            {"filterType": "MIN_NOTIONAL", "minNotional": "0.1"},
                            {"filterType": "MAX_NOTIONAL", "maxNotional": "1000000"},
                            {
                                "filterType": "NOTIONAL",
                                "minNotional": "0.1",
                                "maxNotional": "1000000",
                            },
                            {
                                "filterType": "PERCENT_PRICE",
                                "multiplierDown": "0.20000",
                                "multiplierUp": "5",
                            },
                            {
                                "filterType": "PERCENT_PRICE_BY_SIDE",
                                "bidMultiplierUp": "5",
                                "askMultiplierUp": "5",
                                "bidMultiplierDown": "0.20000",
                                "askMultiplierDown": "0.20000",
                            },
                        ],
                        "orderTypes": ["LIMIT"],
                    }
                ],
                "orderTypes": "",
            },
        }

        self.set_mock_response(expected_response)

        response = self.client.get_exchange_info()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert (
            "/bapi/defi/v1/public/alpha-trade/get-exchange-info"
            in request_kwargs["url"]
        )
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(GetExchangeInfoResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(GetExchangeInfoResponse, "from_dict"):
            expected = GetExchangeInfoResponse.from_dict(expected_response)
        else:
            expected = GetExchangeInfoResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_get_exchange_info_server_error(self):
        """Test that get_exchange_info() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.get_exchange_info = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.get_exchange_info()

    def test_klines_success(self):
        """Test klines() successfully with required parameters only."""

        params = {
            "symbol": "symbol_example",
            "interval": "interval_example",
        }

        expected_response = {
            "code": "000000",
            "message": "",
            "messageDetail": "",
            "success": True,
            "data": [
                [
                    "1752642000000",
                    "0.00171473",
                    "0.00172515",
                    "0.00171473",
                    "0.00172515",
                    "1771.86000000",
                    "1752645599999",
                    "3.05093481",
                    "2",
                    "1771.86000000",
                    "3.05093481",
                    0,
                ]
            ],
        }

        self.set_mock_response(expected_response)

        response = self.client.klines(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert "/bapi/defi/v1/public/alpha-trade/klines" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["symbol"] == "symbol_example"
        assert normalized["interval"] == "interval_example"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(KlinesResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(KlinesResponse, "from_dict"):
            expected = KlinesResponse.from_dict(expected_response)
        else:
            expected = KlinesResponse.model_validate_json(json.dumps(expected_response))

        assert response.data() == expected

    def test_klines_success_with_optional_params(self):
        """Test klines() successfully with optional parameters."""

        params = {
            "symbol": "symbol_example",
            "interval": "interval_example",
            "limit": 500,
            "start_time": 1623319461670,
            "end_time": 1641782889000,
        }

        expected_response = {
            "code": "000000",
            "message": "",
            "messageDetail": "",
            "success": True,
            "data": [
                [
                    "1752642000000",
                    "0.00171473",
                    "0.00172515",
                    "0.00171473",
                    "0.00172515",
                    "1771.86000000",
                    "1752645599999",
                    "3.05093481",
                    "2",
                    "1771.86000000",
                    "3.05093481",
                    0,
                ]
            ],
        }

        self.set_mock_response(expected_response)

        response = self.client.klines(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/bapi/defi/v1/public/alpha-trade/klines" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(KlinesResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(KlinesResponse, "from_dict"):
            expected = KlinesResponse.from_dict(expected_response)
        else:
            expected = KlinesResponse.model_validate_json(json.dumps(expected_response))

        assert response.data() == expected

    def test_klines_missing_required_param_symbol(self):
        """Test that klines() raises RequiredError when 'symbol' is missing."""
        params = {
            "symbol": "symbol_example",
            "interval": "interval_example",
        }
        params["symbol"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'symbol'"):
            self.client.klines(**params)

    def test_klines_missing_required_param_interval(self):
        """Test that klines() raises RequiredError when 'interval' is missing."""
        params = {
            "symbol": "symbol_example",
            "interval": "interval_example",
        }
        params["interval"] = None

        with pytest.raises(
            RequiredError, match="Missing required parameter 'interval'"
        ):
            self.client.klines(**params)

    def test_klines_server_error(self):
        """Test that klines() raises an error when the server returns an error."""

        params = {
            "symbol": "symbol_example",
            "interval": "interval_example",
        }

        mock_error = Exception("ResponseError")
        self.client.klines = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.klines(**params)

    def test_ticker_success(self):
        """Test ticker() successfully with required parameters only."""

        params = {"symbol": "symbol_example"}

        expected_response = {
            "code": "000000",
            "message": "",
            "messageDetail": "",
            "data": {
                "symbol": "ALPHA_175USDT",
                "priceChange": "-0.00007172",
                "priceChangePercent": "-8.841",
                "weightedAvgPrice": "0.00079608",
                "lastPrice": "0.00073954",
                "lastQty": "12600.43000000",
                "openPrice": "0.00081126",
                "highPrice": "0.00081126",
                "lowPrice": "0.00073954",
                "volume": "1204754.30000000",
                "quoteVolume": "959.07729927",
                "openTime": 1768808100000,
                "closeTime": 1768893244772,
                "firstId": 93742,
                "lastId": 93768,
                "count": 38,
            },
            "success": True,
        }

        self.set_mock_response(expected_response)

        response = self.client.ticker(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs
        parsed_params = parse_qs(request_kwargs["params"])
        camel_case_params = {snake_to_camel(k): v for k, v in params.items()}
        normalized = normalize_query_values(parsed_params, camel_case_params)

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert "/bapi/defi/v1/public/alpha-trade/ticker" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"
        assert normalized["symbol"] == "symbol_example"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TickerResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(TickerResponse, "from_dict"):
            expected = TickerResponse.from_dict(expected_response)
        else:
            expected = TickerResponse.model_validate_json(json.dumps(expected_response))

        assert response.data() == expected

    def test_ticker_success_with_optional_params(self):
        """Test ticker() successfully with optional parameters."""

        params = {"symbol": "symbol_example"}

        expected_response = {
            "code": "000000",
            "message": "",
            "messageDetail": "",
            "data": {
                "symbol": "ALPHA_175USDT",
                "priceChange": "-0.00007172",
                "priceChangePercent": "-8.841",
                "weightedAvgPrice": "0.00079608",
                "lastPrice": "0.00073954",
                "lastQty": "12600.43000000",
                "openPrice": "0.00081126",
                "highPrice": "0.00081126",
                "lowPrice": "0.00073954",
                "volume": "1204754.30000000",
                "quoteVolume": "959.07729927",
                "openTime": 1768808100000,
                "closeTime": 1768893244772,
                "firstId": 93742,
                "lastId": 93768,
                "count": 38,
            },
            "success": True,
        }

        self.set_mock_response(expected_response)

        response = self.client.ticker(**params)

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        assert "url" in request_kwargs
        assert "/bapi/defi/v1/public/alpha-trade/ticker" in request_kwargs["url"]
        assert request_kwargs["method"] == "GET"

        self.mock_session.request.assert_called_once()
        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TickerResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(TickerResponse, "from_dict"):
            expected = TickerResponse.from_dict(expected_response)
        else:
            expected = TickerResponse.model_validate_json(json.dumps(expected_response))

        assert response.data() == expected

    def test_ticker_missing_required_param_symbol(self):
        """Test that ticker() raises RequiredError when 'symbol' is missing."""
        params = {"symbol": "symbol_example"}
        params["symbol"] = None

        with pytest.raises(RequiredError, match="Missing required parameter 'symbol'"):
            self.client.ticker(**params)

    def test_ticker_server_error(self):
        """Test that ticker() raises an error when the server returns an error."""

        params = {"symbol": "symbol_example"}

        mock_error = Exception("ResponseError")
        self.client.ticker = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.ticker(**params)

    def test_token_list_success(self):
        """Test token_list() successfully with required parameters only."""

        expected_response = {
            "code": "000000",
            "message": "",
            "messageDetail": "",
            "success": True,
            "data": [
                {
                    "tokenId": "3F350C8B3621770A673159B7A19BC034",
                    "chainId": "56",
                    "chainIconUrl": "https://bin.bnbstatic.com/image/admin_mgs_image_upload/20250228/d0216ce4-a3e9-4bda-8937-4a6aa943ccf2.png",
                    "chainName": "BSC",
                    "contractAddress": "0xcf640fdf9b3d9e45cbd69fda91d7e22579c14444",
                    "name": "gorilla",
                    "symbol": "gorilla",
                    "iconUrl": "https://bin.bnbstatic.com/images/web3-data/public/token/logos/248b5406f88a4ee28913a29107875339.png",
                    "price": "0.00080595003978242023",
                    "percentChange24h": "-7.42",
                    "volume24h": "14847.711222633409558029675",
                    "marketCap": "805950.03978242",
                    "fdv": "805950.03978242",
                    "liquidity": "263192.72813121626034",
                    "totalSupply": "1000000000",
                    "circulatingSupply": "1000000000",
                    "holders": "7120",
                    "decimals": 18,
                    "listingCex": False,
                    "hotTag": False,
                    "cexCoinName": "",
                    "canTransfer": False,
                    "denomination": 1,
                    "offline": False,
                    "tradeDecimal": 8,
                    "alphaId": "ALPHA_175",
                    "offsell": False,
                    "priceHigh24h": "0.00088873864475645879",
                    "priceLow24h": "0.0008020805165487083",
                    "count24h": "166",
                    "onlineTge": False,
                    "onlineAirdrop": False,
                    "score": 1,
                    "cexOffDisplay": False,
                    "stockState": False,
                    "listingTime": 1746686700000,
                    "mulPoint": 1,
                    "bnExclusiveState": False,
                }
            ],
        }

        self.set_mock_response(expected_response)

        response = self.client.token_list()

        actual_call_args = self.mock_session.request.call_args
        request_kwargs = actual_call_args.kwargs

        self.mock_session.request.assert_called_once()

        assert "url" in request_kwargs
        assert (
            "/bapi/defi/v1/public/wallet-direct/buw/wallet/cex/alpha/all/token/list"
            in request_kwargs["url"]
        )
        assert request_kwargs["method"] == "GET"

        assert response is not None
        is_list = isinstance(expected_response, list)
        is_flat_list = (
            is_list and not isinstance(expected_response[0], list) if is_list else False
        )
        is_oneof = is_one_of_model(TokenListResponse)

        if is_list and not is_flat_list:
            expected = expected_response
        elif is_oneof or is_list or hasattr(TokenListResponse, "from_dict"):
            expected = TokenListResponse.from_dict(expected_response)
        else:
            expected = TokenListResponse.model_validate_json(
                json.dumps(expected_response)
            )

        assert response.data() == expected

    def test_token_list_server_error(self):
        """Test that token_list() raises an error when the server returns an error."""

        mock_error = Exception("ResponseError")
        self.client.token_list = MagicMock(side_effect=mock_error)

        with pytest.raises(Exception, match="ResponseError"):
            self.client.token_list()
