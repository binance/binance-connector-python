"""
Alpha WebSocket Market Streams

Access Alpha market streams over WebSocket.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from binance_common.errors import RequiredError
from binance_common.utils import ws_streams_placeholder
from binance_common.websocket import (
    WebSocketStreamBase,
    RequestStream,
    RequestStreamHandle,
)

from ..models import AggregateTradeStreamResponse
from ..models import AllBookTickerStreamResponse
from ..models import AllMiniTickerStreamResponse
from ..models import AllTickerStreamResponse
from ..models import AllTokens24hTickerStreamResponse
from ..models import BookTickerStreamResponse
from ..models import ContractKlineStreamResponse
from ..models import FullDepthStreamResponse
from ..models import KlineStreamResponse
from ..models import MiniTickerStreamResponse
from ..models import PartialDepthStreamResponse
from ..models import TickerStreamResponse
from ..models import TradeStreamResponse


from ..models import ContractKlineStreamIntervalEnum


from ..models import FullDepthStreamIntervalEnum


from ..models import KlineStreamIntervalEnum


from ..models import PartialDepthStreamLevelsEnum
from ..models import PartialDepthStreamIntervalEnum


from typing import Optional, Union


class DefaultApi:
    """Client for DefaultApi endpoints."""

    def __init__(self, websocket_base: WebSocketStreamBase) -> None:
        self.websocket_base = websocket_base

    async def aggregate_trade_stream(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Aggregate Trade Stream
        /<symbol>@aggTrade
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#aggregate-trade-stream

        Pushes aggregate trade updates for a symbol.

        Args:
                symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        if symbol is None:
            raise RequiredError(
                field="symbol", error_message="Missing required parameter 'symbol'"
            )

        stream = ws_streams_placeholder(
            "/<symbol>@aggTrade".replace("/", "", 1),
            {
                "symbol": symbol,
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=AggregateTradeStreamResponse,
            stream_url="",
        )

    async def all_book_ticker_stream(
        self,
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        All Book Ticker Stream
        /!bookTicker
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#all-book-ticker-stream

        Pushes best bid/ask updates for all symbols.

        Args:
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        stream = ws_streams_placeholder(
            "/!bookTicker".replace("/", "", 1),
            {
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=AllBookTickerStreamResponse,
            stream_url="",
        )

    async def all_mini_ticker_stream(
        self,
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        All Mini Ticker Stream
        /!miniTicker@arr
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#all-mini-ticker-stream

        Pushes mini ticker statistics for all symbols.

        Args:
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        stream = ws_streams_placeholder(
            "/!miniTicker@arr".replace("/", "", 1),
            {
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=AllMiniTickerStreamResponse,
            stream_url="",
        )

    async def all_ticker_stream(
        self,
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        All Ticker Stream
        /!ticker@arr
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#all-ticker-stream

        Pushes full ticker statistics for all symbols.

        Args:
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        stream = ws_streams_placeholder(
            "/!ticker@arr".replace("/", "", 1),
            {
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=AllTickerStreamResponse,
            stream_url="",
        )

    async def all_tokens24h_ticker_stream(
        self,
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        All Tokens 24h Ticker Stream
        /came@allTokens@ticker24
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#all-tokens24h-ticker-stream

        Pushes 24h ticker-like metrics for all tokens.

        Args:
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        stream = ws_streams_placeholder(
            "/came@allTokens@ticker24".replace("/", "", 1),
            {
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=AllTokens24hTickerStreamResponse,
            stream_url="",
        )

    async def book_ticker_stream(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Book Ticker Stream
        /<symbol>@bookTicker
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#book-ticker-stream

        Pushes best bid/ask updates for a symbol.

        Args:
                symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        if symbol is None:
            raise RequiredError(
                field="symbol", error_message="Missing required parameter 'symbol'"
            )

        stream = ws_streams_placeholder(
            "/<symbol>@bookTicker".replace("/", "", 1),
            {
                "symbol": symbol,
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=BookTickerStreamResponse,
            stream_url="",
        )

    async def contract_kline_stream(
        self,
        contract_address: Union[str, None],
        chain_id: Union[str, None],
        interval: Union[ContractKlineStreamIntervalEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Contract Kline Stream
        /came@<contractAddress>@<chainId>@kline_<interval>
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#contract-kline-stream

        Pushes kline updates by contractAddress@chainId.

        Args:
                contract_address (Union[str, None]): Contract address.
                chain_id (Union[str, None]): Chain ID.
                interval (Union[ContractKlineStreamIntervalEnum, None]): Kline interval.
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        if contract_address is None:
            raise RequiredError(
                field="contract_address",
                error_message="Missing required parameter 'contract_address'",
            )
        if chain_id is None:
            raise RequiredError(
                field="chain_id", error_message="Missing required parameter 'chain_id'"
            )
        if interval is None:
            raise RequiredError(
                field="interval", error_message="Missing required parameter 'interval'"
            )

        stream = ws_streams_placeholder(
            "/came@<contractAddress>@<chainId>@kline_<interval>".replace("/", "", 1),
            {
                "contract_address": contract_address,
                "chain_id": chain_id,
                "interval": interval,
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=ContractKlineStreamResponse,
            stream_url="",
        )

    async def full_depth_stream(
        self,
        symbol: Union[str, None],
        interval: Union[FullDepthStreamIntervalEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Full Depth Stream
        /<symbol>@fulldepth@<interval>
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#full-depth-stream

        Returns all available depth, including UI and API orders.

        Args:
                symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
                interval (Union[FullDepthStreamIntervalEnum, None]): Update interval.
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        if symbol is None:
            raise RequiredError(
                field="symbol", error_message="Missing required parameter 'symbol'"
            )
        if interval is None:
            raise RequiredError(
                field="interval", error_message="Missing required parameter 'interval'"
            )

        stream = ws_streams_placeholder(
            "/<symbol>@fulldepth@<interval>".replace("/", "", 1),
            {
                "symbol": symbol,
                "interval": interval,
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=FullDepthStreamResponse,
            stream_url="",
        )

    async def kline_stream(
        self,
        symbol: Union[str, None],
        interval: Union[KlineStreamIntervalEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Kline Stream
        /<symbol>@kline_<interval>
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#kline-stream

        Pushes kline updates for a symbol.

        Args:
                symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
                interval (Union[KlineStreamIntervalEnum, None]): Kline interval.
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        if symbol is None:
            raise RequiredError(
                field="symbol", error_message="Missing required parameter 'symbol'"
            )
        if interval is None:
            raise RequiredError(
                field="interval", error_message="Missing required parameter 'interval'"
            )

        stream = ws_streams_placeholder(
            "/<symbol>@kline_<interval>".replace("/", "", 1),
            {
                "symbol": symbol,
                "interval": interval,
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=KlineStreamResponse,
            stream_url="",
        )

    async def mini_ticker_stream(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Mini Ticker Stream
        /<symbol>@miniTicker
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#mini-ticker-stream

        Pushes 24h rolling mini ticker statistics.

        Args:
                symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        if symbol is None:
            raise RequiredError(
                field="symbol", error_message="Missing required parameter 'symbol'"
            )

        stream = ws_streams_placeholder(
            "/<symbol>@miniTicker".replace("/", "", 1),
            {
                "symbol": symbol,
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=MiniTickerStreamResponse,
            stream_url="",
        )

    async def partial_depth_stream(
        self,
        symbol: Union[str, None],
        levels: Union[PartialDepthStreamLevelsEnum, None],
        interval: Union[PartialDepthStreamIntervalEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Partial Depth Stream
        /<symbol>@depth<levels>@<interval>
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#partial-depth-stream

        Pushes partial depth updates (UI orders only).

        Args:
                symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
                levels (Union[PartialDepthStreamLevelsEnum, None]): Depth levels.
                interval (Union[PartialDepthStreamIntervalEnum, None]): Update interval.
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        if symbol is None:
            raise RequiredError(
                field="symbol", error_message="Missing required parameter 'symbol'"
            )
        if levels is None:
            raise RequiredError(
                field="levels", error_message="Missing required parameter 'levels'"
            )
        if interval is None:
            raise RequiredError(
                field="interval", error_message="Missing required parameter 'interval'"
            )

        stream = ws_streams_placeholder(
            "/<symbol>@depth<levels>@<interval>".replace("/", "", 1),
            {
                "symbol": symbol,
                "levels": levels,
                "interval": interval,
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=PartialDepthStreamResponse,
            stream_url="",
        )

    async def ticker_stream(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Ticker Stream
        /<symbol>@ticker
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#ticker-stream

        Pushes full 24h rolling ticker statistics.

        Args:
                symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        if symbol is None:
            raise RequiredError(
                field="symbol", error_message="Missing required parameter 'symbol'"
            )

        stream = ws_streams_placeholder(
            "/<symbol>@ticker".replace("/", "", 1),
            {
                "symbol": symbol,
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=TickerStreamResponse,
            stream_url="",
        )

    async def trade_stream(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Trade Stream
        /<symbol>@trade
        https://developers.binance.com/en/docs/catalog/advanced-trading-alpha-trading/api/ws-streams/~#trade-stream

        Pushes raw trade updates for a symbol.

        Args:
                symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
                id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        if symbol is None:
            raise RequiredError(
                field="symbol", error_message="Missing required parameter 'symbol'"
            )

        stream = ws_streams_placeholder(
            "/<symbol>@trade".replace("/", "", 1),
            {
                "symbol": symbol,
                "id": id,
            },
        )

        return await RequestStream(
            self.websocket_base,
            stream=stream,
            response_model=TradeStreamResponse,
            stream_url="",
        )
