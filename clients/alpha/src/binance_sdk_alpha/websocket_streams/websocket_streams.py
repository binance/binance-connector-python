"""
Alpha WebSocket Market Streams

Access Alpha market streams over WebSocket.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from pydantic import BaseModel
from typing import Callable, Optional, TypeVar, Union

from binance_common.configuration import ConfigurationWebSocketStreams
from binance_common.websocket import (
    WebSocketStreamBase,
    WebSocketConnection,
    RequestStreamHandle,
)


from .streams.default_api import DefaultApi


from .models import ContractKlineStreamIntervalEnum


from .models import FullDepthStreamIntervalEnum


from .models import KlineStreamIntervalEnum


from .models import PartialDepthStreamLevelsEnum
from .models import PartialDepthStreamIntervalEnum


T = TypeVar("T", bound=BaseModel)


class AlphaWebSocketStreams(WebSocketStreamBase):
    def __init__(
        self,
        configuration: ConfigurationWebSocketStreams,
    ) -> None:
        super().__init__(configuration)
        self.configuration = configuration

        self._defaultApi = DefaultApi(self)

    async def ping_server(self, connection: WebSocketConnection):
        """Sends a ping message to the WebSocket server to check the connection status.

        Args:
            connection (WebSocketConnection): The WebSocket connection to send the ping message to.
        """

        if self.session:
            await super().ping_ws_stream(connection=connection)
        else:
            raise ValueError("WebSocket session is not initialized.")

    async def subscribe(
        self,
        streams,
        response_model: Optional[T] = None,
        stream_url: Optional[str] = None,
    ):
        """Subscribes to the specified WebSocket streams.

        Args:
            streams (list): A list of WebSocket streams to subscribe to.
            response_model (Optional[T]): The Pydantic model to validate the response against.
        """

        await super().subscribe(
            streams, response_model=response_model, stream_url=stream_url
        )

    async def unsubscribe(self, streams):
        """Unsubscribes from the specified WebSocket streams.

        Args:
            streams (list): A list of WebSocket streams to unsubscribe from.
        """

        await super().unsubscribe(streams)

    def on(self, event: str, callback: Callable[[T], None], stream: str):
        """Registers a callback function for a specific WebSocket event.

        Args:
            event (str): The event type to listen for (e.g., 'message', 'error').
            callback (Callable[[T], None]): The callback function to be called when the event occurs.
            stream (str): The specific WebSocket stream to listen to.
        """

        super().on(event, callback, stream)

    async def list_subscribe(self) -> dict:
        """Lists all currently subscribed WebSocket streams.

        Returns:
            dict: A dictionary containing the current subscriptions.
        """

        return await super().list_subscribe()

    async def close_connection(
        self, connection: WebSocketConnection = None, close_session: bool = True
    ):
        """Closes the WebSocket connection and optionally the session.

        Args:
            connection (WebSocketConnection, optional): The WebSocket connection to close. If None, the current session's connection is used.
            close_session (bool, optional): Whether to close the session after closing the connection. Defaults to True.
        """

        await super().close_connection(connection, close_session)

    async def aggregate_trade_stream(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Aggregate Trade Stream

        Pushes aggregate trade updates for a symbol.

        Args:
            symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.aggregate_trade_stream(symbol, id)

    async def all_book_ticker_stream(
        self,
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        All Book Ticker Stream

        Pushes best bid/ask updates for all symbols.

        Args:
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.all_book_ticker_stream(id)

    async def all_mini_ticker_stream(
        self,
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        All Mini Ticker Stream

        Pushes mini ticker statistics for all symbols.

        Args:
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.all_mini_ticker_stream(id)

    async def all_ticker_stream(
        self,
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        All Ticker Stream

        Pushes full ticker statistics for all symbols.

        Args:
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.all_ticker_stream(id)

    async def all_tokens24h_ticker_stream(
        self,
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        All Tokens 24h Ticker Stream

        Pushes 24h ticker-like metrics for all tokens.

        Args:
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.all_tokens24h_ticker_stream(id)

    async def book_ticker_stream(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Book Ticker Stream

        Pushes best bid/ask updates for a symbol.

        Args:
            symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.book_ticker_stream(symbol, id)

    async def contract_kline_stream(
        self,
        contract_address: Union[str, None],
        chain_id: Union[str, None],
        interval: Union[ContractKlineStreamIntervalEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Contract Kline Stream

        Pushes kline updates by contractAddress@chainId.

        Args:
            contract_address (Union[str, None]): Contract address.
            chain_id (Union[str, None]): Chain ID.
            interval (Union[ContractKlineStreamIntervalEnum, None]): Kline interval.
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.contract_kline_stream(
            contract_address, chain_id, interval, id
        )

    async def full_depth_stream(
        self,
        symbol: Union[str, None],
        interval: Union[FullDepthStreamIntervalEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Full Depth Stream

        Returns all available depth, including UI and API orders.

        Args:
            symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
            interval (Union[FullDepthStreamIntervalEnum, None]): Update interval.
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.full_depth_stream(symbol, interval, id)

    async def kline_stream(
        self,
        symbol: Union[str, None],
        interval: Union[KlineStreamIntervalEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Kline Stream

        Pushes kline updates for a symbol.

        Args:
            symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
            interval (Union[KlineStreamIntervalEnum, None]): Kline interval.
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.kline_stream(symbol, interval, id)

    async def mini_ticker_stream(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Mini Ticker Stream

        Pushes 24h rolling mini ticker statistics.

        Args:
            symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.mini_ticker_stream(symbol, id)

    async def partial_depth_stream(
        self,
        symbol: Union[str, None],
        levels: Union[PartialDepthStreamLevelsEnum, None],
        interval: Union[PartialDepthStreamIntervalEnum, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Partial Depth Stream

        Pushes partial depth updates (UI orders only).

        Args:
            symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
            levels (Union[PartialDepthStreamLevelsEnum, None]): Depth levels.
            interval (Union[PartialDepthStreamIntervalEnum, None]): Update interval.
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.partial_depth_stream(symbol, levels, interval, id)

    async def ticker_stream(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Ticker Stream

        Pushes full 24h rolling ticker statistics.

        Args:
            symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.ticker_stream(symbol, id)

    async def trade_stream(
        self,
        symbol: Union[str, None],
        id: Optional[str] = None,
    ) -> RequestStreamHandle:
        r"""
        Trade Stream

        Pushes raw trade updates for a symbol.

        Args:
            symbol (Union[str, None]): Symbol to subscribe, in lowercase stream format.
            id (Optional[str] = None): Unique WebSocket request ID.

        Returns:
            RequestStreamHandle

        Raises:
            RequiredError: If a required parameter is missing.

        """

        return await self._defaultApi.trade_stream(symbol, id)
