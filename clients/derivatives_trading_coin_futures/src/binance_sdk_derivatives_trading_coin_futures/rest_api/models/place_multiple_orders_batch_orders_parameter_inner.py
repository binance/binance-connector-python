# coding: utf-8

"""
Futures (COIN-M) REST API

Access market data, manage accounts, and trade COIN-M perpetual and delivery futures.
The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    StrictFloat,
    StrictInt,
    StrictStr,
    field_validator,
)
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from typing import Set
from typing_extensions import Self


class PlaceMultipleOrdersBatchOrdersParameterInner(BaseModel):
    """
    PlaceMultipleOrdersBatchOrdersParameterInner
    """  # noqa: E501

    symbol: StrictStr = Field(description="Symbol")
    side: StrictStr
    position_side: Optional[StrictStr] = Field(
        default=None,
        description="Default `BOTH` for One-way Mode ; `LONG` or `SHORT` for Hedge Mode. It must be sent with Hedge Mode.",
        alias="positionSide",
    )
    type: StrictStr = Field(
        description="**After CM migration, stop-type values (`STOP`, `STOP_MARKET`, `TAKE_PROFIT`, `TAKE_PROFIT_MARKET`, `TRAILING_STOP_MARKET`) are no longer accepted on a per-element basis and will return element-level `-4120`. Use the new `/dapi/v1/algoOrder` endpoint instead.**"
    )
    time_in_force: Optional[StrictStr] = Field(default=None, alias="timeInForce")
    quantity: Union[StrictFloat, StrictInt] = Field(
        description="quantity measured by contract number"
    )
    reduce_only: Optional[StrictStr] = Field(default="FALSE", alias="reduceOnly")
    price: Optional[Union[StrictFloat, StrictInt]] = Field(
        default=None, description="Order price"
    )
    new_client_order_id: Optional[StrictStr] = Field(
        default=None,
        description="A unique id among open orders. Automatically generated if not sent. Can only be string following the rule: `^[\\.A-Z\\:/a-z0-9_-]{1,36}$`",
        alias="newClientOrderId",
    )
    stop_price: Optional[Union[StrictFloat, StrictInt]] = Field(
        default=None,
        description="Used with `STOP/STOP_MARKET` or `TAKE_PROFIT/TAKE_PROFIT_MARKET` orders.",
        alias="stopPrice",
    )
    activation_price: Optional[Union[StrictFloat, StrictInt]] = Field(
        default=None,
        description="Used with `TRAILING_STOP_MARKET` orders, default as the latest price(supporting different `workingType`)",
        alias="activationPrice",
    )
    callback_rate: Optional[
        Union[
            Annotated[float, Field(le=4, strict=True, ge=0.1)],
            Annotated[int, Field(le=4, strict=True, ge=1)],
        ]
    ] = Field(
        default=None,
        description="Used with `TRAILING_STOP_MARKET` orders, min 0.1, max 4 where 1 for 1%",
        alias="callbackRate",
    )
    working_type: Optional[StrictStr] = Field(
        default="CONTRACT_PRICE", alias="workingType"
    )
    price_protect: Optional[StrictStr] = Field(
        default="FALSE",
        description="Used with `STOP/STOP_MARKET` or `TAKE_PROFIT/TAKE_PROFIT_MARKET` orders.",
        alias="priceProtect",
    )
    new_order_resp_type: Optional[StrictStr] = Field(
        default="ACK", alias="newOrderRespType"
    )
    price_match: Optional[StrictStr] = Field(
        default=None,
        description="only avaliable for `LIMIT`/`STOP`/`TAKE_PROFIT` order; can't be passed together with `price`",
        alias="priceMatch",
    )
    self_trade_prevention_mode: Optional[StrictStr] = Field(
        default="EXPIRE_MAKER",
        description="`EXPIRE_TAKER`:expire taker order when STP triggers/ `EXPIRE_MAKER`:expire taker order when STP triggers/ `EXPIRE_BOTH`:expire both orders when STP triggers",
        alias="selfTradePreventionMode",
    )
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = [
        "symbol",
        "side",
        "positionSide",
        "type",
        "timeInForce",
        "quantity",
        "reduceOnly",
        "price",
        "newClientOrderId",
        "stopPrice",
        "activationPrice",
        "callbackRate",
        "workingType",
        "priceProtect",
        "newOrderRespType",
        "priceMatch",
        "selfTradePreventionMode",
    ]

    @field_validator("side")
    def side_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["BUY", "SELL"]):
            raise ValueError("must be one of enum values ('BUY', 'SELL')")
        return value

    @field_validator("position_side")
    def position_side_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["BOTH", "LONG", "SHORT"]):
            raise ValueError("must be one of enum values ('BOTH', 'LONG', 'SHORT')")
        return value

    @field_validator("type")
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(
            [
                "LIMIT",
                "MARKET",
                "STOP",
                "STOP_MARKET",
                "TAKE_PROFIT",
                "TAKE_PROFIT_MARKET",
                "TRAILING_STOP_MARKET",
            ]
        ):
            raise ValueError(
                "must be one of enum values ('LIMIT', 'MARKET', 'STOP', 'STOP_MARKET', 'TAKE_PROFIT', 'TAKE_PROFIT_MARKET', 'TRAILING_STOP_MARKET')"
            )
        return value

    @field_validator("time_in_force")
    def time_in_force_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["GTC", "IOC", "FOK", "GTX"]):
            raise ValueError("must be one of enum values ('GTC', 'IOC', 'FOK', 'GTX')")
        return value

    @field_validator("reduce_only")
    def reduce_only_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["true", "false"]):
            raise ValueError("must be one of enum values ('true', 'false')")
        return value

    @field_validator("working_type")
    def working_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["MARK_PRICE", "CONTRACT_PRICE"]):
            raise ValueError(
                "must be one of enum values ('MARK_PRICE', 'CONTRACT_PRICE')"
            )
        return value

    @field_validator("price_protect")
    def price_protect_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["true", "false"]):
            raise ValueError("must be one of enum values ('true', 'false')")
        return value

    @field_validator("new_order_resp_type")
    def new_order_resp_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["ACK", "RESULT"]):
            raise ValueError("must be one of enum values ('ACK', 'RESULT')")
        return value

    @field_validator("price_match")
    def price_match_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(
            [
                "OPPONENT",
                "OPPONENT_5",
                "OPPONENT_10",
                "OPPONENT_20",
                "QUEUE",
                "QUEUE_5",
                "QUEUE_10",
                "QUEUE_20",
            ]
        ):
            raise ValueError(
                "must be one of enum values ('OPPONENT', 'OPPONENT_5', 'OPPONENT_10', 'OPPONENT_20', 'QUEUE', 'QUEUE_5', 'QUEUE_10', 'QUEUE_20')"
            )
        return value

    @field_validator("self_trade_prevention_mode")
    def self_trade_prevention_mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["EXPIRE_TAKER", "EXPIRE_MAKER", "EXPIRE_BOTH"]):
            raise ValueError(
                "must be one of enum values ('EXPIRE_TAKER', 'EXPIRE_MAKER', 'EXPIRE_BOTH')"
            )
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def is_array(cls) -> bool:
        return False

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PlaceMultipleOrdersBatchOrdersParameterInner from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set(
            [
                "additional_properties",
            ]
        )

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PlaceMultipleOrdersBatchOrdersParameterInner from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "symbol": obj.get("symbol"),
                "side": obj.get("side"),
                "positionSide": obj.get("positionSide"),
                "type": obj.get("type"),
                "timeInForce": obj.get("timeInForce"),
                "quantity": obj.get("quantity"),
                "reduceOnly": (
                    obj.get("reduceOnly")
                    if obj.get("reduceOnly") is not None
                    else "FALSE"
                ),
                "price": obj.get("price"),
                "newClientOrderId": obj.get("newClientOrderId"),
                "stopPrice": obj.get("stopPrice"),
                "activationPrice": obj.get("activationPrice"),
                "callbackRate": obj.get("callbackRate"),
                "workingType": (
                    obj.get("workingType")
                    if obj.get("workingType") is not None
                    else "CONTRACT_PRICE"
                ),
                "priceProtect": (
                    obj.get("priceProtect")
                    if obj.get("priceProtect") is not None
                    else "FALSE"
                ),
                "newOrderRespType": (
                    obj.get("newOrderRespType")
                    if obj.get("newOrderRespType") is not None
                    else "ACK"
                ),
                "priceMatch": obj.get("priceMatch"),
                "selfTradePreventionMode": (
                    obj.get("selfTradePreventionMode")
                    if obj.get("selfTradePreventionMode") is not None
                    else "EXPIRE_MAKER"
                ),
            }
        )
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj
